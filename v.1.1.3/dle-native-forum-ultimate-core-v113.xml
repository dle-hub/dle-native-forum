<?xml version="1.0" encoding="utf-8"?>
<dleplugin>
	<name>DLE Native Forum (Ultimate Core v1.1.3)</name>
	<description>Dlehub Özel - Hatasız Backend, OOP, AJAX, SEO ve Admin Panel (Bugfix)</description>
	<icon>public/adminpanel/images/forum.png</icon>
	<version>1.1.3</version>
	<dleversion>18.0</dleversion>
	<versioncompare>greater</versioncompare>
	<upgradeurl></upgradeurl>
	<filedelete>0</filedelete>
	<needplugin></needplugin>
	<mnotice>0</mnotice>
	<mysqlinstall><![CDATA[CREATE TABLE IF NOT EXISTS `{prefix}_forum_cats` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `parent_id` int(11) DEFAULT '0',
  `name` varchar(255) NOT NULL,
  `alt_name` varchar(255) NOT NULL,
  `description` varchar(500) DEFAULT '',
  `prefix_id` int(11) DEFAULT '0',
  `icon` varchar(100) DEFAULT 'fa-folder',
  `posi` int(11) DEFAULT '0',
  `topic_count` int(11) DEFAULT '0',
  `post_count` int(11) DEFAULT '0',
  `last_topic_id` int(11) DEFAULT '0',
  `last_post_id` int(11) DEFAULT '0',
  `permissions` varchar(500) DEFAULT 'all',
  `allowed_groups` varchar(500) DEFAULT '',
  `is_private` tinyint(1) DEFAULT '0',
  `read_groups` varchar(500) DEFAULT '',
  `write_groups` varchar(500) DEFAULT '',
  PRIMARY KEY (`id`),
  KEY `alt_name` (`alt_name`),
  KEY `parent_id` (`parent_id`),
  KEY `is_private` (`is_private`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_topics` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `cat_id` int(11) NOT NULL,
  `title` varchar(255) NOT NULL,
  `alt_name` varchar(255) NOT NULL,
  `description` varchar(500) DEFAULT '',
  `user_id` int(11) NOT NULL,
  `views` int(11) DEFAULT '0',
  `replies` int(11) DEFAULT '0',
  `is_pinned` tinyint(1) DEFAULT '0',
  `is_locked` tinyint(1) DEFAULT '0',
  `is_deleted` tinyint(1) DEFAULT '0',
  `last_bump_date` datetime DEFAULT NULL,
  `last_post_id` int(11) DEFAULT '0',
  `last_user_id` int(11) DEFAULT '0',
  `last_post_date` datetime DEFAULT NULL,
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `alt_name` (`alt_name`),
  KEY `cat_id` (`cat_id`),
  KEY `user_id` (`user_id`),
  KEY `last_post_date` (`last_post_date`),
  KEY `is_deleted` (`is_deleted`),
  KEY `cat_id_deleted_pinned` (`cat_id`,`is_deleted`,`is_pinned`),
  FULLTEXT KEY `ft_title` (`title`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_posts` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `topic_id` int(11) NOT NULL,
  `user_id` int(11) NOT NULL,
  `text` mediumtext NOT NULL,
  `likes` int(11) DEFAULT '0',
  `dislikes` int(11) DEFAULT '0',
  `is_deleted` tinyint(1) DEFAULT '0',
  `is_approved` tinyint(1) NOT NULL DEFAULT '1',
  `ip` varchar(45) DEFAULT '',
  `date` datetime NOT NULL,
  `updated_at` datetime DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `topic_id` (`topic_id`),
  KEY `user_id` (`user_id`),
  KEY `date` (`date`),
  KEY `topic_id_deleted` (`topic_id`,`is_deleted`),
  KEY `is_approved` (`is_approved`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_notifs` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `user_id` int(11) NOT NULL,
  `from_user` int(11) NOT NULL,
  `type` varchar(50) NOT NULL,
  `topic_id` int(11) NOT NULL,
  `post_id` int(11) DEFAULT '0',
  `payload` text DEFAULT NULL,
  `is_read` tinyint(1) DEFAULT '0',
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `user_id` (`user_id`),
  KEY `is_read` (`is_read`),
  KEY `topic_id` (`topic_id`),
  KEY `user_id_read_date` (`user_id`,`is_read`,`date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_ranks` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `title` varchar(100) NOT NULL,
  `points` int(11) NOT NULL DEFAULT '0',
  `color` varchar(20) DEFAULT '#001f3f',
  `icon` varchar(100) DEFAULT 'fa-user',
  `badge_text` varchar(50) DEFAULT '',
  PRIMARY KEY (`id`),
  UNIQUE KEY `unique_title` (`title`),
  KEY `points` (`points`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_subs` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `topic_id` int(11) NOT NULL,
  `user_id` int(11) NOT NULL,
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `user_topic` (`user_id`,`topic_id`),
  KEY `topic_id` (`topic_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_likes` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `post_id` int(11) NOT NULL,
  `user_id` int(11) NOT NULL,
  `type` enum('like','dislike') NOT NULL DEFAULT 'like',
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `user_post` (`user_id`,`post_id`),
  KEY `post_id` (`post_id`),
  KEY `post_id_type` (`post_id`,`type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_readlog` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `user_id` int(11) NOT NULL,
  `topic_id` int(11) NOT NULL,
  `last_read_post` int(11) NOT NULL,
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `user_topic` (`user_id`,`topic_id`),
  KEY `topic_id` (`topic_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_edit_history` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `post_id` int(11) NOT NULL,
  `editor_id` int(11) NOT NULL,
  `old_text` mediumtext NOT NULL,
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `post_id` (`post_id`),
  KEY `editor_id` (`editor_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_logs` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `mod_id` int(11) NOT NULL,
  `action` varchar(255) NOT NULL,
  `target_type` varchar(30) DEFAULT 'topic',
  `topic_id` int(11) DEFAULT '0',
  `post_id` int(11) DEFAULT '0',
  `date` datetime NOT NULL,
  `ip` varchar(45) NOT NULL,
  PRIMARY KEY (`id`),
  KEY `mod_id` (`mod_id`),
  KEY `date` (`date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_settings` (
  `name` varchar(100) NOT NULL,
  `value` text NOT NULL,
  PRIMARY KEY (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_banned_words` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `word` varchar(255) NOT NULL,
  `replacement` varchar(255) DEFAULT '***',
  PRIMARY KEY (`id`),
  KEY `word` (`word`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_attachments` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `post_id` int(11) NOT NULL,
  `topic_id` int(11) NOT NULL,
  `user_id` int(11) NOT NULL,
  `filename` varchar(255) NOT NULL,
  `original_name` varchar(255) NOT NULL,
  `filesize` int(11) DEFAULT '0',
  `filetype` varchar(100) DEFAULT '',
  `mime_type` varchar(100) DEFAULT '',
  `file_hash` varchar(64) DEFAULT '',
  `is_image` tinyint(1) DEFAULT '0',
  `downloads` int(11) DEFAULT '0',
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `post_id` (`post_id`),
  KEY `topic_id` (`topic_id`),
  KEY `user_id` (`user_id`),
  KEY `file_hash` (`file_hash`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_flood` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `user_id` int(11) NOT NULL DEFAULT '0',
  `ip` varchar(45) NOT NULL DEFAULT '',
  `action` varchar(50) NOT NULL DEFAULT 'post',
  `last_action` int(11) NOT NULL DEFAULT '0',
  `count` int(11) NOT NULL DEFAULT '1',
  `blocked_until` int(11) NOT NULL DEFAULT '0',
  PRIMARY KEY (`id`),
  UNIQUE KEY `user_action` (`user_id`,`action`),
  KEY `ip_action` (`ip`,`action`),
  KEY `blocked_until` (`blocked_until`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_roles` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `cat_id` int(11) NOT NULL DEFAULT '0',
  `user_id` int(11) NOT NULL DEFAULT '0',
  `group_id` int(11) NOT NULL DEFAULT '0',
  `role` enum('admin','moderator','member','banned') NOT NULL DEFAULT 'member',
  PRIMARY KEY (`id`),
  UNIQUE KEY `cat_user` (`cat_id`,`user_id`),
  KEY `cat_id` (`cat_id`),
  KEY `user_id` (`user_id`),
  KEY `group_id` (`group_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_conversations` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `subject` varchar(255) NOT NULL,
  `sender_id` int(11) NOT NULL,
  `recipient_id` int(11) NOT NULL,
  `created_at` int(11) NOT NULL,
  `updated_at` int(11) NOT NULL,
  PRIMARY KEY (`id`),
  KEY `sender_id` (`sender_id`),
  KEY `recipient_id` (`recipient_id`),
  KEY `updated_at` (`updated_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_conversation_users` (
  `conversation_id` int(11) NOT NULL,
  `user_id` int(11) NOT NULL,
  `last_read_at` int(11) DEFAULT NULL,
  `is_deleted` tinyint(1) DEFAULT '0',
  PRIMARY KEY (`conversation_id`,`user_id`),
  KEY `user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_conversations_messages` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `conversation_id` int(11) NOT NULL,
  `sender_id` int(11) NOT NULL,
  `content` text NOT NULL,
  `created_at` int(11) NOT NULL,
  PRIMARY KEY (`id`),
  KEY `conversation_id` (`conversation_id`),
  KEY `sender_id` (`sender_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- Forum tabloları arası Foreign Key'ler (aynı DB, cross-prefix sorun değil)
ALTER TABLE `{prefix}_forum_topics`
  ADD CONSTRAINT `fk_topics_cat` FOREIGN KEY (`cat_id`) REFERENCES `{prefix}_forum_cats` (`id`) ON DELETE CASCADE;

ALTER TABLE `{prefix}_forum_posts`
  ADD CONSTRAINT `fk_posts_topic` FOREIGN KEY (`topic_id`) REFERENCES `{prefix}_forum_topics` (`id`) ON DELETE CASCADE;

ALTER TABLE `{prefix}_forum_notifs`
  ADD CONSTRAINT `fk_notifs_topic` FOREIGN KEY (`topic_id`) REFERENCES `{prefix}_forum_topics` (`id`) ON DELETE CASCADE;

ALTER TABLE `{prefix}_forum_subs`
  ADD CONSTRAINT `fk_subs_topic` FOREIGN KEY (`topic_id`) REFERENCES `{prefix}_forum_topics` (`id`) ON DELETE CASCADE;

ALTER TABLE `{prefix}_forum_likes`
  ADD CONSTRAINT `fk_likes_post` FOREIGN KEY (`post_id`) REFERENCES `{prefix}_forum_posts` (`id`) ON DELETE CASCADE;

ALTER TABLE `{prefix}_forum_readlog`
  ADD CONSTRAINT `fk_readlog_topic` FOREIGN KEY (`topic_id`) REFERENCES `{prefix}_forum_topics` (`id`) ON DELETE CASCADE;

ALTER TABLE `{prefix}_forum_edit_history`
  ADD CONSTRAINT `fk_edithistory_post` FOREIGN KEY (`post_id`) REFERENCES `{prefix}_forum_posts` (`id`) ON DELETE CASCADE;

ALTER TABLE `{prefix}_forum_attachments`
  ADD CONSTRAINT `fk_attachments_post` FOREIGN KEY (`post_id`) REFERENCES `{prefix}_forum_posts` (`id`) ON DELETE CASCADE;

ALTER TABLE `{prefix}_forum_roles`
  ADD CONSTRAINT `fk_roles_cat` FOREIGN KEY (`cat_id`) REFERENCES `{prefix}_forum_cats` (`id`) ON DELETE CASCADE;

ALTER TABLE `{prefix}_conversation_users`
  ADD CONSTRAINT `fk_convusers_conv` FOREIGN KEY (`conversation_id`) REFERENCES `{prefix}_conversations` (`id`) ON DELETE CASCADE;

ALTER TABLE `{prefix}_conversations_messages`
  ADD CONSTRAINT `fk_convmsgs_conv` FOREIGN KEY (`conversation_id`) REFERENCES `{prefix}_conversations` (`id`) ON DELETE CASCADE;

-- Mevcut kurulumlar için ALTER (upgrade desteği)
ALTER TABLE `{prefix}_users` ADD COLUMN IF NOT EXISTS `forum_points` INT(11) DEFAULT '0';
ALTER TABLE `{prefix}_users` ADD COLUMN IF NOT EXISTS `forum_rank_id` INT(11) DEFAULT '0';
ALTER TABLE `{prefix}_users` ADD COLUMN IF NOT EXISTS `forum_post_count` INT(11) DEFAULT '0';
ALTER TABLE `{prefix}_forum_posts` ADD COLUMN IF NOT EXISTS `dislikes` INT(11) DEFAULT '0';
ALTER TABLE `{prefix}_forum_likes` ADD COLUMN IF NOT EXISTS `type` ENUM('like','dislike') NOT NULL DEFAULT 'like';
ALTER TABLE `{prefix}_forum_notifs` ADD COLUMN IF NOT EXISTS `payload` TEXT DEFAULT NULL;
ALTER TABLE `{prefix}_forum_attachments` ADD COLUMN IF NOT EXISTS `mime_type` VARCHAR(100) DEFAULT '';
ALTER TABLE `{prefix}_forum_attachments` ADD COLUMN IF NOT EXISTS `file_hash` VARCHAR(64) DEFAULT '';
ALTER TABLE `{prefix}_forum_cats` ADD COLUMN IF NOT EXISTS `is_private` TINYINT(1) DEFAULT '0';
ALTER TABLE `{prefix}_forum_cats` ADD COLUMN IF NOT EXISTS `allowed_groups` VARCHAR(500) DEFAULT '';
ALTER TABLE `{prefix}_forum_cats` ADD COLUMN IF NOT EXISTS `read_groups` VARCHAR(500) DEFAULT '';
ALTER TABLE `{prefix}_forum_cats` ADD COLUMN IF NOT EXISTS `write_groups` VARCHAR(500) DEFAULT '';

-- Index eklemeleri (mevcut kurulumlar için)
ALTER TABLE `{prefix}_forum_posts` ADD INDEX IF NOT EXISTS `date` (`date`);
ALTER TABLE `{prefix}_forum_posts` ADD INDEX IF NOT EXISTS `topic_id_deleted` (`topic_id`,`is_deleted`);
ALTER TABLE `{prefix}_forum_posts` ADD INDEX IF NOT EXISTS `is_approved` (`is_approved`);
ALTER TABLE `{prefix}_forum_topics` ADD INDEX IF NOT EXISTS `is_deleted` (`is_deleted`);
ALTER TABLE `{prefix}_forum_topics` ADD INDEX IF NOT EXISTS `cat_id_deleted_pinned` (`cat_id`,`is_deleted`,`is_pinned`);
ALTER TABLE `{prefix}_forum_notifs` ADD INDEX IF NOT EXISTS `topic_id` (`topic_id`);
ALTER TABLE `{prefix}_forum_notifs` ADD INDEX IF NOT EXISTS `user_id_read_date` (`user_id`,`is_read`,`date`);
ALTER TABLE `{prefix}_forum_likes` ADD INDEX IF NOT EXISTS `post_id` (`post_id`);
ALTER TABLE `{prefix}_forum_likes` ADD INDEX IF NOT EXISTS `post_id_type` (`post_id`,`type`);
ALTER TABLE `{prefix}_forum_subs` ADD INDEX IF NOT EXISTS `topic_id` (`topic_id`);
ALTER TABLE `{prefix}_forum_readlog` ADD INDEX IF NOT EXISTS `topic_id` (`topic_id`);
ALTER TABLE `{prefix}_forum_edit_history` ADD INDEX IF NOT EXISTS `editor_id` (`editor_id`);
ALTER TABLE `{prefix}_forum_logs` ADD INDEX IF NOT EXISTS `date` (`date`);
ALTER TABLE `{prefix}_forum_attachments` ADD INDEX IF NOT EXISTS `file_hash` (`file_hash`);
ALTER TABLE `{prefix}_forum_banned_words` ADD INDEX IF NOT EXISTS `word` (`word`);
ALTER TABLE `{prefix}_conversations` ADD INDEX IF NOT EXISTS `updated_at` (`updated_at`);
ALTER TABLE `{prefix}_conversation_users` ADD INDEX IF NOT EXISTS `user_id` (`user_id`);
ALTER TABLE `{prefix}_conversations_messages` ADD INDEX IF NOT EXISTS `sender_id` (`sender_id`);

INSERT IGNORE INTO `{prefix}_forum_settings` (`name`, `value`) VALUES
('posts_per_page', '20'),
('topics_per_page', '25'),
('allow_guests_view', '1'),
('min_post_length', '10'),
('max_post_length', '10000'),
('allow_attachments', '1'),
('max_attachment_size', '5242880'),
('allowed_filetypes', 'jpg,jpeg,png,gif,webp,pdf,zip,rar,doc,docx'),
('flood_time', '30'),
('flood_ip_time', '60'),
('flood_max_per_hour', '30'),
('points_new_topic', '10'),
('points_new_reply', '2'),
('points_received_like', '5'),
('approval_threshold', '0'),
('bump_cooldown_hours', '24'),
('allow_private_cats', '1'),
('attachment_hash_check', '1');

INSERT IGNORE INTO `{prefix}_forum_ranks` (`title`, `points`, `color`, `icon`, `badge_text`) VALUES
('Yeni Üye', 0, '#888888', 'fa-user', 'Yeni'),
('Aktif Üye', 100, '#27ae60', 'fa-user-plus', 'Aktif'),
('Uzman', 500, '#2980b9', 'fa-star', 'Uzman'),
('Efsane Üye', 2000, '#8e44ad', 'fa-trophy', 'Efsane'),
('Moderatör', 9999, '#e74c3c', 'fa-shield', 'Mod');

-- v1.1.3 Yeni Tablolar
CREATE TABLE IF NOT EXISTS `{prefix}_forum_prefixes` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `cat_id` int(11) NOT NULL DEFAULT '0',
  `title` varchar(50) NOT NULL,
  `color` varchar(20) DEFAULT '#3498db',
  `bg_color` varchar(20) DEFAULT '#ebf5fb',
  `posi` int(11) DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `cat_id` (`cat_id`),
  KEY `posi` (`posi`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_tags` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `name` varchar(50) NOT NULL,
  `slug` varchar(50) NOT NULL,
  `count` int(11) DEFAULT '0',
  PRIMARY KEY (`id`),
  UNIQUE KEY `slug` (`slug`),
  KEY `count` (`count`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_topic_tags` (
  `topic_id` int(11) NOT NULL,
  `tag_id` int(11) NOT NULL,
  PRIMARY KEY (`topic_id`,`tag_id`),
  KEY `tag_id` (`tag_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

INSERT IGNORE INTO `{prefix}_admin_sections` (`name`, `title`, `descr`, `icon`, `allow_groups`) VALUES ('forum', 'Forum Yönetimi', 'DLE Native Forum v1.1.2', 'forum.png', '1');

CREATE TABLE IF NOT EXISTS `{prefix}_forum_visitors` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `user_id` int(11) NOT NULL DEFAULT '0',
  `ip` varchar(45) NOT NULL DEFAULT '',
  `page` varchar(255) DEFAULT '',
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_date` (`date`),
  KEY `idx_date_ip` (`date`,`ip`),
  KEY `idx_date_user` (`date`,`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;]]></mysqlinstall>
	<mysqlupgrade><![CDATA[ALTER TABLE `{prefix}_forum_topics` ADD COLUMN IF NOT EXISTS `description` varchar(500) DEFAULT '' AFTER `alt_name`;
ALTER TABLE `{prefix}_forum_topics` DROP INDEX IF EXISTS `ft_title`;
ALTER TABLE `{prefix}_forum_topics` ADD FULLTEXT INDEX `ft_title` (`title`);

ALTER TABLE `{prefix}_users` ADD COLUMN IF NOT EXISTS `forum_points` INT(11) DEFAULT '0';
ALTER TABLE `{prefix}_users` ADD COLUMN IF NOT EXISTS `forum_rank_id` INT(11) DEFAULT '0';
ALTER TABLE `{prefix}_users` ADD COLUMN IF NOT EXISTS `forum_post_count` INT(11) DEFAULT '0';
ALTER TABLE `{prefix}_forum_posts` ADD COLUMN IF NOT EXISTS `dislikes` INT(11) DEFAULT '0';
ALTER TABLE `{prefix}_forum_likes` ADD COLUMN IF NOT EXISTS `type` ENUM('like','dislike') NOT NULL DEFAULT 'like';

CREATE TABLE IF NOT EXISTS `{prefix}_forum_cats` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `parent_id` int(11) DEFAULT '0',
  `name` varchar(255) NOT NULL,
  `alt_name` varchar(255) NOT NULL,
  `description` varchar(500) DEFAULT '',
  `icon` varchar(100) DEFAULT 'fa-folder',
  `posi` int(11) DEFAULT '0',
  `topic_count` int(11) DEFAULT '0',
  `post_count` int(11) DEFAULT '0',
  `last_topic_id` int(11) DEFAULT '0',
  `last_post_id` int(11) DEFAULT '0',
  `permissions` varchar(100) DEFAULT 'all',
  PRIMARY KEY (`id`),
  KEY `alt_name` (`alt_name`),
  KEY `parent_id` (`parent_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_topics` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `cat_id` int(11) NOT NULL,
  `title` varchar(255) NOT NULL,
  `alt_name` varchar(255) NOT NULL,
  `user_id` int(11) NOT NULL,
  `views` int(11) DEFAULT '0',
  `replies` int(11) DEFAULT '0',
  `is_pinned` tinyint(1) DEFAULT '0',
  `is_locked` tinyint(1) DEFAULT '0',
  `is_deleted` tinyint(1) DEFAULT '0',
  `last_bump_date` datetime DEFAULT NULL,
  `last_post_id` int(11) DEFAULT '0',
  `last_user_id` int(11) DEFAULT '0',
  `last_post_date` datetime DEFAULT NULL,
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `alt_name` (`alt_name`),
  KEY `cat_id` (`cat_id`),
  KEY `user_id` (`user_id`),
  KEY `last_post_date` (`last_post_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_posts` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `topic_id` int(11) NOT NULL,
  `user_id` int(11) NOT NULL,
  `text` mediumtext NOT NULL,
  `likes` int(11) DEFAULT '0',
  `dislikes` int(11) DEFAULT '0',
  `is_deleted` tinyint(1) DEFAULT '0',
  `is_approved` tinyint(1) NOT NULL DEFAULT '1',
  `ip` varchar(45) DEFAULT '',
  `date` datetime NOT NULL,
  `updated_at` datetime DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `topic_id` (`topic_id`),
  KEY `user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_notifs` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `user_id` int(11) NOT NULL,
  `from_user` int(11) NOT NULL,
  `type` varchar(50) NOT NULL,
  `topic_id` int(11) NOT NULL,
  `post_id` int(11) DEFAULT '0',
  `is_read` tinyint(1) DEFAULT '0',
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `user_id` (`user_id`),
  KEY `is_read` (`is_read`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_ranks` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `title` varchar(100) NOT NULL,
  `points` int(11) NOT NULL DEFAULT '0',
  `color` varchar(20) DEFAULT '#001f3f',
  `icon` varchar(100) DEFAULT 'fa-user',
  `badge_text` varchar(50) DEFAULT '',
  PRIMARY KEY (`id`),
  UNIQUE KEY `unique_title` (`title`),
  KEY `points` (`points`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_subs` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `topic_id` int(11) NOT NULL,
  `user_id` int(11) NOT NULL,
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `user_topic` (`user_id`,`topic_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_likes` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `post_id` int(11) NOT NULL,
  `user_id` int(11) NOT NULL,
  `type` enum('like','dislike') NOT NULL DEFAULT 'like',
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `user_post` (`user_id`,`post_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_readlog` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `user_id` int(11) NOT NULL,
  `topic_id` int(11) NOT NULL,
  `last_read_post` int(11) NOT NULL,
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `user_topic` (`user_id`,`topic_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_edit_history` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `post_id` int(11) NOT NULL,
  `editor_id` int(11) NOT NULL,
  `old_text` mediumtext NOT NULL,
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `post_id` (`post_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_logs` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `mod_id` int(11) NOT NULL,
  `action` varchar(255) NOT NULL,
  `target_type` varchar(30) DEFAULT 'topic',
  `topic_id` int(11) DEFAULT '0',
  `post_id` int(11) DEFAULT '0',
  `date` datetime NOT NULL,
  `ip` varchar(45) NOT NULL,
  PRIMARY KEY (`id`),
  KEY `mod_id` (`mod_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_settings` (
  `name` varchar(100) NOT NULL,
  `value` text NOT NULL,
  PRIMARY KEY (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_banned_words` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `word` varchar(255) NOT NULL,
  `replacement` varchar(255) DEFAULT '***',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_attachments` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `post_id` int(11) NOT NULL,
  `topic_id` int(11) NOT NULL,
  `user_id` int(11) NOT NULL,
  `filename` varchar(255) NOT NULL,
  `original_name` varchar(255) NOT NULL,
  `filesize` int(11) DEFAULT '0',
  `filetype` varchar(100) DEFAULT '',
  `is_image` tinyint(1) DEFAULT '0',
  `downloads` int(11) DEFAULT '0',
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `post_id` (`post_id`),
  KEY `topic_id` (`topic_id`),
  KEY `user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_conversations` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `subject` varchar(255) NOT NULL,
  `sender_id` int(11) NOT NULL,
  `recipient_id` int(11) NOT NULL,
  `created_at` int(11) NOT NULL,
  `updated_at` int(11) NOT NULL,
  PRIMARY KEY (`id`),
  KEY `sender_id` (`sender_id`),
  KEY `recipient_id` (`recipient_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_conversation_users` (
  `conversation_id` int(11) NOT NULL,
  `user_id` int(11) NOT NULL,
  `last_read_at` int(11) DEFAULT NULL,
  `is_deleted` tinyint(1) DEFAULT '0',
  PRIMARY KEY (`conversation_id`,`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

ALTER TABLE `{prefix}_conversation_users` MODIFY `last_read_at` int(11) DEFAULT NULL;

CREATE TABLE IF NOT EXISTS `{prefix}_conversations_messages` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `conversation_id` int(11) NOT NULL,
  `sender_id` int(11) NOT NULL,
  `content` text NOT NULL,
  `created_at` int(11) NOT NULL,
  PRIMARY KEY (`id`),
  KEY `conversation_id` (`conversation_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

INSERT IGNORE INTO `{prefix}_forum_settings` (`name`, `value`) VALUES
('posts_per_page', '20'),
('topics_per_page', '25'),
('allow_guests_view', '1'),
('min_post_length', '10'),
('max_post_length', '10000'),
('allow_attachments', '1'),
('max_attachment_size', '5242880'),
('allowed_filetypes', 'jpg,jpeg,png,gif,webp,pdf,zip,rar,doc,docx'),
('flood_time', '30'),
('points_new_topic', '10'),
('points_new_reply', '2'),
('points_received_like', '5'),
('approval_threshold', '0'),
('bump_cooldown_hours', '24');

INSERT IGNORE INTO `{prefix}_admin_sections` (`name`, `title`, `descr`, `icon`, `allow_groups`) VALUES ('forum', 'Forum Yönetimi', 'DLE Native Forum Ultimate v1.1.2', 'forum.png', '1');

DROP TABLE IF EXISTS `{prefix}_forum_flood`;
CREATE TABLE IF NOT EXISTS `{prefix}_forum_flood` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `user_id` int(11) NOT NULL DEFAULT '0',
  `ip` varchar(45) NOT NULL DEFAULT '',
  `action` varchar(50) NOT NULL DEFAULT 'post',
  `last_action` int(11) NOT NULL DEFAULT '0',
  `count` int(11) NOT NULL DEFAULT '1',
  `blocked_until` int(11) NOT NULL DEFAULT '0',
  PRIMARY KEY (`id`),
  UNIQUE KEY `user_action` (`user_id`,`action`),
  KEY `ip_action` (`ip`,`action`),
  KEY `blocked_until` (`blocked_until`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_visitors` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `user_id` int(11) NOT NULL DEFAULT '0',
  `ip` varchar(45) NOT NULL DEFAULT '',
  `page` varchar(255) DEFAULT '',
  `date` datetime NOT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_date` (`date`),
  KEY `idx_date_ip` (`date`,`ip`),
  KEY `idx_date_user` (`date`,`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

ALTER TABLE `{prefix}_forum_topics` ADD COLUMN IF NOT EXISTS `prefix_id` INT(11) DEFAULT '0';
ALTER TABLE `{prefix}_forum_topics` ADD COLUMN IF NOT EXISTS `description` VARCHAR(500) DEFAULT '';

-- v1.1.3 Yeni tablolar (etiket ve önek sistemi)
CREATE TABLE IF NOT EXISTS `{prefix}_forum_prefixes` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `cat_id` int(11) NOT NULL DEFAULT '0',
  `title` varchar(50) NOT NULL,
  `color` varchar(20) DEFAULT '#3498db',
  `bg_color` varchar(20) DEFAULT '#ebf5fb',
  `posi` int(11) DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `cat_id` (`cat_id`),
  KEY `posi` (`posi`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_tags` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `name` varchar(50) NOT NULL,
  `slug` varchar(50) NOT NULL,
  `count` int(11) DEFAULT '0',
  PRIMARY KEY (`id`),
  UNIQUE KEY `slug` (`slug`),
  KEY `count` (`count`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS `{prefix}_forum_topic_tags` (
  `topic_id` int(11) NOT NULL,
  `tag_id` int(11) NOT NULL,
  PRIMARY KEY (`topic_id`,`tag_id`),
  KEY `tag_id` (`tag_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- v1.1.3 Sticky OP Ayarı Enjeksiyonu
INSERT IGNORE INTO `{prefix}_forum_settings` (`name`, `value`) VALUES ('sticky_op', '1');]]></mysqlupgrade>
	<mysqlenable><![CDATA[INSERT IGNORE INTO `{prefix}_admin_sections`
(`name`, `title`, `descr`, `icon`, `allow_groups`) 
VALUES 
('forum', 'Forum Yönetimi', 'DLE Native Forum', 'forum.png', '1');]]></mysqlenable>
	<mysqldisable><![CDATA[DELETE FROM `{prefix}_admin_sections` 
WHERE name='forum';]]></mysqldisable>
	<mysqldelete><![CDATA[-- Forum Tabloları
DROP TABLE IF EXISTS `{prefix}_forum_posts`;
DROP TABLE IF EXISTS `{prefix}_forum_topics`;
DROP TABLE IF EXISTS `{prefix}_forum_cats`;
DROP TABLE IF EXISTS `{prefix}_forum_notifs`;
DROP TABLE IF EXISTS `{prefix}_forum_ranks`;
DROP TABLE IF EXISTS `{prefix}_forum_subs`;
DROP TABLE IF EXISTS `{prefix}_forum_likes`;
DROP TABLE IF EXISTS `{prefix}_forum_readlog`;
DROP TABLE IF EXISTS `{prefix}_forum_edit_history`;
DROP TABLE IF EXISTS `{prefix}_forum_pm`;
DROP TABLE IF EXISTS `{prefix}_forum_logs`;
DROP TABLE IF EXISTS `{prefix}_forum_settings`;
DROP TABLE IF EXISTS `{prefix}_forum_banned_words`;
DROP TABLE IF EXISTS `{prefix}_forum_attachments`;

-- v1.1.3 Yeni Tablolar
DROP TABLE IF EXISTS `{prefix}_forum_prefixes`;
DROP TABLE IF EXISTS `{prefix}_forum_tags`;
DROP TABLE IF EXISTS `{prefix}_forum_topic_tags`;

-- Conversation Tabloları
DROP TABLE IF EXISTS `{prefix}_conversations`;
DROP TABLE IF EXISTS `{prefix}_conversation_users`;
DROP TABLE IF EXISTS `{prefix}_conversations_messages`;

-- Kullanıcı Sütunları
ALTER TABLE `{prefix}_users` DROP COLUMN IF EXISTS `forum_points`;
ALTER TABLE `{prefix}_users` DROP COLUMN IF EXISTS `forum_rank_id`;
ALTER TABLE `{prefix}_users` DROP COLUMN IF EXISTS `forum_post_count`;

-- Admin Menü
DELETE FROM `{prefix}_admin_sections` WHERE name='forum';

-- Diğer
DROP TABLE IF EXISTS `{prefix}_forum_flood`;
DROP TABLE IF EXISTS `{prefix}_forum_visitors`;]]></mysqldelete>
	<phpinstall><![CDATA[function forum_seo_write() {

    // ── DLE 19+: DLEUrl sistemi ──────────────────────────────────────────────
    if (class_exists('DLEUrl')) {

        $rules = [
            'forum_main','forum_cat','forum_topic','forum_user',
            'forum_new_topic','forum_search','forum_notifications',
            'forum_pm','forum_pm_read','forum_ajax'
        ];
        foreach ($rules as $r) {
            DLEUrl::DeleteRule('custom.' . $r);
        }

        DLEUrl::AddRule('forum_main',          '/forum/',                                     'index.php?do=forum');
        DLEUrl::AddRule('forum_new_topic',     '/forum/new-topic/',                           'index.php?do=forum&action=new_topic');
        DLEUrl::AddRule('forum_search',        '/forum/search/',                              'index.php?do=forum&action=search');
        DLEUrl::AddRule('forum_notifications', '/forum/notifications/',                       'index.php?do=forum&action=notifications');
        DLEUrl::AddRule('forum_pm',            '/forum/pm/',                                  'index.php?do=forum&action=pm');
        DLEUrl::AddRule('forum_ajax',          '/forum/ajax/',                                'index.php?do=forum&action=ajax');
        DLEUrl::AddRule('forum_user',          '/forum/user/{user}/',                         'index.php?do=forum&action=user_posts&user={user}');
        DLEUrl::AddRule('forum_pm_read',       '/forum/pm/read/{newsid}/',                    'index.php?do=forum&action=pm&sub=read&id={newsid}');
        DLEUrl::AddRule('forum_topic',         '/forum/{category}/{newsid}-{news_name}.html', 'index.php?do=forum&action=topic&id={newsid}');
        DLEUrl::AddRule('forum_cat',           '/forum/{category}/',                          'index.php?do=forum&action=category&cat={category}');

        if (is_array(DLEUrl::$rules)) {
            $custom_rules = [];
            $other_rules  = [];
            foreach (DLEUrl::$rules as $k => $v) {
                if (strpos($k, 'custom.forum_') === 0) $custom_rules[$k] = $v;
                else $other_rules[$k] = $v;
            }
            DLEUrl::$rules = array_merge($custom_rules, $other_rules);
            DLEUrl::SaveRules(DLEUrl::$rules);
        }

    // ── DLE 18: .htaccess mod_rewrite ────────────────────────────────────────
    } else {

        $htaccess_file  = ROOT_DIR . '/.htaccess';
        $marker_start   = '# BEGIN DLE-Forum-SEO';
        $marker_end     = '# END DLE-Forum-SEO';

        $forum_block = $marker_start . '
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteRule ^forum/?$ index.php?do=forum [L,QSA]
RewriteRule ^forum/new-topic/?$ index.php?do=forum&action=new_topic [L,QSA]
RewriteRule ^forum/search/?$ index.php?do=forum&action=search [L,QSA]
RewriteRule ^forum/notifications/?$ index.php?do=forum&action=notifications [L,QSA]
RewriteRule ^forum/pm/?$ index.php?do=forum&action=pm [L,QSA]
RewriteRule ^forum/pm/read/([0-9]+)/?$ index.php?do=forum&action=pm&sub=read&id=$1 [L,QSA]
RewriteRule ^forum/ajax/?$ index.php?do=forum&action=ajax [L,QSA]
RewriteRule ^forum/user/([^/]+)/?$ index.php?do=forum&action=user_posts&user=$1 [L,QSA]
RewriteRule ^forum/([^/]+)/([0-9]+)-([^/]+)\.html$ index.php?do=forum&action=topic&id=$2 [L,QSA]
RewriteRule ^forum/([^/]+)/?$ index.php?do=forum&action=category&cat=$1 [L,QSA]
</IfModule>
' . $marker_end;

        // Mevcut htaccess'i oku (yoksa boş başla)
        $current = file_exists($htaccess_file) ? file_get_contents($htaccess_file) : '';

        // Marker'lı bloğu temizle (varsa — upgrade güvenliği)
        $pattern = '/' . preg_quote($marker_start, '/') . '.*?' . preg_quote($marker_end, '/') . '\s*/s';
        $current = preg_replace($pattern, '', $current);

        // Marker'sız eski satırları da temizle (marker silinmiş kenar durum)
        // forum/... RewriteRule satırlarını bul ve kaldır
        $current = preg_replace('/^RewriteRule\s+\^forum\b.*$/m', '', $current);

        // Boş satır birikimini temizle (3+ boş satır → 1 boş satır)
        $current = preg_replace('/\n{3,}/', "\n\n", trim($current));

        // Forum bloğunu en üste yaz (DLE'nin kendi kurallarından önce işlenmeli)
        $new_content = $forum_block . "\n\n" . $current;
        file_put_contents($htaccess_file, $new_content);
    }
}

forum_seo_write();]]></phpinstall>
	<phpupgrade><![CDATA[function forum_seo_write() {

    // ── DLE 19+: DLEUrl sistemi ──────────────────────────────────────────────
    if (class_exists('DLEUrl')) {

        $rules = [
            'forum_main','forum_cat','forum_topic','forum_user',
            'forum_new_topic','forum_search','forum_notifications',
            'forum_pm','forum_pm_read','forum_ajax'
        ];
        foreach ($rules as $r) {
            DLEUrl::DeleteRule('custom.' . $r);
        }

        DLEUrl::AddRule('forum_main',          '/forum/',                                     'index.php?do=forum');
        DLEUrl::AddRule('forum_new_topic',     '/forum/new-topic/',                           'index.php?do=forum&action=new_topic');
        DLEUrl::AddRule('forum_search',        '/forum/search/',                              'index.php?do=forum&action=search');
        DLEUrl::AddRule('forum_notifications', '/forum/notifications/',                       'index.php?do=forum&action=notifications');
        DLEUrl::AddRule('forum_pm',            '/forum/pm/',                                  'index.php?do=forum&action=pm');
        DLEUrl::AddRule('forum_ajax',          '/forum/ajax/',                                'index.php?do=forum&action=ajax');
        DLEUrl::AddRule('forum_user',          '/forum/user/{user}/',                         'index.php?do=forum&action=user_posts&user={user}');
        DLEUrl::AddRule('forum_pm_read',       '/forum/pm/read/{newsid}/',                    'index.php?do=forum&action=pm&sub=read&id={newsid}');
        DLEUrl::AddRule('forum_topic',         '/forum/{category}/{newsid}-{news_name}.html', 'index.php?do=forum&action=topic&id={newsid}');
        DLEUrl::AddRule('forum_cat',           '/forum/{category}/',                          'index.php?do=forum&action=category&cat={category}');

        if (is_array(DLEUrl::$rules)) {
            $custom_rules = [];
            $other_rules  = [];
            foreach (DLEUrl::$rules as $k => $v) {
                if (strpos($k, 'custom.forum_') === 0) $custom_rules[$k] = $v;
                else $other_rules[$k] = $v;
            }
            DLEUrl::$rules = array_merge($custom_rules, $other_rules);
            DLEUrl::SaveRules(DLEUrl::$rules);
        }

    // ── DLE 18: .htaccess mod_rewrite ────────────────────────────────────────
    } else {

        $htaccess_file  = ROOT_DIR . '/.htaccess';
        $marker_start   = '# BEGIN DLE-Forum-SEO';
        $marker_end     = '# END DLE-Forum-SEO';

        $forum_block = $marker_start . '
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteRule ^forum/?$ index.php?do=forum [L,QSA]
RewriteRule ^forum/new-topic/?$ index.php?do=forum&action=new_topic [L,QSA]
RewriteRule ^forum/search/?$ index.php?do=forum&action=search [L,QSA]
RewriteRule ^forum/notifications/?$ index.php?do=forum&action=notifications [L,QSA]
RewriteRule ^forum/pm/?$ index.php?do=forum&action=pm [L,QSA]
RewriteRule ^forum/pm/read/([0-9]+)/?$ index.php?do=forum&action=pm&sub=read&id=$1 [L,QSA]
RewriteRule ^forum/ajax/?$ index.php?do=forum&action=ajax [L,QSA]
RewriteRule ^forum/user/([^/]+)/?$ index.php?do=forum&action=user_posts&user=$1 [L,QSA]
RewriteRule ^forum/([^/]+)/([0-9]+)-([^/]+)\.html$ index.php?do=forum&action=topic&id=$2 [L,QSA]
RewriteRule ^forum/([^/]+)/?$ index.php?do=forum&action=category&cat=$1 [L,QSA]
</IfModule>
' . $marker_end;

        // Mevcut htaccess'i oku (yoksa boş başla)
        $current = file_exists($htaccess_file) ? file_get_contents($htaccess_file) : '';

        // Marker'lı bloğu temizle (varsa — upgrade güvenliği)
        $pattern = '/' . preg_quote($marker_start, '/') . '.*?' . preg_quote($marker_end, '/') . '\s*/s';
        $current = preg_replace($pattern, '', $current);

        // Marker'sız eski satırları da temizle (marker silinmiş kenar durum)
        // forum/... RewriteRule satırlarını bul ve kaldır
        $current = preg_replace('/^RewriteRule\s+\^forum\b.*$/m', '', $current);

        // Boş satır birikimini temizle (3+ boş satır → 1 boş satır)
        $current = preg_replace('/\n{3,}/', "\n\n", trim($current));

        // Forum bloğunu en üste yaz (DLE'nin kendi kurallarından önce işlenmeli)
        $new_content = $forum_block . "\n\n" . $current;
        file_put_contents($htaccess_file, $new_content);
    }
}

forum_seo_write();]]></phpupgrade>
	<phpenable><![CDATA[function forum_seo_write() {

    // ── DLE 19+: DLEUrl sistemi ──────────────────────────────────────────────
    if (class_exists('DLEUrl')) {

        $rules = [
            'forum_main','forum_cat','forum_topic','forum_user',
            'forum_new_topic','forum_search','forum_notifications',
            'forum_pm','forum_pm_read','forum_ajax'
        ];
        foreach ($rules as $r) {
            DLEUrl::DeleteRule('custom.' . $r);
        }

        DLEUrl::AddRule('forum_main',          '/forum/',                                     'index.php?do=forum');
        DLEUrl::AddRule('forum_new_topic',     '/forum/new-topic/',                           'index.php?do=forum&action=new_topic');
        DLEUrl::AddRule('forum_search',        '/forum/search/',                              'index.php?do=forum&action=search');
        DLEUrl::AddRule('forum_notifications', '/forum/notifications/',                       'index.php?do=forum&action=notifications');
        DLEUrl::AddRule('forum_pm',            '/forum/pm/',                                  'index.php?do=forum&action=pm');
        DLEUrl::AddRule('forum_ajax',          '/forum/ajax/',                                'index.php?do=forum&action=ajax');
        DLEUrl::AddRule('forum_user',          '/forum/user/{user}/',                         'index.php?do=forum&action=user_posts&user={user}');
        DLEUrl::AddRule('forum_pm_read',       '/forum/pm/read/{newsid}/',                    'index.php?do=forum&action=pm&sub=read&id={newsid}');
        DLEUrl::AddRule('forum_topic',         '/forum/{category}/{newsid}-{news_name}.html', 'index.php?do=forum&action=topic&id={newsid}');
        DLEUrl::AddRule('forum_cat',           '/forum/{category}/',                          'index.php?do=forum&action=category&cat={category}');

        if (is_array(DLEUrl::$rules)) {
            $custom_rules = [];
            $other_rules  = [];
            foreach (DLEUrl::$rules as $k => $v) {
                if (strpos($k, 'custom.forum_') === 0) $custom_rules[$k] = $v;
                else $other_rules[$k] = $v;
            }
            DLEUrl::$rules = array_merge($custom_rules, $other_rules);
            DLEUrl::SaveRules(DLEUrl::$rules);
        }

    } else {

        $htaccess_file  = ROOT_DIR . '/.htaccess';
        $marker_start   = '# BEGIN DLE-Forum-SEO';
        $marker_end     = '# END DLE-Forum-SEO';

        $forum_block = $marker_start . '
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteRule ^forum/?$ index.php?do=forum [L,QSA]
RewriteRule ^forum/new-topic/?$ index.php?do=forum&action=new_topic [L,QSA]
RewriteRule ^forum/search/?$ index.php?do=forum&action=search [L,QSA]
RewriteRule ^forum/notifications/?$ index.php?do=forum&action=notifications [L,QSA]
RewriteRule ^forum/pm/?$ index.php?do=forum&action=pm [L,QSA]
RewriteRule ^forum/pm/read/([0-9]+)/?$ index.php?do=forum&action=pm&sub=read&id=$1 [L,QSA]
RewriteRule ^forum/ajax/?$ index.php?do=forum&action=ajax [L,QSA]
RewriteRule ^forum/user/([^/]+)/?$ index.php?do=forum&action=user_posts&user=$1 [L,QSA]
RewriteRule ^forum/([^/]+)/([0-9]+)-([^/]+)\.html$ index.php?do=forum&action=topic&id=$2 [L,QSA]
RewriteRule ^forum/([^/]+)/?$ index.php?do=forum&action=category&cat=$1 [L,QSA]
</IfModule>
' . $marker_end;

        $current = file_exists($htaccess_file) ? file_get_contents($htaccess_file) : '';
        $pattern = '/' . preg_quote($marker_start, '/') . '.*?' . preg_quote($marker_end, '/') . '\s*/s';
        $current = preg_replace($pattern, '', $current);
        $current = preg_replace('/^RewriteRule\s+\^forum\b.*$/m', '', $current);
        $current = preg_replace('/\n{3,}/', "\n\n", trim($current));
        $new_content = $forum_block . "\n\n" . $current;
        file_put_contents($htaccess_file, $new_content);
    }
}

forum_seo_write();]]></phpenable>
	<phpdisable><![CDATA[function forum_seo_remove() {

    if (class_exists('DLEUrl')) {

        $rules = [
            'forum_main','forum_cat','forum_topic','forum_user',
            'forum_new_topic','forum_search','forum_notifications',
            'forum_pm','forum_pm_read','forum_ajax'
        ];
        foreach ($rules as $r) {
            DLEUrl::DeleteRule('custom.' . $r);
        }

    } else {

        $htaccess_file = ROOT_DIR . '/.htaccess';
        $marker_start  = '# BEGIN DLE-Forum-SEO';
        $marker_end    = '# END DLE-Forum-SEO';

        if (!file_exists($htaccess_file)) return;

        $current = file_get_contents($htaccess_file);

        // Marker'lı bloğu sil
        $pattern = '/' . preg_quote($marker_start, '/') . '.*?' . preg_quote($marker_end, '/') . '\s*/s';
        $current = preg_replace($pattern, '', $current);

        // Markersız eski forum satırları kalmışsa onları da temizle
        $current = preg_replace('/^RewriteRule\s+\^forum\b.*$/m', '', $current);

        // Boş satır birikimini temizle
        $current = preg_replace('/\n{3,}/', "\n\n", trim($current));

        file_put_contents($htaccess_file, $current . "\n");
    }
}

forum_seo_remove();]]></phpdisable>
	<phpdelete><![CDATA[function forum_seo_remove() {

    if (class_exists('DLEUrl')) {

        $rules = [
            'forum_main','forum_cat','forum_topic','forum_user',
            'forum_new_topic','forum_search','forum_notifications',
            'forum_pm','forum_pm_read','forum_ajax'
        ];
        foreach ($rules as $r) {
            DLEUrl::DeleteRule('custom.' . $r);
        }

    } else {

        $htaccess_file = ROOT_DIR . '/.htaccess';
        $marker_start  = '# BEGIN DLE-Forum-SEO';
        $marker_end    = '# END DLE-Forum-SEO';

        if (!file_exists($htaccess_file)) return;

        $current = file_get_contents($htaccess_file);
        $pattern = '/' . preg_quote($marker_start, '/') . '.*?' . preg_quote($marker_end, '/') . '\s*/s';
        $current = preg_replace($pattern, '', $current);
        $current = preg_replace('/^RewriteRule\s+\^forum\b.*$/m', '', $current);
        $current = preg_replace('/\n{3,}/', "\n\n", trim($current));
        file_put_contents($htaccess_file, $current . "\n");
    }
}

forum_seo_remove();]]></phpdelete>
	<notice><![CDATA[]]></notice>
	<file name="engine/engine.php">
		<operation action="replace">
			<searchcode><![CDATA[switch ( $do ) {]]></searchcode>
			<replacecode><![CDATA[switch ( $do ) {

case "forum" :
    $dle_module = "forum";
    $allow_active_news = false;
    include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/init.php'));
    break;]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/forum/actions/pm.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
/*
=====================================================
 DLE Native Forum (Nexus) - by Dlehub
-----------------------------------------------------
 https://github.com/dlehub
-----------------------------------------------------
 Copyright (c) 2024-2026 Dlehub & Elegance
=====================================================
 This code is protected by copyright
=====================================================
 File: engine/forum/actions/pm.php
-----------------------------------------------------
 Use: DLE Native Conversations API'ye Yönlendirme
 ⚠️ KRITIK: Forum PM'leri artık DLE'nin kendi conversations 
    sistemini kullanıyor (v19+ compatibility)
=====================================================
*/
if(!defined('DATALIFEENGINE')) die("Hacking attempt!");

// ⚠️ Forum PM'leri DLE Native Conversations'a taşındı (v19+)
// Kullanıcı yönlendir DLE'nin kendi PM sistemine
if(!$is_logged) {
    header("Location: " . $config['http_home_url'] . "index.php?do=pm");
    die();
}

// DLE'nin kendi PM sistemine yönlendir
// Forum alt sekmesi gerekirse, forumdan yönlendi parametresi eklenebilir
$redirect_url = isset($_GET['sub']) ? 
    $config['http_home_url'] . "index.php?do=pm&action=" . preg_replace('/[^a-z]/', '', $_GET['sub']) :
    $config['http_home_url'] . "index.php?do=pm";

header("Location: " . $redirect_url);
die();

/*
 * LEGACY KOD (Yedek - DLE API ile Native Conversations Kullanılıyor)
 * ===================================================================
 * Eski forum_pm tablosu artık oluşturulmamaktadır.
 * Tüm mesajlaşma işlemleri DLE'nin:
 * - {prefix}_conversations
 * - {prefix}_conversation_users
 * - {prefix}_conversations_messages
 * 
 * tablolarını kullanıyor.
 * 
 * Mesaj Gönderme Örneği:
 * include_once (DLEPlugins::Check(ENGINE_DIR . '/api/api.class.php'));
 * global $dle_api;
 * $dle_api->send_pm_to_user($recipient_id, $subject, $text, $from_id);
 */

if(!$is_logged) {
    // DÜZELTME: msg yerine msgbox kullanıldı.
    msgbox(fl('forum_error'), fl('forum_err_login_pm'));
    return;
}

 $csrf        = $dle_login_hash;
 $uid         = intval($member_id['user_id']);
 $sub_action  = isset($_GET['sub']) ? preg_replace('/[^a-z_]/', '', $_GET['sub']) : 'inbox';
 $time        = time();

// --- POST İŞLEMLERİ ---

// 1. Mesaj Gönderme
if(isset($_POST['submit_pm']) && hash_equals((string)$csrf, (string)($_POST['user_hash'] ?? ''))) {
    
    $name    = $db->safesql(trim($_POST['name']));
    $subject = $db->safesql(trim($_POST['subject']));
    $text    = $db->safesql(trim($_POST['comments'])); // DLE editörü 'comments' name ile gönderir
    
    $to_user = $db->super_query("SELECT user_id FROM " . USERPREFIX . "_users WHERE name='{$name}'");
    $conv_id = isset($_POST['conv_id']) ? intval($_POST['conv_id']) : 0;
    
    if(!$to_user) {
        $pm_error = fl('forum_err_pm_user');
        $sub_action = 'compose'; 
    } else {
        if($conv_id > 0) {
            // Mevcut konuşmaya ekle
            $db->query("INSERT INTO " . USERPREFIX . "_conversations_messages (conversation_id, sender_id, content, created_at) VALUES ('{$conv_id}', '{$uid}', '{$text}', '{$time}')");
            $db->query("UPDATE " . USERPREFIX . "_conversations SET updated_at='{$time}' WHERE id='{$conv_id}'");
        } else {
            // Yeni konuşma oluştur
            $db->query("INSERT INTO " . USERPREFIX . "_conversations (subject, sender_id, recipient_id, created_at, updated_at) VALUES ('{$subject}', '{$uid}', '{$to_user['user_id']}', '{$time}', '{$time}')");
            $conv_id = $db->insert_id();
            $db->query("INSERT INTO " . USERPREFIX . "_conversation_users (conversation_id, user_id) VALUES ('{$conv_id}', '{$uid}'), ('{$conv_id}', '{$to_user['user_id']}')");
            $db->query("INSERT INTO " . USERPREFIX . "_conversations_messages (conversation_id, sender_id, content, created_at) VALUES ('{$conv_id}', '{$uid}', '{$text}', '{$time}')");
        }
        
        // Sayaç güncelle
        $db->query("UPDATE " . USERPREFIX . "_users SET pm_all=pm_all+1 WHERE user_id IN ('{$uid}', '{$to_user['user_id']}')");
        
        $pm_success = fl('forum_ok_pm_sent');
        $sub_action = 'inbox'; 
    }
}

// 2. Mesaj Silme
if(isset($_POST['delete_pm']) && hash_equals((string)$csrf, (string)($_POST['user_hash'] ?? ''))) {
    $pmid = intval($_POST['pm_id']);
    $db->query("DELETE FROM " . USERPREFIX . "_conversation_users WHERE conversation_id='{$pmid}' AND user_id='{$uid}'");
    
    $count = $db->super_query("SELECT COUNT(*) as c FROM " . USERPREFIX . "_conversation_users WHERE conversation_id='{$pmid}'");
    if(!$count['c']) {
        $db->query("DELETE FROM " . USERPREFIX . "_conversations WHERE id='{$pmid}'");
        $db->query("DELETE FROM " . USERPREFIX . "_conversations_messages WHERE conversation_id='{$pmid}'");
    }
    
    $db->query("UPDATE " . USERPREFIX . "_users SET pm_all=GREATEST(0, pm_all-1) WHERE user_id='{$uid}'");
    
    header("Location: " . forum_url('forum_pm')); exit;
}

// 3. Çoklu Mesaj Silme
if(isset($_POST['delete_selected_pms']) && hash_equals((string)$csrf, (string)($_POST['user_hash'] ?? ''))) {
    if(!empty($_POST['selected_pms']) && is_array($_POST['selected_pms'])) {
        foreach($_POST['selected_pms'] as $pmid) {
            $pmid = intval($pmid);
            $db->query("DELETE FROM " . USERPREFIX . "_conversation_users WHERE conversation_id='{$pmid}' AND user_id='{$uid}'");
            $count = $db->super_query("SELECT COUNT(*) as c FROM " . USERPREFIX . "_conversation_users WHERE conversation_id='{$pmid}'");
            if(!$count['c']) {
                $db->query("DELETE FROM " . USERPREFIX . "_conversations WHERE id='{$pmid}'");
                $db->query("DELETE FROM " . USERPREFIX . "_conversations_messages WHERE conversation_id='{$pmid}'");
            }
            $db->query("UPDATE " . USERPREFIX . "_users SET pm_all=GREATEST(0, pm_all-1) WHERE user_id='{$uid}'");
        }
    }
    header("Location: " . forum_url('forum_pm')); exit;
}

// --- TEMPLATE YÜKLEME ---
 $tpl->load_template('forum/pm.tpl');

// --- 1. YENİ MESAJ (COMPOSE) ---
if($sub_action == 'compose') {
    $tpl->set('[compose]', ''); $tpl->set('[/compose]', '');
    $tpl->set_block("'\\[inbox\\](.*?)\\[/inbox\\]'is", "");
    $tpl->set_block("'\\[read\\](.*?)\\[/read\\]'is", "");

    // DLE Standart Editör Yükleme (Buffer doğru alınıyor)
    $onload_scripts = array(); // Reset
    $wysiwyg = "";
    
    // DLE 14+ için editor/pm.php, yoksa comments.php
    if(file_exists(ENGINE_DIR . '/editor/pm.php')) {
        include_once(DLEPlugins::Check(ENGINE_DIR . '/editor/pm.php'));
    } else {
        include_once(DLEPlugins::Check(ENGINE_DIR . '/editor/comments.php'));
    }
    
    // Eğer onload_scripts varsa (TinyMCE init vs), bunları <script> içine sarıp $wysiwyg'e ekliyoruz
    if(count($onload_scripts)) {
        $wysiwyg .= "<script>\n";
        foreach($onload_scripts as $script) {
            $wysiwyg .= $script . "\n";
        }
        $wysiwyg .= "</script>";
    }
    
    $tpl->set('{editor}', $wysiwyg);
    $tpl->set('{recipient}', isset($_GET['to']) ? htmlspecialchars(urldecode($_GET['to']), ENT_QUOTES, 'UTF-8') : '');
    $tpl->set('{subject}', isset($_GET['subject']) ? htmlspecialchars(urldecode($_GET['subject']), ENT_QUOTES, 'UTF-8') : '');
    $tpl->set('{conv_id}', isset($_GET['conv_id']) ? intval($_GET['conv_id']) : 0);
    
    $alert = '';
    if(!empty($pm_error)) $alert = '<div class="df-alert df-alert-error">'.$pm_error.'</div>';
    $tpl->set('{alert}', $alert);
}

// --- 2. MESAJ OKUMA (READ) ---
elseif($sub_action == 'read' && isset($_GET['id'])) {
    $tpl->set('[read]', ''); $tpl->set('[/read]', '');
    $tpl->set_block("'\\[inbox\\](.*?)\\[/inbox\\]'is", "");
    $tpl->set_block("'\\[compose\\](.*?)\\[/compose\\]'is", "");

    $pmid = intval($_GET['id']);
    
    $row = $db->super_query(
        "SELECT c.id, c.subject, c.sender_id, c.recipient_id, sender.name as sender_name, recipient.name as recipient_name
         FROM " . USERPREFIX . "_conversations c
         JOIN " . USERPREFIX . "_conversation_users cu ON c.id = cu.conversation_id
         JOIN " . USERPREFIX . "_users sender ON c.sender_id = sender.user_id
         JOIN " . USERPREFIX . "_users recipient ON c.recipient_id = recipient.user_id
         WHERE cu.user_id='$uid' AND c.id='$pmid'"
    );

    if($row) {
        $db->query("INSERT INTO " . USERPREFIX . "_conversation_users (conversation_id, user_id, last_read_at) VALUES ('$pmid', '$uid', '$time') ON DUPLICATE KEY UPDATE last_read_at='$time'");
        
        $s_title = stripslashes($row['subject']);
        $tpl->set('{pm_title}', $s_title);
        
        $other_user = ($row['sender_id'] == $uid) ? $row['recipient_name'] : $row['sender_name'];
        $tpl->set('{pm_from}', $other_user);
        
        $reply_link = forum_fix_url(forum_url('forum_pm'), 'sub=compose&to=' . urlencode($other_user) . '&subject=Re: ' . urlencode($s_title) . '&conv_id=' . $pmid);
        $tpl->set('{reply_link}', $reply_link);
        $tpl->set('{pm_id}', $pmid);
        
        $msgs = $db->query(
            "SELECT m.content, m.created_at, m.sender_id, u.name, u.foto 
             FROM " . USERPREFIX . "_conversations_messages m 
             LEFT JOIN " . USERPREFIX . "_users u ON m.sender_id = u.user_id 
             WHERE m.conversation_id='$pmid' ORDER BY m.created_at ASC"
        );
        
        $messages_list = '';
        $parse = new ParseFilter();
        $parse->safe_mode = true;
        $parse->edit_mode = false;
        $parse->wysiwyg = false;

        while($m = $db->get_row($msgs)) {
            $m_text = $parse->decodeBBCodes(stripslashes($m['content']), true, false);
            $m_date = date("d.m.Y H:i", $m['created_at']);
            $m_usr  = htmlspecialchars($m['name']);
            $avatar = $m['foto'] ? rtrim($config['http_home_url'], '/') . '/' . $m['foto'] : rtrim($config['http_home_url'], '/') . '/templates/' . $config['skin'] . '/dleimages/noavatar.png';
            $usr_link = forum_url('forum_user', ['user' => urlencode($m['name'])]);
            
            $messages_list .= '
            <div class="df-post df-pm-message-item">
                <div class="df-post-user">
                    <div class="df-user-panel">
                        <img src="'.$avatar.'" class="df-avatar">
                        <a href="'.$usr_link.'" class="df-post-username">'.$m_usr.'</a>
                    </div>
                </div>
                <div class="df-post-main">
                    <div class="df-post-header">
                        <i class="fa fa-clock-o"></i> '.$m_date.'
                    </div>
                    <div class="df-post-content">
                        '.$m_text.'
                    </div>
                </div>
            </div>';
        }
        
        $tpl->set('{pm_text}', $messages_list);
        $tpl->set('{alert}', '');

    } else {
        $tpl->set('{pm_text}', '<div class="df-alert df-alert-error">' . fl('forum_pm_not_found') . '</div>');
    }
}

// --- 3. GELEN KUTUSU (INBOX) ---
else {
    $tpl->set('[inbox]', ''); $tpl->set('[/inbox]', '');
    $tpl->set_block("'\\[compose\\](.*?)\\[/compose\\]'is", "");
    $tpl->set_block("'\\[read\\](.*?)\\[/read\\]'is", "");

    $page = isset($_GET['page']) ? intval($_GET['page']) : 1;
    $limit = 20;
    $offset = ($page-1)*$limit;

    $sql = "
        SELECT c.id, c.subject, c.updated_at, 
               (SELECT m.content FROM " . USERPREFIX . "_conversations_messages m WHERE m.conversation_id = c.id ORDER BY m.created_at DESC LIMIT 1) as last_text,
               u.name as last_user_name, u.foto as last_user_foto,
               CASE WHEN cu.last_read_at IS NULL OR c.updated_at > cu.last_read_at THEN 0 ELSE 1 END as is_read
        FROM " . USERPREFIX . "_conversations c
        JOIN " . USERPREFIX . "_conversation_users cu ON c.id = cu.conversation_id AND cu.user_id = '$uid'
        LEFT JOIN " . USERPREFIX . "_users u ON (c.sender_id = u.user_id OR c.recipient_id = u.user_id) AND u.user_id != '$uid'
        WHERE cu.user_id = '$uid'
        GROUP BY c.id
        ORDER BY is_read ASC, c.updated_at DESC LIMIT $offset, $limit
    ";

    $pms = $db->query($sql);
    $pm_rows = '';
    
    if($db->num_rows($pms) == 0) {
        $pm_rows = '<div class="df-empty">' . fl('forum_pm_empty') . '</div>';
    } else {
        while($row = $db->get_row($pms)) {
            $unread_cls = $row['is_read'] ? '' : 'unread';
            $pm_avatar  = $row['last_user_foto'] ? rtrim($config['http_home_url'], '/') . '/' . $row['last_user_foto'] : rtrim($config['http_home_url'], '/') . '/templates/' . $config['skin'] . '/dleimages/noavatar.png';
            $pm_from    = htmlspecialchars($row['last_user_name']);
            $pm_title   = stripslashes($row['subject']);
            $pm_date    = date("d.m.Y H:i", $row['updated_at']);
            $pm_preview = htmlspecialchars(mb_strimwidth(strip_tags($row['last_text']), 0, 80, "..."));
            
            $pm_rows .= '
            <div class="df-pm-row '.$unread_cls.'">
                <div class="df-pm-select">
                    <input type="checkbox" name="selected_pms[]" value="'.intval($row['id']).'" class="df-checkbox-custom">
                </div>
                <div class="df-pm-avatar-wrap">
                    <img src="'.$pm_avatar.'" class="df-pm-avatar">
                </div>
                <div class="df-pm-content">
                    <a href="' . forum_url('forum_pm_read', ['newsid' => intval($row['id'])]) . '" class="df-pm-title">'.$pm_title.'</a>
                    <div class="df-pm-snippet">'.$pm_preview.'</div>
                </div>
                <div class="df-pm-meta">
                    <strong>'.$pm_from.'</strong><br>
                    <span>'.$pm_date.'</span>
                </div>
            </div>';
        }
    }
    
    $tpl->set('{pm_list}', $pm_rows);
    $tpl->set('{pm_pagination}', ''); 
    $tpl->set('{breadcrumb}', $Forum->getBreadcrumb(0, 0, fl('forum_pm')));
    $tpl->set('{alert}', '');
}

// Genel Taglar
 $tpl->set('{user_hash}', $csrf);
 $tpl->set('{SITEURL}', $config['http_home_url']);

 $tpl->compile('content');
 $tpl->clear();
?>
]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/forum/actions/notifications.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
/*
=====================================================
 DLE Native Forum (Nexus) - by Dlehub
-----------------------------------------------------
 https://github.com/dlehub
-----------------------------------------------------
 Copyright (c) 2024-2026 Dlehub & Elegance
=====================================================
 This code is protected by copyright
=====================================================
 File: engine/forum/actions/notifications.php
-----------------------------------------------------
 Use: Kullanıcı bildirimlerini (cevap, beğeni, pm, etiket) listeleme ve yönetme
=====================================================
*/
if(!defined('DATALIFEENGINE')) die("Hacking attempt!");

if(!$is_logged) {
    msgbox(fl('forum_error'), fl('forum_err_login_notif'));
    return;
}

// Bildirimleri okundu yap ve çek
$Forum->markNotifsRead();
$notifs = $Forum->getNotifs($Forum->getUserId(), 50);

// Tüm bildirim tiplerini tanımla (Mavi tonu senin tarzına uygun hale getirildi)
$notif_type_labels = [
    'reply'   => ['icon' => 'fa-reply',       'color' => '#475569', 'text' => fl('forum_notif_reply')],
    'like'    => ['icon' => 'fa-thumbs-up',   'color' => '#f59e0b', 'text' => fl('forum_notif_like')],
    'pm'      => ['icon' => 'fa-envelope',    'color' => '#8b5cf6', 'text' => fl('forum_notif_pm')],
    'mention' => ['icon' => 'fa-at',          'color' => '#10b981', 'text' => fl('forum_notif_mention')],
];

$notif_html = '';
if(empty($notifs)) {
    $notif_html = '<div class="df-empty">' . fl('forum_no_notifs') . '</div>';
} else {
    foreach($notifs as $n) {
        $type      = $n['type'] ?: 'reply';
        $lbl       = $notif_type_labels[$type] ?? $notif_type_labels['reply'];
        
        $from      = htmlspecialchars($n['from_name'] ?: 'Biri');
        $topic     = htmlspecialchars($n['topic_title'] ?: fl('forum_deleted_topic'));
        $n_date    = date('d.m.Y H:i', strtotime($n['date']));
        
        // Link Mantığı: Konu varsa linkle, yoksa pasif yap
        $link_start = '';
        $link_end   = '';
        $arrow      = '';
        
        if($n['topic_title']) {
            $topic_url = forum_url('forum_topic', ['newsid' => intval($n['topic_id'])]);
            if($n['post_id']) $topic_url .= '#post-' . intval($n['post_id']);
            $link_start = '<a href="' . $topic_url . '" class="df-notif-row">';
            $link_end   = '</a>';
            $arrow      = '<i class="fa fa-chevron-right df-notif-arrow"></i>';
        } else {
            // Konu silinmişse tıklanamaz yap ve görsel geri bildirim ver
            $link_start = '<div class="df-notif-row df-notif-deleted" style="opacity:0.6; cursor:default;">';
            $link_end   = '</div>';
        }

        $notif_html .= '
        ' . $link_start . '
            <div class="df-notif-icon" style="background:' . $lbl['color'] . '20">
                <i class="fa ' . $lbl['icon'] . ' text-sm" style="color:' . $lbl['color'] . '"></i>
            </div>
            <div class="df-flex-fill">
                <div class="df-notif-text">
                    <span class="df-notif-from">' . $from . '</span>
                    <span class="df-notif-action"> ' . $lbl['text'] . ': </span>
                    <span class="df-notif-topic">' . $topic . '</span>
                </div>
                <div class="df-notif-date">' . $n_date . '</div>
            </div>
            ' . $arrow . '
        ' . $link_end;
    }
}

// HTML kart sarmalayıcıları kaldırıldı, sadece içerik şablona gönderiliyor
$tpl->load_template('forum/notifications.tpl');
$tpl->set('{notif_content}', $notif_html);
$tpl->set('{SITEURL}',       $config['http_home_url']);
$tpl->set('{forum_url}',    forum_url('forum_main'));
$tpl->set('{breadcrumb}',   $Forum->getBreadcrumb(0, 0, fl('forum_notifications')));

$tpl->compile('content'); $tpl->clear();

define('KSEARCH',  false);
define('RSSPARSE', false);
$metatags['title'] = fl('forum_notifications') . ' - ' . fl('forum_forum');
?>
]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/forum/actions/main.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
/*
=====================================================
 DLE Native Forum (Nexus) - by Dlehub
-----------------------------------------------------
 https://github.com/dlehub
-----------------------------------------------------
 Copyright (c) 2024-2026 Dlehub & Elegance
=====================================================
 File: engine/forum/actions/main.php
-----------------------------------------------------
 Use: Forum ana sayfası, XenForo tarzı hiyerarşik kategori listeleme
=====================================================
*/

if(!defined('DATALIFEENGINE')) die("Hacking attempt!");

// 1. Sadece En Üst Ana Kategorileri Çek (parent_id = 0)
$parent_cats = $Forum->getCats(0) ?: [];
$stats       = $Forum->getStats();
$online_count= $Forum->getOnlineCount() ?: 0; 
$noavatar    = rtrim($config['http_home_url'], '/') . '/templates/' . $config['skin'] . '/dleimages/noavatar.png';

// Ziyaretçiyi logla (15 dk interval ile)
$Forum->logVisitor();

// Bugünkü ziyaretçi sayısını al (cache'li, 1 saat)
$today_visitors = $Forum->getTodayVisitors();

// Çevrimiçi istatistikleri al (cache'li)
$online_stats = $Forum->getOnlineStats();

// Template etiketlerine gönder
$tpl->set('{online_total}',   $online_stats['total']);
$tpl->set('{online_members}', $online_stats['members']);
$tpl->set('{online_guests}',  $online_stats['guests']);
$tpl->set('{today_visitors}', $today_visitors['total_visitors']);

$cats_html = '';

if(empty($parent_cats)) {
    $cats_html = '<div class="df-empty">' . fl('forum_err_no_cats') . '</div>';
} else {
    // 2. Ana Kategorileri Döngüye Al (Mavi Başlıklar)
    foreach($parent_cats as $pcat) {
        
        $cats_html .= '
        <div class="df-category-block">
            <div class="df-cat-head">
                <span class="df-cat-title"><i class="fa ' . htmlspecialchars($pcat['icon'] ?: 'fa-folder') . '"></i> ' . htmlspecialchars($pcat['name']) . '</span>
            </div>';

        // 3. Bu Ana Kategorinin Altındaki Asıl Forumları Çek
        $forums = $Forum->getCats($pcat['id']) ?: [];

        if(!empty($forums)) {
            $cats_html .= '
            <div class="df-forum-list">
                <div class="df-list-header">
                    <div class="df-header-icon"></div>
                    <div class="df-header-title">' . fl('forum_forum') . '</div>
                    <div class="df-header-stats">' . fl('forum_stats') . '</div>
                    <div class="df-header-last">' . fl('forum_last_posts') . '</div>
                </div>';

            // 4. Asıl Forumları Satır Satır Döngüye Al
            foreach($forums as $forum) {
                
                // --- ALT-ALT FORUMLARI ÇEK (Açıklamanın altındaki minik linkler) ---
                $sub_forums = $Forum->getCats($forum['id']) ?: [];
                $sub_cats_html = '';
                
                if(!empty($sub_forums)) {
                    $sub_links = [];
                    foreach($sub_forums as $sscat) {
                        $ss_url = forum_url('forum_cat', ['category' => $sscat['alt_name']]);
                        $sub_links[] = '<a href="' . $ss_url . '" class="df-subcat-link"><i class="fa ' . htmlspecialchars($sscat['icon'] ?: 'fa-file-text-o') . '"></i> ' . htmlspecialchars($sscat['name']) . '</a>';
                    }
                    $sub_cats_html = '<div class="df-subcats-wrap">' . implode(', ', $sub_links) . '</div>';
                }

                // --- SON MESAJ VE AVATAR İŞLEMLERİ ---
                $lt = null;
                if(intval($forum['last_topic_id']) > 0) {
                    $lt = $Forum->getTopicById($forum['last_topic_id']);
                }

                $last_post_html = '<span class="df-no-post">' . fl('forum_no_posts') . '</span>';
                
                if($lt) {
                    $per_page    = intval($Forum->getSetting('posts_per_page') ?: 20);
                    $total_posts = intval($lt['replies']) + 1;
                    $target_page = ceil($total_posts / $per_page);

                    $lt_url_params = [
                        'newsid'    => intval($lt['id']),
                        'news_name' => $lt['alt_name'],
                        'category'  => $lt['cat_slug']
                    ];

                    if ($target_page > 1) {
                        $lt_url_params['cstart'] = $target_page;
                    }

                    $lt_url = forum_url('forum_topic', $lt_url_params);
                    if (intval($forum['last_post_id']) > 0) {
                        $lt_url .= '#post-' . intval($forum['last_post_id']);
                    }

                    $lt_user  = htmlspecialchars($lt['last_username'] ?: $lt['username'] ?: fl('forum_guest', 'Misafir'));
                    $lt_date_raw = $lt['last_post_date'] ?: $lt['date'];
                    $lt_date  = (strtotime($lt_date_raw) > 0) ? date('d.m.Y H:i', strtotime($lt_date_raw)) : '-';
                    $lt_title = htmlspecialchars($lt['title']);
                    
                    // XSS Açığı Kapatıldı
                    $lt_avatar = !empty($lt['last_user_avatar']) ? rtrim($config['http_home_url'], '/') . '/' . htmlspecialchars($lt['last_user_avatar']) : $noavatar;
                    
                    $last_post_html = '
                        <img src="' . $lt_avatar . '" class="df-last-post-avatar" alt="' . $lt_user . '">
                        <div class="df-last-post-wrap">
                            <a href="' . $lt_url . '" class="df-last-title" title="' . $lt_title . '">' . $lt_title . '</a>
                            <div class="df-last-meta">
                                <span class="df-last-user">' . $lt_user . '</span>
                                <span class="df-sep">•</span>
                                <span>' . $lt_date . '</span>
                            </div>
                        </div>';
                }

                $cat_url = forum_url('forum_cat', ['category' => $forum['alt_name']]);
                $icon_class = htmlspecialchars($forum['icon'] ?: 'fa-comments');
                
                // HTML Satır Çıktısı
                $cats_html .= '
                    <div class="df-forum-row">
                        <div class="df-col-icon">
                            <div class="df-icon-box"><i class="fa ' . $icon_class . '"></i></div>
                        </div>
                        <div class="df-col-main">
                            <a href="' . $cat_url . '" class="df-title-link">' . htmlspecialchars($forum['name']) . '</a>
                            <div class="df-desc-text">' . htmlspecialchars($forum['description']) . '</div>
                            ' . $sub_cats_html . ' 
                        </div>
                        <div class="df-col-stats">
                            <div class="df-stat-num">' . intval($forum['topic_count']) . ' <small>' . fl('forum_topic') . '</small></div>
                            <div class="df-stat-num">' . intval($forum['post_count']) . ' <small>' . fl('forum_post') . '</small></div>
                        </div>
                        <div class="df-col-last">
                            ' . $last_post_html . '
                        </div>
                    </div>';
            }

            $cats_html .= '</div>'; // .df-forum-list sonu
        } else {
            // Eğer ana kategorinin içi boşsa
            $cats_html .= '<div class="df-empty" style="padding:15px; border-top:1px solid var(--df-border); font-size:13px; color:var(--df-text-muted);">' . fl('forum_err_no_cats') . '</div>';
        }

        $cats_html .= '</div>'; // .df-category-block sonu
    }
}

$tpl->load_template('forum/main.tpl');
$tpl->set('{forum_cats}', $cats_html);
$tpl->set('{total_topics}', number_format($stats['total_topics']));
$tpl->set('{forum_content}', '');
$tpl->set('{total_posts}', number_format($stats['total_posts']));
$tpl->set('{total_members}', number_format($stats['total_users']));
$tpl->set('{online_count}', $online_count);
$tpl->set('{breadcrumb}', $Forum->getBreadcrumb());
$tpl->set('{forum_home}', fl('forum_home'));

$last_member_name = htmlspecialchars($stats['last_user']['name'] ?? fl('forum_guest'));
$lu_id     = intval($stats['last_user']['user_id'] ?? 0);
$lu_foto   = !empty($stats['last_user']['foto'])
    ? rtrim($config['http_home_url'], '/') . '/uploads/fotos/' . htmlspecialchars($stats['last_user']['foto'])
    : $noavatar;
$lu_url    = $config['http_home_url'] . 'user/' . urlencode($stats['last_user']['name'] ?? '');

$last_member_html = '<a href="' . $lu_url . '" class="df-stat-last-member" title="' . fl('forum_view_profile') . '">'
    . '<img src="' . $lu_foto . '" class="df-mini-avatar" alt="' . $last_member_name . '">'
    . '<strong>' . $last_member_name . '</strong>'
    . '</a>';
$tpl->set('{last_member}', $last_member_html);

// Son Mesaj bloku
$lp = $stats['last_post'] ?? [];
if (!empty($lp['topic_id'])) {
    $lp_url = forum_url('forum_topic', [
        'newsid'    => intval($lp['topic_id']),
        'news_name' => $lp['topic_alt'] ?? '',
        'category'  => $lp['cat_slug'] ?? ''
    ]) . '#post-' . intval($lp['id']);
    $lp_topic = htmlspecialchars(mb_strimwidth($lp['topic_title'] ?? '', 0, 40, '...'));
    $lp_user  = htmlspecialchars($lp['username'] ?? fl('forum_guest'));
    $lp_date  = date('d.m.Y H:i', strtotime($lp['date'] ?? 'now'));
    $lp_avatar = !empty($lp['user_avatar'])
        ? rtrim($config['http_home_url'], '/') . '/uploads/fotos/' . htmlspecialchars($lp['user_avatar'])
        : $noavatar;
    $tpl->set('{last_post_topic}', $lp_topic);
    $tpl->set('{last_post_user}',  $lp_user);
    $tpl->set('{last_post_date}',  $lp_date);
    $tpl->set('{last_post_link}',  $lp_url);
    $tpl->set('{last_post_avatar}', $lp_avatar);
    $tpl->set('[last-post]',  '');
    $tpl->set('[/last-post]', '');
} else {
    $tpl->set_block("'\\[last-post\\](.*?)\\[/last-post\\]'is", '');
}

// Bot sayisi: DLE'de botlar _users tablosuna yazılmaz, her zaman 0
$tpl->set('{online_bots}', 0);

$tpl->set('{new_topic_btn}', $member_id['user_id'] ? '<a href="' . forum_url('forum_new_topic') . '" class="df-btn-new-topic"><i class="fa fa-plus"></i> ' . fl('forum_new_topic') . '</a>' : '');
$tpl->set('{SITEURL}', $config['http_home_url']);

$tpl->compile('content'); 
$tpl->clear();
$metatags['title'] = fl('forum_title');
?>]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/forum/actions/category.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
/*
=====================================================
 DLE Native Forum (Nexus) - by Dlehub
-----------------------------------------------------
 https://github.com/dlehub
-----------------------------------------------------
 Copyright (c) 2024-2026 Dlehub & Elegance
=====================================================
 File: engine/forum/actions/category.php
-----------------------------------------------------
 Use: Belirli bir kategori içindeki konuları listeleme (Topic List)
=====================================================
*/

if(!defined('DATALIFEENGINE')) die("Hacking attempt!");

// DÜZELTME: Slug'lar safesql ile kesinlikle işlenmez, sadece güvenli karakterlere izin verilir.
// Bu sayede hem SQL injection kapalı hem de URL'ler bozulmaz (Çift escape yok).
// Tümünü okundu işaretle
if(isset($_POST['mark_cat_read']) && $is_logged) {
    $cat_id_mark = intval($_POST['cat_id']);
    if($cat_id_mark > 0) {
        $Forum->markCatAsRead($cat_id_mark);
    }
    header("Location: " . forum_url('forum_cat', ['category' => preg_replace('/[^a-zA-Z0-9_\-]/', '', $_GET['cat'] ?? '')]));
    exit;
}

 $cat_slug = isset($_GET['cat']) ? preg_replace('/[^a-zA-Z0-9_\-]/', '', $_GET['cat']) : '';

if(empty($cat_slug)) {
    msgbox(fl('forum_error'), fl('forum_err_cat_invalid'));
    return;
}

 $cat = $Forum->getCatBySlug($cat_slug);

if(!$cat) {
    msgbox(fl('forum_error'), fl('forum_err_cat_not_found'));
    return;
}

 $cat_id   = intval($cat['id']);
 $page     = isset($_GET['page']) ? max(1, intval($_GET['page'])) : 1;
 $per_page = intval($Forum->getSetting('topics_per_page') ?: 25);

 $noavatar = rtrim($config['http_home_url'], '/') . '/templates/' . $config['skin'] . '/dleimages/noavatar.png';

// ----------------------------------------------------------------
// 1. ALT FORUMLARI ÇEK VE LİSTELE (Klasik Forum Hiyerarşisi)
// ----------------------------------------------------------------
$sub_forums = $Forum->getCats($cat_id) ?: [];
$sub_forums_html = '';

if(!empty($sub_forums)) {
    $sub_forums_html .= '<div class="df-category-block" style="margin-bottom:20px;">
        <div class="df-forum-list">
            <div class="df-list-header">
                <div class="df-header-icon"></div>
                <div class="df-header-title">' . fl('forum_forum') . '</div>
                <div class="df-header-stats">' . fl('forum_stats') . '</div>
                <div class="df-header-last">' . fl('forum_last_posts') . '</div>
            </div>';

    foreach($sub_forums as $forum) {
        
        // Bu alt forumun da alt forumu (Level 3) varsa virgüllü link yap
        $sub_sub_cats = $Forum->getCats($forum['id']) ?: [];
        $sub_cats_links_html = '';
        if(!empty($sub_sub_cats)) {
            $sub_links = [];
            foreach($sub_sub_cats as $sscat) {
                $ss_url = forum_url('forum_cat', ['category' => $sscat['alt_name']]);
                $sub_links[] = '<a href="' . $ss_url . '" class="df-subcat-link"><i class="fa ' . htmlspecialchars($sscat['icon'] ?: 'fa-file-text-o') . '"></i> ' . htmlspecialchars($sscat['name']) . '</a>';
            }
            $sub_cats_links_html = '<div class="df-subcats-wrap">' . implode(', ', $sub_links) . '</div>';
        }

        // Son Mesaj Bilgileri
        $lt = null;
        if(intval($forum['last_topic_id']) > 0) {
            $lt = $Forum->getTopicById($forum['last_topic_id']);
        }

        $last_post_html = '<span class="df-no-post">' . fl('forum_no_posts') . '</span>';
        if($lt) {
            $per_page    = intval($Forum->getSetting('posts_per_page') ?: 20);
            $total_posts = intval($lt['replies']) + 1;
            $target_page = ceil($total_posts / $per_page);

            $lt_url_params = [
                'newsid'    => intval($lt['id']),
                'news_name' => $lt['alt_name'],
                'category'  => $lt['cat_slug']
            ];

            if ($target_page > 1) {
                $lt_url_params['cstart'] = $target_page;
            }

            $lt_url = forum_url('forum_topic', $lt_url_params);
            if (intval($forum['last_post_id']) > 0) {
                $lt_url .= '#post-' . intval($forum['last_post_id']);
            }
            $lt_user  = htmlspecialchars($lt['last_username'] ?: $lt['username'] ?: fl('forum_guest'));
            $lt_date_raw = $lt['last_post_date'] ?: $lt['date'];
            $lt_date  = (strtotime($lt_date_raw) > 0) ? date('d.m.Y H:i', strtotime($lt_date_raw)) : '-';
            $lt_title = htmlspecialchars($lt['title']);
            $lt_avatar = !empty($lt['last_user_avatar']) ? rtrim($config['http_home_url'], '/') . '/' . htmlspecialchars($lt['last_user_avatar']) : $noavatar;
            
            $last_post_html = '
                <img src="' . $lt_avatar . '" class="df-last-post-avatar" alt="' . $lt_user . '">
                <div class="df-last-post-wrap">
                    <a href="' . $lt_url . '" class="df-last-title" title="' . $lt_title . '">' . $lt_title . '</a>
                    <div class="df-last-meta">
                        <span class="df-last-user">' . $lt_user . '</span>
                        <span class="df-sep">•</span>
                        <span>' . $lt_date . '</span>
                    </div>
                </div>';
        }

        $forum_url = forum_url('forum_cat', ['category' => $forum['alt_name']]);
        $icon_class = htmlspecialchars($forum['icon'] ?: 'fa-comments');
        
        $sub_forums_html .= '
            <div class="df-forum-row">
                <div class="df-col-icon"><div class="df-icon-box"><i class="fa ' . $icon_class . '"></i></div></div>
                <div class="df-col-main">
                    <a href="' . $forum_url . '" class="df-title-link">' . htmlspecialchars($forum['name']) . '</a>
                    <div class="df-desc-text">' . htmlspecialchars($forum['description']) . '</div>
                    ' . $sub_cats_links_html . ' 
                </div>
                <div class="df-col-stats">
                    <div class="df-stat-num">' . intval($forum['topic_count']) . ' <small>' . fl('forum_topic') . '</small></div>
                    <div class="df-stat-num">' . intval($forum['post_count']) . ' <small>' . fl('forum_post') . '</small></div>
                </div>
                <div class="df-col-last">' . $last_post_html . '</div>
            </div>';
    }
    $sub_forums_html .= '</div></div>';
}

// ----------------------------------------------------------------
// 2. KONULARI ÇEK VE LİSTELE
// ----------------------------------------------------------------
 $topics       = $Forum->getTopics($cat_id, $page, $per_page);
 $total_topics = $Forum->countTopics($cat_id);

 $topics_list_html = '';

if(empty($topics)) {
    // Eğer alt forum da yoksa "hiç konu yok" yazsın. 
    // Alt forum varsa zaten üstte listelendiği için aşağıya çirkin bir "Konu yok" yazısı basmasın.
    if(empty($sub_forums)) {
        $topics_list_html = '<div class="df-empty">' . fl('forum_err_no_topics') . '</div>';
    }
} else {
    foreach($topics as $t) {
        // DÜZELTME: ID'ler intval ile sarıldı
        $t_url     = forum_url('forum_topic', ['newsid' => intval($t['id']), 'news_name' => $t['alt_name'], 'category' => $cat_slug]);
        // Prefix rozeti
        $t_prefix = '';
        if(!empty($t['prefix_id'])) {
            $pf = $Forum->getPrefixById(intval($t['prefix_id']));
            if($pf) $t_prefix = $Forum->formatPrefix($pf);
        }
        $t_title   = htmlspecialchars($t['title']);
        $t_desc    = !empty($t['description']) ? '<div class="df-topic-desc" style="font-size:12px; color:#64748b; margin-top:2px;">' . htmlspecialchars(mb_strimwidth($t['description'], 0, 150, '...')) . '</div>' : '';
        // Etiketler
        $t_tags_html = '';
        $t_tags = $Forum->getTopicTags(intval($t['id']));
        if($t_tags) {
            $t_tags_html = '<div class="df-topic-tags" style="margin-top:3px;">';
            foreach($t_tags as $tg) {
                $tg_slug = htmlspecialchars($tg['slug']);
                $tg_name = htmlspecialchars($tg['name']);
                $t_tags_html .= '<a href="' . forum_url('forum_main') . '?action=tag&slug=' . $tg_slug . '" class="df-tag" style="display:inline-block;font-size:10px;padding:1px 6px;border-radius:10px;background:#e2e8f0;color:#475569;margin-right:3px;text-decoration:none;">#' . $tg_name . '</a>';
            }
            $t_tags_html .= '</div>';
        }
        $t_desc = $t_desc . $t_tags_html;
        $t_user    = htmlspecialchars($t['username'] ?: fl('forum_guest'));
        
        // Tarih Güvenliği
        $t_date    = (strtotime($t['date']) > 0) ? date('d.m.Y', strtotime($t['date'])) : '-';
        $t_views   = number_format(intval($t['views'] ?? 0));
        $t_replies = number_format(intval($t['replies'] ?? 0));

        $last_user = htmlspecialchars($t['last_username'] ?: $t_user);
        
        // Son Mesaj Tarihi Güvenliği
        $last_date_raw = $t['last_post_date'] ?: $t['date'];
        $last_date = (strtotime($last_date_raw) > 0) ? date('d.m.Y H:i', strtotime($last_date_raw)) : '-';

        // DÜZELTME: XSS açığını kapatmak için htmlspecialchars eklendi
        $last_avatar = !empty($t['last_user_avatar']) 
            ? rtrim($config['http_home_url'], '/') . '/' . htmlspecialchars($t['last_user_avatar']) 
            : $noavatar;

        // Durum İkonu
        $status_icon = '<i class="fa fa-comments df-icon-muted"></i>'; 
        if(!empty($t['is_pinned']) && !empty($t['is_locked'])) { 
            $status_icon = '<i class="fa fa-thumb-tack" style="color:var(--df-accent); margin-right:4px;"></i><i class="fa fa-lock" style="color:var(--df-danger)"></i>'; 
        } elseif(!empty($t['is_pinned'])) { 
            $status_icon = '<i class="fa fa-thumb-tack" style="color:var(--df-accent)"></i>'; 
        } elseif(!empty($t['is_locked'])) { 
            $status_icon = '<i class="fa fa-lock" style="color:var(--df-danger)"></i>'; 
        }

        // Okunmadı göstergesi - üye ve ziyaretçi
        $unread_dot = '';
        $is_unread  = false;

        if($is_logged) {
            // Üye: readlog tablosuna bak
            $is_unread = $Forum->isUnread(intval($t['id']), intval($t['last_post_id'] ?? 0));
        } else {
            // Ziyaretçi: session'a bak - son ziyaret saatinden sonra mesaj geldi mi?
            $session_start = isset($_SESSION['forum_visit_start']) ? intval($_SESSION['forum_visit_start']) : 0;
            if($session_start > 0 && !empty($t['last_post_date'])) {
                $last_post_ts = strtotime($t['last_post_date']);
                $is_unread = $last_post_ts > $session_start;
            }
        }

        if($is_unread) {
            $unread_dot = '<span class="df-unread-dot" title="Yeni mesaj var"></span>';
        }

        $topics_list_html .= '
        <div class="df-topic-row' . ($t['is_pinned'] ? ' df-topic-pinned' : '') . '">
            <div class="df-col-icon">' . $status_icon . '</div>
            <div class="df-col-main">
                <div class="df-title-row">' . $unread_dot . '
                    <a href="' . $t_url . '" class="df-title-link">' . $t_prefix . ' ' . $t_title . '</a>
                </div>
                ' . $t_desc . ' <div class="df-topic-meta">' . $t_user . ' • ' . $t_date . '</div>
            </div>
            <div class="df-col-stats">
                <div class="df-stat-num">' . $t_replies . ' <small>' . fl('forum_reply') . '</small></div>
                <div class="df-stat-num">' . $t_views . ' <small>' . fl('forum_views') . '</small></div>
            </div>
            <div class="df-col-last">
                <img src="' . $last_avatar . '" class="df-last-post-avatar" alt="' . $last_user . '">
                <div class="df-last-post-wrap">
                    <a href="' . $t_url . '#post-' . intval($t['last_post_id'] ?? 0) . '" class="df-last-title">' . $last_user . '</a>
                    <div class="df-last-meta">' . $last_date . '</div>
                </div>
            </div>
        </div>';
    }
}

 $base_url  = forum_url('forum_cat', ['category' => $cat_slug]);
 $pagination = $Forum->pagination($total_topics, $per_page, $page, $base_url);

 $new_topic_btn = $is_logged
    ? '<a href="' . forum_fix_url(forum_url('forum_new_topic'), 'cat_id=' . $cat_id) . '" class="df-btn-new-topic-sm"><i class="fa fa-plus"></i> ' . fl('forum_new_topic_btn') . '</a>'
    : '';

 $tpl->load_template('forum/category.tpl');
 $tpl->set('{breadcrumb}', $Forum->getBreadcrumb(0, 0, fl('forum_unanswered')));
 $tpl->set('{cat_name}',     htmlspecialchars($cat['name']));
 $tpl->set('{cat_desc}',     htmlspecialchars($cat['description']));
 
 // YENİ EKLENEN ŞABLON ETİKETİ
 $tpl->set('{sub_forums}',   $sub_forums_html);
 
 // "Tümünü Okundu Say" butonu - sadece giriş yapmış kullanıcılara
 $mark_read_btn = '';
 if($is_logged) {
     $mark_read_btn = '<form method="post" style="display:inline;">
         <input type="hidden" name="mark_cat_read" value="1">
         <input type="hidden" name="cat_id" value="' . $cat_id . '">
         <button type="submit" class="df-btn-mark-read" title="' . fl('forum_mark_all_read') . '">
             <i class="fa fa-check-circle-o"></i> ' . fl('forum_mark_all_read') . '
         </button>
     </form>';
 }
 $tpl->set('{mark_read_btn}', $mark_read_btn);
 $tpl->set('{topics_list}',  $topics_list_html);
 $tpl->set('{pagination}',   $pagination);
 $tpl->set('{new_topic_btn}', $new_topic_btn);
  $breadcrumb_cache = dle_cache('forum_breadcrumb_' . $cat_id, $config['skin']);
  if ($breadcrumb_cache) {
      $breadcrumb = $breadcrumb_cache;
  } else {
      $breadcrumb = $Forum->getBreadcrumb($cat_id);
      create_cache('forum_breadcrumb_' . $cat_id, $breadcrumb, $config['skin']);
  }
  $tpl->set('{breadcrumb}', $breadcrumb);
 $tpl->set('{SITEURL}',      $config['http_home_url']);

 $tpl->compile('content'); 
 $tpl->clear();
 $metatags['title'] = htmlspecialchars($cat['name']) . ' - ' . fl('forum_forum');
?>
]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/forum/actions/search.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
/*
=====================================================
 DLE Native Forum (Nexus) - by Dlehub
-----------------------------------------------------
 File: engine/forum/actions/search.php
-----------------------------------------------------
 Use: Forum içi arama sonuçlarını listeleme
=====================================================
*/

if(!defined('DATALIFEENGINE')) die("Hacking attempt!");

 $q = isset($_GET['q']) ? trim($_GET['q']) : '';
 $page = isset($_GET['page']) ? max(1, intval($_GET['page'])) : 1;
 $per_page = 25;

if(empty($q)) {
    msgbox(fl('forum_warning'), fl('forum_err_search_empty'));
    return;
}

// Arama Flood Koruması — Forum sınıfının checkFloodPublic metodunu kullan
if($is_logged) {
    if (is_object($Forum)) {
        $flood_result = $Forum->checkFloodPublic('search', 5); 
        if($flood_result !== true) {
            msgbox(fl('forum_warning'), $flood_result['error']);
            return;
        }
    }
}

 $topics = $Forum->searchTopics($q, $page, $per_page);
 $total_topics = $Forum->countSearchTopics($q);

 $topics_list_html = '';
 $noavatar = rtrim($config['http_home_url'], '/') . '/templates/' . $config['skin'] . '/dleimages/noavatar.png';

if(empty($topics)) {
    $topics_list_html = '<div class="df-empty"><div class="df-empty-icon"><i class="fa fa-search"></i></div>' . fl('forum_search_no_results') . '</div>';
} else {
    foreach($topics as $t) {
        $t_id      = intval($t['id']);
        $t_url     = forum_url('forum_topic', ['newsid' => $t_id, 'news_name' => $t['alt_name'], 'category' => $t['cat_slug']]);
        $t_title   = htmlspecialchars($t['title'], ENT_QUOTES, 'UTF-8');
        $t_user    = htmlspecialchars($t['username'] ?: fl('forum_member'), ENT_QUOTES, 'UTF-8'); 
        
        $t_date = (isset($t['date']) && strtotime($t['date']) > 0) ? date('d.m.Y', strtotime($t['date'])) : '-';
        $t_views = number_format(intval($t['views'] ?? 0));
        $t_replies = number_format(intval($t['replies'] ?? 0));
        
        $last_user = htmlspecialchars($t['last_username'] ?? $t_user, ENT_QUOTES, 'UTF-8');
        $last_date_raw = $t['last_post_date'] ?? ($t['lp_date'] ?? $t['date']);
        $last_date = ($last_date_raw && strtotime($last_date_raw) > 0) ? date('d.m.Y H:i', strtotime($last_date_raw)) : '-';

        $last_avatar = !empty($t['last_user_avatar']) 
            ? rtrim($config['http_home_url'], '/') . '/' . htmlspecialchars($t['last_user_avatar'], ENT_QUOTES, 'UTF-8') 
            : $noavatar;

        $status_icon = '<i class="fa fa-comment-dots df-icon-muted"></i>';
        if(!empty($t['is_pinned']) && !empty($t['is_locked'])) {
            $status_icon = '<i class="fa fa-thumb-tack" style="color:var(--df-accent); margin-right:4px;"></i><i class="fa fa-lock" style="color:var(--df-danger)"></i>';
        } elseif(!empty($t['is_pinned'])) {
            $status_icon = '<i class="fa fa-thumb-tack" style="color:var(--df-accent)"></i>';
        } elseif(!empty($t['is_locked'])) {
            $status_icon = '<i class="fa fa-lock" style="color:var(--df-danger)"></i>';
        }

        $topics_list_html .= '
        <div class="df-topic-row">
            <div class="df-col-icon">' . $status_icon . '</div>
            <div class="df-col-main">
                <div class="df-title-row">
                    <a href="' . $t_url . '" class="df-title-link">' . $t_title . '</a>
                </div>
                <div class="df-topic-meta">' . $t_user . ' • ' . $t_date . '</div>
            </div>
            <div class="df-col-stats">
                <div class="df-stat-num">' . $t_replies . ' <small>' . fl('forum_reply') . '</small></div>
                <div class="df-stat-num">' . $t_views . ' <small>' . fl('forum_views') . '</small></div>
            </div>
            <div class="df-col-last">
                <img src="' . $last_avatar . '" class="df-last-post-avatar" alt="' . $last_user . '">
                <div class="df-last-post-wrap">
                    <a href="' . $t_url . '#post-' . intval($t['last_post_id'] ?? 0) . '" class="df-last-title">' . $last_user . '</a>
                    <div class="df-last-meta">' . $last_date . '</div>
                </div>
            </div>
        </div>';
    }
}

 $base_url = forum_fix_url(forum_url('forum_search'), 'q=' . urlencode($q));
 $pagination = $Forum->pagination($total_topics, $per_page, $page, $base_url);

 $tpl->load_template('forum/category.tpl'); 
 $tpl->set('{breadcrumb}', $Forum->getBreadcrumb(0, 0, fl('forum_search') . ': ' . htmlspecialchars($q)));
 $tpl->set('{sub_forums}', ''); 
 $tpl->set('{cat_name}', fl('forum_search_results') . ': ' . htmlspecialchars($q, ENT_QUOTES, 'UTF-8'));
 $tpl->set('{cat_desc}', sprintf(fl('forum_topics_found'), intval($total_topics)));
 
 // v1.1.3 FIX: Arama sonuçlarında boşta kalan kategori bazlı etiketleri temizle
 $tpl->set('{mark_read_btn}', '');
 
 $tpl->set('{topics_list}', $topics_list_html);
 $tpl->set('{pagination}', $pagination);
 $tpl->set('{new_topic_btn}', '');
 $tpl->set('{SITEURL}', $config['http_home_url']);

 $tpl->compile('content'); 
 $tpl->clear();
 $metatags['title'] = fl('forum_search') . ': ' . htmlspecialchars($q, ENT_QUOTES, 'UTF-8') . ' - ' . fl('forum_forum');
?>]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/forum/actions/new_topic.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
/*
=====================================================
 DLE Native Forum (Nexus) - by Dlehub
-----------------------------------------------------
 https://github.com/dlehub
-----------------------------------------------------
 Copyright (c) 2024-2026 Dlehub & Elegance
=====================================================
 This code is protected by copyright
=====================================================
 File: engine/forum/actions/new_topic.php
-----------------------------------------------------
 Use: Yeni konu açma (Create Topic) form yönetimi ve işlemleri
=====================================================
*/
if(!defined('DATALIFEENGINE')) die("Hacking attempt!");

// 1. Giriş ve Yetki Kontrolü
if(!$is_logged) {
    msgbox(fl('forum_error'), fl('forum_err_login_topic'));
    return;
}

if(!$Forum->canDo('create_topic')) {
    msgbox(fl('forum_error'), fl('forum_err_no_perm_cat'));
    return;
}

 $cat_id = isset($_REQUEST['cat_id']) ? intval($_REQUEST['cat_id']) : 0;
 $cat = $Forum->getCatById($cat_id);

if(!$cat) {
    msgbox(fl('forum_error'), fl('forum_err_cat_select'));
    return;
}

 $error = '';
// Form verilerini korumak için değişkenler
 $post_title_val = '';
 $post_content_val = '';

// 2. Form Gönderimi ve CSRF Kontrolü
if(isset($_POST['submit_topic'])) {
    
    // GÜVENLİK: CSRF Token Doğrulaması
    if(!$Forum->verifyCsrf($_POST['csrf_token'] ?? '')) {
        $error = fl('forum_err_csrf_retry');
    } else {
        $title   = trim(strip_tags($_POST['topic_title'] ?? ''));
        $desc    = trim(strip_tags($_POST['description'] ?? ''));
        $content = trim($_POST['comments'] ?? $_POST['topic_content'] ?? '');
        
        $clean_content = trim(strip_tags(html_entity_decode($content, ENT_QUOTES, 'UTF-8')));
        $clean_content = str_replace('&nbsp;', '', $clean_content);

        if(mb_strlen($title) < 3) {
            $error = fl('forum_err_title_len');
        } elseif(mb_strlen($title) > 255) {
            $error = fl('forum_err_title_long');
        } elseif(mb_strlen($clean_content) < 10) {
            $error = fl('forum_err_content_len');
        } else {
            $result = $Forum->createTopic($cat_id, $title, $content, $desc);
            if(isset($result['error'])) {
                $error = $result['error'];
            } else {
                $new_topic_id = intval($result['topic_id']);

                // Prefix kaydet
                $prefix_id = isset($_POST['prefix_id']) ? intval($_POST['prefix_id']) : 0;
                if($prefix_id > 0) {
                    $db->query("UPDATE " . PREFIX . "_forum_topics SET prefix_id='$prefix_id' WHERE id='$new_topic_id'");
                }

                // Etiketleri kaydet
                $tags_raw = isset($_POST['tags']) ? trim($_POST['tags']) : '';
                if($tags_raw !== '') {
                    $Forum->setTopicTags($new_topic_id, $tags_raw);
                }


                header("Location: " . forum_url('forum_topic', ['newsid' => $new_topic_id]));
                exit;
            }
        }
        
        if($error) {
            $post_title_val = htmlspecialchars($title);
            $post_content_val = htmlspecialchars($content);
        }
    }
}

 $parse = new ParseFilter();
 $editor_html = "";

if($config['allow_comments_wysiwyg'] == 1) {

    // shortsite.php için gerekli değişkenler
    $id         = 0;
    $dark_theme = '';
    $row        = [
        'short_story' => ($error ? $content : ''),
    ];

    // $onload_scripts ve $js_array mevcut değilse başlat
    if(!isset($onload_scripts)) $onload_scripts = [];
    if(!isset($js_array))       $js_array = [];

    $onload_before = count($onload_scripts);

    // shortsite.php: $onload_scripts[]'e TinyMCE init yazar,
    //                $shortarea'ya textarea HTML'ini koyar
    include_once(DLEPlugins::Check(ENGINE_DIR . '/editor/shortsite.php'));

    // Textarea: short_story → comments (isim köprüsü)
    $shortarea = str_replace(
        ['name="short_story"', 'id="short_story"'],
        ['name="comments"',   'id="comments"'],
        $shortarea
    );

    // TinyMCE init scriptlerini $editor_html'e ekle
    $onload_after = count($onload_scripts);
    if($onload_after > $onload_before) {
        // tinymce.min.js'i yükle (shortsite.php $js_array'e ekliyor ama
        // forum sayfasında bu array işlenmeyebilir, script tag olarak ekleyelim)
        $tinymce_url   = $config['http_home_url'] . 'public/editor/tiny_mce/tinymce.min.js';
        $dlebutton_url = $config['http_home_url'] . 'public/editor/tiny_mce/plugins/dlebutton/plugin.min.js';

        $init_script = '';
        for($si = $onload_before; $si < $onload_after; $si++) {
            $init_script .= $onload_scripts[$si];
        }

        $editor_html = $shortarea . '
<script>
(function() {
    function runInit() {
        ' . $init_script . '
    }
    if(typeof tinymce !== "undefined") {
        runInit();
    } else {
        var s = document.createElement("script");
        s.src = "' . $tinymce_url . '";
        s.onload = function() {
            var s2 = document.createElement("script");
            s2.src = "' . $dlebutton_url . '";
            s2.onload = runInit;
            document.head.appendChild(s2);
        };
        document.head.appendChild(s);
    }
})();
</script>';
    } else {
        // Hiç onload_script eklenmedi — en azından textarea'yı göster
        $editor_html = $shortarea;
    }

} else {
    include_once(DLEPlugins::Check(ENGINE_DIR . '/editor/bbcode.php'));
    $editor_html = $bb_code;
}

// Hata sonrası içerik geri yükleme
if($error && $config['allow_comments_wysiwyg'] == 1) {
    $safe_text_js = json_encode($content, JSON_UNESCAPED_UNICODE);
    $editor_html .= "<script>
    jQuery(function(){
        var i = setInterval(function(){
            if(typeof tinymce !== 'undefined' && tinymce.get('comments') && tinymce.get('comments').initialized){
                tinymce.get('comments').setContent({$safe_text_js});
                clearInterval(i);
            }
        }, 200);
    });
    </script>";
}

global $text; 
if($error) {
    if($config['allow_comments_wysiwyg'] == 1) {
        $text = $content; 
    } else {
        $text = $post_content_val; 
    }
} else {
    $text = '';
}

// 4. Template Çıktısı
 $tpl->load_template('forum/new_topic.tpl');
 $tpl->set('{cat_name}', htmlspecialchars($cat['name']));
 $tpl->set('{cat_id}', $cat_id);

// Prefix dropdown
$prefixes     = $Forum->getPrefixes($cat_id);
$prefix_opts  = '<option value="0">-- Önek Seç --</option>';
$sel_prefix   = isset($_POST['prefix_id']) ? intval($_POST['prefix_id']) : 0;
if($prefixes) {
    foreach($prefixes as $pf) {
        $selected    = ($sel_prefix == $pf['id']) ? ' selected' : '';
        $prefix_opts .= '<option value="' . intval($pf['id']) . '"' . $selected . '>'
                      . htmlspecialchars($pf['title']) . '</option>';
    }
}
$tpl->set('{prefix_options}', count($prefixes ?? []) > 0 ? $prefix_opts : '');

// Etiket input
$tags_val = isset($_POST['tags']) ? htmlspecialchars(trim($_POST['tags'])) : '';
$tpl->set('{tags_value}', $tags_val);

// Konu açıklaması
$desc_val = isset($_POST['description']) ? htmlspecialchars(trim($_POST['description'])) : '';
$tpl->set('{description_value}', $desc_val);

// DÜZELTME: Hata olursa TinyMCE içeriği geri yükleniyor
if($error && $config['allow_comments_wysiwyg'] == 1) {
    $safe_text_js = json_encode($content, JSON_UNESCAPED_UNICODE);
    $editor_html .= "<script>document.addEventListener('DOMContentLoaded',function(){var i=setInterval(function(){if(typeof tinymce!='undefined'&&tinymce.get('comments')&&tinymce.get('comments').initialized){tinymce.get('comments').setContent({$safe_text_js});clearInterval(i);}},200);});</script>";
}


 $tpl->set('{editor}', $editor_html);
 $tpl->set('{title_val}', $post_title_val);
 $tpl->set('{csrf_token}', $Forum->csrfToken());
 $tpl->set('{breadcrumb}', $Forum->getBreadcrumb($cat_id, 0, fl('forum_new_topic_btn')));
 $tpl->set('{SITEURL}', $config['http_home_url']);

if($error) {
    $tpl->set('[error]', ''); $tpl->set('[/error]', '');
    $tpl->set('{error}', $error);
} else {
    $tpl->set_block("'\\[error\\](.*?)\\[/error\\]'is", "");
}

 $tpl->compile('content'); 
 $tpl->clear();

define('KSEARCH', false);
define('RSSPARSE', false);
 $metatags['title'] = fl('forum_new_topic_btn') . ': ' . htmlspecialchars($cat['name']) . ' - ' . $config['home_title'];
?>]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/ajax/complaint.php">
		<operation action="before">
			<searchcode><![CDATA[} elseif ($_POST['action'] == "orfo") {]]></searchcode>
			<replacecode><![CDATA[    } elseif ($_POST['action'] == "forum") {
        // 1. GÜVENLİK: CSRF Token Kontrolü
        if (!isset($_POST['user_hash']) || !hash_equals((string)$dle_login_hash, (string)$_POST['user_hash'])) {
            die("error");
        }

        // 2. Yetki ve Flood Koruması
        if (!$is_logged) {
            $author = $db->safesql($_IP);
            $db->query("SELECT id FROM " . PREFIX . "_complaint WHERE `from`='{$author}'");
            if ($db->num_rows() > 2) {
                echo $lang['error_complaint_1']; // Çok fazla şikayet
                die();
            }
        } else {
            $author = $db->safesql($member_id['name']);
        }

        $id = intval($_POST['id'] ?? 0); // DUZELTME: $_POST uzerinden alinmali
        if (!$id) die("error");
        
        $text = trim($_POST['text'] ?? '');
        if (empty($text)) {
            echo $lang['error_complaint_4']; 
            die();
        }

        // 3. Limite Takılma Kontrolü
        $row = $db->super_query("SELECT COUNT(*) as count FROM " . PREFIX . "_complaint WHERE text LIKE '[FORUM]%' AND `from`='{$author}'");
        if ($row['count'] >= $config['max_complaints']) {
            echo $lang['error_complaint_5'];
            die();
        }

        // 4. Veritabanı Kontrolü
        $post = $db->super_query("SELECT p.topic_id, t.title FROM " . PREFIX . "_forum_posts p LEFT JOIN " . PREFIX . "_forum_topics t ON t.id = p.topic_id WHERE p.id = '{$id}'");

        if (!$post) die("error"); 

        // 5. SEO Helper: forum_url() bu dosyada yuklu degil, DLE native VFS ile dahil et
        if (!function_exists('forum_url')) {
            include_once(DLEPlugins::Check(ENGINE_DIR . '/forum/forum.helper.php'));
        }
        
        $safe_topic_id = intval($post['topic_id']);
        $raw_topic_link = function_exists('forum_url')
            ? forum_url('forum_topic', ['newsid' => $safe_topic_id])
            : $config['http_home_url'] . 'forum/topic/' . $safe_topic_id . '/';
        
        // DÜZELTME: Çift encode kaldırıldı (htmlspecialchars kullanılmadan direkt safesql)
        $raw_complaint_text = "[FORUM] Mesaj ID: #{$id} | Konu: {$post['title']} | Link: {$raw_topic_link} | Neden: {$text}";
        $complaint_text = $db->safesql($raw_complaint_text);

        $db->query("INSERT INTO " . PREFIX . "_complaint (`news_id`, `from`, `text`, `date`) VALUES ('0', '{$author}', '{$complaint_text}', '{$_TIME}')");

        // 6. MAIL BİLDİRİMİ (mail sınıfı burada henüz yüklü olmayabilir)
        if ($config['allow_complaint_mail']) {
            include_once(DLEPlugins::Check(ENGINE_DIR . '/classes/mail.class.php'));
            $mail_obj = new dle_mail($config);
            $mail_message = "Sitenizde yeni bir Forum Şikayeti yapıldı:\n\n" . $raw_complaint_text . "\n\nLütfen Yönetim Paneline girerek durumu inceleyiniz.";
            $mail_obj->send($config['admin_mail'], "Yeni Forum Şikayeti: " . $post['title'], $mail_message);
        }
        
        if (ob_get_length()) ob_clean(); // Buffer temizle
        echo "ok";
        exit;]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/forum/classes/forum.class.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
/*
=====================================================
 DLE Native Forum (Nexus) - by Dlehub
-----------------------------------------------------
 https://github.com/dlehub
-----------------------------------------------------
 Copyright (c) 2024-2026 Dlehub & Elegance
=====================================================
 This code is protected by copyright
=====================================================
 File: engine/forum/classes/forum.class.php
-----------------------------------------------------
 Use: Forum çekirdek sınıfı (Core Class) - Veritabanı işlemleri, yetkilendirme ve ana fonksiyonlar
=====================================================
*/
if(!defined('DATALIFEENGINE')) die("Hacking attempt!");

class Forum {

    protected $db, $user, $config;

    public function __construct($db, $user, $config) {
        $this->db     = $db;
        $this->user   = $user;
        $this->config = $config;
    }

    /**
     * Dinamik ve dile duyarlı Breadcrumb (Ekmek Kırıntısı) oluşturur
     * @param int $cat_id Kategori ID (isteğe bağlı)
     * @param int $topic_id Konu ID (isteğe bağlı)
     * @param string $custom Özel başlık (isteğe bağlı)
     * @return string HTML Breadcrumb
     */
    public function getBreadcrumb($cat_id = 0, $topic_id = 0, $custom = '') {
        $parts = [];
        // 1. Ana Sayfa (DLE Ana Sayfası) - Dinamik Metin
        $home_text = fl('forum_home', 'Ana Sayfa');
        $parts[] = '<a href="' . $this->config['http_home_url'] . '" class="df-breadcrumb-link">' . $home_text . '</a>';
        
        // 2. Forum Ana Sayfası
        $parts[] = '<a href="' . forum_url('forum_main') . '" class="df-breadcrumb-link">' . fl('forum_forum') . '</a>';

        // 3. Kategori Hiyerarşisi (Loop Safety: $safety < 20)
        if ($cat_id > 0) {
            $current_cat_id = intval($cat_id);
            $cat_path = [];
            $safety = 0;
            
            while ($current_cat_id > 0 && $safety < 20) {
                $safety++;
                $cat = $this->getCatById($current_cat_id);
                if (!$cat) break;
                
                array_unshift($cat_path, '<a href="' . forum_url('forum_cat', ['category' => $cat['alt_name']]) . '" class="df-breadcrumb-link">' . htmlspecialchars($cat['name']) . '</a>');
                $current_cat_id = intval($cat['parent_id']);
            }
            $parts = array_merge($parts, $cat_path);
        }

        // 4. Konu Başlığı veya Özel Sayfa Başlığı
        if ($topic_id > 0) {
            $topic = $this->getTopicById($topic_id);
            if ($topic) {
                $parts[] = '<span class="df-breadcrumb-current">' . htmlspecialchars($topic['title']) . '</span>';
            }
        } elseif ($custom) {
            $parts[] = '<span class="df-breadcrumb-current">' . htmlspecialchars($custom) . '</span>';
        }

        $sep = '<i class="fa fa-chevron-right df-breadcrumb-sep"></i>';
        return '<nav class="df-breadcrumb">' . implode($sep, $parts) . '</nav>';
    }

    public function makeSlug($title) {
        return totranslit($title, true, false);
    }

    public function isLogged() {
        return !empty($this->user['user_id']);
    }


    /**
     * Gelişmiş flood kontrolü: user-based + IP-based + saatlik rate limit
     */
    private function getForumSetting($key, $default = null) {
        return $this->getSetting($key) ?? $default;
    }

    public function checkFloodPublic($action, $wait_seconds = 30) {
        if (!method_exists($this, 'checkFlood')) {
            return false;
        }
        return $this->checkFlood($action, $wait_seconds);
    }

    private function checkFlood($action, $wait_seconds = 30) {
        $user_id = $this->getUserId();
        $ip      = $this->db->safesql($_SERVER['REMOTE_ADDR'] ?? '0.0.0.0');
        $now     = time();

        // 1. Kullanıcı bazlı flood kontrolü (Integer tırnakları kaldırıldı)
        $row = $this->db->super_query(
            "SELECT last_action, count, blocked_until FROM " . PREFIX . "_forum_flood
             WHERE user_id=$user_id AND action='$action' LIMIT 1"
        );

        if($row) {
            // Blok süresi dolmadıysa reddet
            if(intval($row['blocked_until']) > $now) {
                $remaining = intval($row['blocked_until']) - $now;
                return ['error' => fl('err_flood_post', $remaining)];
            }
            // Flood süresi içindeyse say
            if(($now - intval($row['last_action'])) < $wait_seconds) {
                $count = intval($row['count']) + 1;
                // 5 kez flood → 10 dakika blok
                $blocked = ($count >= 5) ? ($now + 600) : 0;
                $this->db->query(
                    "UPDATE " . PREFIX . "_forum_flood
                     SET last_action='$now', count='$count', blocked_until='$blocked'
                     WHERE user_id='$user_id' AND action='$action'"
                );
                if($blocked) {
                    return ['error' => fl('err_flood_blocked')];
                }
                $wait = $wait_seconds - ($now - intval($row['last_action']));
                return ['error' => fl('err_flood_post', $wait)];
            }
            // Normal güncelle
            $this->db->query(
                "UPDATE " . PREFIX . "_forum_flood
                 SET last_action='$now', count='1', blocked_until='0'
                 WHERE user_id='$user_id' AND action='$action'"
            );
        } else {
            $this->db->query(
                "INSERT INTO " . PREFIX . "_forum_flood (user_id, ip, action, last_action, count, blocked_until)
                 VALUES ('$user_id', '$ip', '$action', '$now', '1', '0')"
            );
        }

        // 2. IP bazlı saatlik rate limit
        $ip_count = $this->db->super_query(
            "SELECT COUNT(*) as c FROM " . PREFIX . "_forum_flood
             WHERE ip='$ip' AND action='$action' AND last_action > " . ($now - 3600)
        );
        $hourly_limit = intval($this->getForumSetting('flood_max_per_hour') ?? 30);
        if($ip_count && intval($ip_count['c']) > $hourly_limit) {
            return ['error' => fl('err_flood_ip')];
        }

        return true;
    }

    public function getUserId() {
        return intval($this->user['user_id']);
    }

    /**
     * Çevrimiçi istatistikleri - Cache'li (5 dk), tek sorgulu
     * {prefix}_session tablosu: member_id > 0 = üye, member_id = 0 = misafir
     * 
     * @return array ['total' => int, 'members' => int, 'guests' => int, 'timestamp' => int]
     */
    public function getOnlineStats() {
        $cacheKey = "forum_online_stats";
        $cached = dle_cache($cacheKey, $this->config['skin']);
        
        if ($cached) {
            $data = unserialize($cached);
            if (is_array($data) && isset($data['timestamp']) && (time() - intval($data['timestamp'])) < 300) {
                return $data;
            }
        }
        
        $online_time = time() - intval($this->config['online_time'] ?? 300);
        
        // DLE Native: grup 1-4 = kayıtlı üye, grup 5 = misafir/onaydaki üye
        $members = $this->db->super_query(
            "SELECT COUNT(*) as c FROM " . USERPREFIX . "_users
             WHERE lastdate > '$online_time' AND user_group >= 1 AND user_group <= 4"
        );
        
        $guests = $this->db->super_query(
            "SELECT COUNT(*) as c FROM " . USERPREFIX . "_users
             WHERE lastdate > '$online_time' AND user_group = 5"
        );
        
        $members_count = intval($members['c'] ?? 0);
        $guests_count  = intval($guests['c'] ?? 0);
        
        $stats = [
            'total'     => $members_count + $guests_count,
            'members'   => $members_count,
            'guests'    => $guests_count,
            'timestamp' => time()
        ];
        
        create_cache($cacheKey, serialize($stats), $this->config['skin']);
        
        return $stats;
    }

    /**
     * Ziyaretçiyi logla - 15 dakika interval kontrolü
     * Sadece forum sayfalarında çağrılır
     */
    public function logVisitor() {
        $user_id = $this->getUserId();
        $ip      = $this->db->safesql($_SERVER['REMOTE_ADDR'] ?? '0.0.0.0');
        $page    = $this->db->safesql($_SERVER['REQUEST_URI'] ?? '');
        
        // Aynı IP'den son 15 dakikada kayıt varsa tekrar ekleme
        $exists = $this->db->super_query(
            "SELECT id FROM " . PREFIX . "_forum_visitors 
             WHERE ip = '$ip' AND date > NOW() - INTERVAL 15 MINUTE LIMIT 1"
        );
        
        if (!$exists) {
            $this->db->query(
                "INSERT INTO " . PREFIX . "_forum_visitors (user_id, ip, page, date) 
                 VALUES ('$user_id', '$ip', '$page', NOW())"
            );
        }
    }

    /**
     * Bugünkü tekil ziyaretçi sayısı - Cache'li (1 saat)
     * 
     * @return array ['total_visitors' => int, 'members' => int, 'guests' => int, 'timestamp' => int]
     */
    public function getTodayVisitors() {
        $cacheKey = "forum_today_visitors";
        $cached = dle_cache($cacheKey, $this->config['skin']);
        
        if ($cached) {
            $data = unserialize($cached);
            if (is_array($data) && isset($data['timestamp']) && (time() - intval($data['timestamp'])) < 3600) {
                return $data;
            }
        }
        
        // Tek sorguda: toplam tekil IP, tekil üye, tekil misafir
        $row = $this->db->super_query(
            "SELECT 
                COUNT(DISTINCT ip) as total_visitors,
                COUNT(DISTINCT IF(user_id > 0, user_id, NULL)) as members,
                COUNT(DISTINCT IF(user_id = 0, ip, NULL)) as guests
             FROM " . PREFIX . "_forum_visitors 
             WHERE date >= CURDATE()"
        );
        
        $stats = [
            'total_visitors' => intval($row['total_visitors'] ?? 0),
            'members'        => intval($row['members'] ?? 0),
            'guests'         => intval($row['guests'] ?? 0),
            'timestamp'      => time()
        ];
        
        create_cache($cacheKey, serialize($stats), $this->config['skin']);
        
        return $stats;
    }

    public function getOnlineCount() {
        $time = time() - 300; 
        $r = $this->db->super_query("SELECT COUNT(*) as c FROM " . USERPREFIX . "_users WHERE lastdate > '$time'");
        return intval($r['c']);
    }

    public function canDo($perm, $cat_id = 0) {
        $group   = intval($this->user['user_group']);
        $user_id = $this->getUserId();

        // Süper admin her şeyi yapabilir
        if($group == 1) return true;

        // Kategori bazlı rol kontrolü (Integer tırnakları kaldırıldı)
        if($cat_id > 0 && $user_id > 0) {
            $cat_id = intval($cat_id);
            $role_row = $this->db->super_query(
                "SELECT role FROM " . PREFIX . "_forum_roles
                 WHERE cat_id=$cat_id AND user_id=$user_id LIMIT 1"
            );
            if($role_row) {
                $role = $role_row['role'];
                if($role === 'banned') return false;
                if($role === 'admin' || $role === 'moderator') {
                    // Moderatör izinleri
                    $mod_perms = ['moderate','lock_topic','pin_topic','delete_any','reply_topic','edit_own','delete_own','view_forum'];
                    if(in_array($perm, $mod_perms)) return true;
                }
            }
        }

        // Kategori gizlilik kontrolü
        if($cat_id > 0 && $perm === 'view_forum') {
            $cat = $this->db->super_query(
                "SELECT is_private, read_groups, permissions FROM " . PREFIX . "_forum_cats WHERE id='$cat_id' LIMIT 1"
            );
            if($cat && intval($cat['is_private']) === 1) {
                // Özel kategori - read_groups kontrol
                $allowed_groups = array_filter(array_map('intval', explode(',', $cat['read_groups'])));
                if(!empty($allowed_groups) && !in_array($group, $allowed_groups)) {
                    return false;
                }
            }
        }

        // Yazma izni için write_groups kontrolü
        if($cat_id > 0 && in_array($perm, ['create_topic','reply_topic'])) {
            $cat = $this->db->super_query(
                "SELECT is_private, write_groups FROM " . PREFIX . "_forum_cats WHERE id='$cat_id' LIMIT 1"
            );
            if($cat && !empty($cat['write_groups'])) {
                $write_groups = array_filter(array_map('intval', explode(',', $cat['write_groups'])));
                if(!empty($write_groups) && !in_array($group, $write_groups)) {
                    return false;
                }
            }
        }

        // Standart DLE grup bazlı izinler
        $allowed = [
            'view_forum'   => [1, 2, 3, 4, 5, 6, 7],
            'create_topic' => [1, 2, 3, 4, 5],
            'reply_topic'  => [1, 2, 3, 4, 5],
            'edit_own'     => [1, 2, 3, 4, 5],
            'delete_own'   => [1, 2, 3, 4, 5],
            'moderate'     => [1, 2],
            'send_pm'      => [1, 2, 3, 4, 5],
            'lock_topic'   => [1, 2],
            'pin_topic'    => [1, 2],
            'delete_any'   => [1, 2],
        ];
        return isset($allowed[$perm]) && in_array($group, $allowed[$perm]);
    }

    public function csrfToken() {
        global $dle_login_hash;
        return $dle_login_hash;
    }

    public function verifyCsrf($token) {
        global $dle_login_hash;
        if(empty($token) || empty($dle_login_hash)) return false;
        return hash_equals((string)$dle_login_hash, (string)$token);
    }

    public function getSetting($name) {
        $row = $this->db->super_query("SELECT value FROM " . PREFIX . "_forum_settings WHERE name = '" . $this->db->safesql($name) . "'");
        return $row ? $row['value'] : null;
    }

    public function getCats($parent_id = 0) {
        $cacheKey = "forum_cats_" . intval($parent_id);
        $cached = dle_cache($cacheKey, $this->config['skin']);
        if($cached) return unserialize($cached);
        $pid = intval($parent_id);
        $cats = $this->db->super_query(
            "SELECT * FROM " . PREFIX . "_forum_cats WHERE parent_id = '$pid' ORDER BY posi ASC",
            true
        );
        create_cache($cacheKey, serialize($cats), $this->config['skin']);
        return $cats;
    }

    public function getCatBySlug($slug) {
        $slug = $this->db->safesql($slug);
        return $this->db->super_query("SELECT * FROM " . PREFIX . "_forum_cats WHERE alt_name = '$slug'");
    }

    public function getCatById($id) {
        $id = intval($id);
        return $this->db->super_query("SELECT * FROM " . PREFIX . "_forum_cats WHERE id = '$id'");
    }

    public function updateCatStats($cat_id) {
        $cat_id = intval($cat_id);
        $tc = $this->db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_topics WHERE cat_id = '$cat_id' AND is_deleted = '0'");
        // DÜZELTME: t.is_deleted = '0' şartı eklendi
        $pc = $this->db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_posts p JOIN " . PREFIX . "_forum_topics t ON t.id = p.topic_id WHERE t.cat_id = '$cat_id' AND p.is_deleted = '0' AND t.is_deleted = '0'");
        $last = $this->db->super_query("SELECT id FROM " . PREFIX . "_forum_topics WHERE cat_id = '$cat_id' AND is_deleted = '0' ORDER BY last_post_date DESC LIMIT 1");
        $last_id = $last ? $last['id'] : 0;
        $this->db->query("UPDATE " . PREFIX . "_forum_cats SET topic_count = '{$tc['c']}', post_count = '{$pc['c']}', last_topic_id = '$last_id' WHERE id = '$cat_id'");
        clear_cache("forum_cats_0");
        clear_cache("forum_cats_" . $cat_id);
    }

    public function getTopics($cat_id, $page = 1, $per_page = 25) {
        $cat_id   = intval($cat_id);
        $per_page = intval($per_page);
        $offset   = (max(1, intval($page)) - 1) * $per_page;
        return $this->db->super_query(
            "SELECT t.*, u.name as username, u.foto as avatar,
             lp.date as lp_date, lu.name as last_username, lu.foto as last_user_avatar
             FROM " . PREFIX . "_forum_topics t
             LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = t.user_id
             LEFT JOIN " . PREFIX . "_forum_posts lp ON lp.id = t.last_post_id
             LEFT JOIN " . USERPREFIX . "_users lu ON lu.user_id = t.last_user_id
             WHERE t.cat_id = '$cat_id' AND t.is_deleted = '0'
             ORDER BY t.is_pinned DESC, t.last_post_date DESC
             LIMIT $offset, $per_page",
            true
        );
    }

    public function countTopics($cat_id) {
        $cat_id = intval($cat_id);
        $r = $this->db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_topics WHERE cat_id = '$cat_id' AND is_deleted = '0'");
        return intval($r['c']);
    }

    public function searchTopics($query, $page = 1, $per_page = 25) {
        // Boolean mode operator injection önleme
        $query = trim($query);
        $query = preg_replace('/[+\-><\(\)~*"@]+/', ' ', $query);
        $query = preg_replace('/\s+/', ' ', $query);
        $query    = $this->db->safesql($query);
        $per_page = intval($per_page);
        $offset   = (max(1, intval($page)) - 1) * $per_page;
        return $this->db->super_query(
            "SELECT t.*, u.name as username, u.foto as avatar,
             lp.date as lp_date, lu.name as last_username, lu.foto as last_user_avatar,
             c.alt_name as cat_slug
             FROM " . PREFIX . "_forum_topics t
             LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = t.user_id
             LEFT JOIN " . PREFIX . "_forum_posts lp ON lp.id = t.last_post_id
             LEFT JOIN " . USERPREFIX . "_users lu ON lu.user_id = t.last_user_id
             LEFT JOIN " . PREFIX . "_forum_cats c ON c.id = t.cat_id
             WHERE MATCH(t.title) AGAINST('$query' IN BOOLEAN MODE) AND t.is_deleted = '0'
             ORDER BY t.last_post_date DESC
             LIMIT $offset, $per_page",
            true
        );
    }

    public function countSearchTopics($query) {
        // Boolean mode operator injection önleme
        $query = trim($query);
        $query = preg_replace('/[+\-><\(\)~*"@]+/', ' ', $query);
        $query = preg_replace('/\s+/', ' ', $query);
        $query = $this->db->safesql($query);
        $r = $this->db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_topics t WHERE MATCH(t.title) AGAINST('$query' IN BOOLEAN MODE) AND t.is_deleted = '0'");
        return intval($r['c']);
    }

    public function getTopicBySlug($slug) {
        $slug = $this->db->safesql($slug);
        return $this->db->super_query(
            "SELECT t.*, u.name as username, u.foto as avatar, c.name as cat_name, c.alt_name as cat_slug
             FROM " . PREFIX . "_forum_topics t
             LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = t.user_id
             LEFT JOIN " . PREFIX . "_forum_cats c ON c.id = t.cat_id
             WHERE t.alt_name = '$slug' AND t.is_deleted = '0'"
        );
    }

    public function getTopicById($id) {
        $id = intval($id);
        return $this->db->super_query(
            "SELECT t.*, u.name as username, u.foto as avatar, c.name as cat_name, c.alt_name as cat_slug,
             lu.name as last_username, lu.foto as last_user_avatar
             FROM " . PREFIX . "_forum_topics t
             LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = t.user_id
             LEFT JOIN " . USERPREFIX . "_users lu ON lu.user_id = t.last_user_id
             LEFT JOIN " . PREFIX . "_forum_cats c ON c.id = t.cat_id
             WHERE t.id = '$id' AND t.is_deleted = '0'"
        );
    }

    public function createTopic($cat_id, $title, $text, $description = '') {
        if(!$this->isLogged()) return ['error' => fl('forum_err_must_login')];
        if(!$this->canDo('create_topic')) return ['error' => fl('forum_err_no_permission')];

        $cat_id   = intval($cat_id);
        $title    = trim($title);
        $text     = trim($text);
        $user_id  = $this->getUserId();

        if(mb_strlen($title) < 3)   return ['error' => fl('forum_err_title_short')];
        if(mb_strlen($title) > 255) return ['error' => fl('forum_err_title_long')];
        $clean_text = trim(strip_tags(html_entity_decode($text, ENT_QUOTES, 'UTF-8')));
        if(mb_strlen($clean_text) < 10) return ['error' => fl('forum_err_post_short')];

        $flood_time = intval($this->getForumSetting('flood_time') ?: 30);
        $flood_result = $this->checkFlood('topic', $flood_time);
        if($flood_result !== true) return $flood_result;

        // DÜZELTME: Slug HAM metinden alınıyor
        $slug = $this->makeSlug($title);
        $exists = $this->db->super_query("SELECT id FROM " . PREFIX . "_forum_topics WHERE alt_name = '$slug'");
        if($exists) $slug .= '-' . time();

        // DÜZELTME: Başlıkta ÇİFT ENCODE önleniyor, sadece safesql kullanılıyor. 
        $title_db = $this->db->safesql($title);
        $desc_db  = $this->db->safesql(mb_substr(trim(strip_tags($description)), 0, 500));
        $text_db  = $this->db->safesql($text);

        $ip = $this->db->safesql($_SERVER['REMOTE_ADDR']);

        $this->db->query("INSERT INTO " . PREFIX . "_forum_topics (cat_id, title, alt_name, description, user_id, date, last_post_date) VALUES ('$cat_id', '$title_db', '$slug', '$desc_db', '$user_id', NOW(), NOW())");
        $topic_id = $this->db->insert_id();

        $this->db->query("INSERT INTO " . PREFIX . "_forum_posts (topic_id, user_id, text, ip, date) VALUES ('$topic_id', '$user_id', '$text_db', '$ip', NOW())");
        $post_id = $this->db->insert_id();

        $this->db->query("UPDATE " . PREFIX . "_forum_topics SET last_post_id = '$post_id', last_user_id = '$user_id' WHERE id = '$topic_id'");

        $this->addPoints($user_id, intval($this->getSetting('points_new_topic') ?: 10));
        $this->updatePostCount($user_id);
        $this->updateCatStats($cat_id);
        clear_cache("forum_topics_" . $cat_id);
        
        // DÜZELTME: Mentionlar HAM text üzerinden aranmalı
        $this->parseMentions($text, $topic_id, $post_id);

        return ['success' => true, 'topic_id' => $topic_id, 'slug' => $slug];
    }

    public function lockTopic($topic_id, $state = 1) {
        if(!$this->canDo('lock_topic')) return false;
        $topic_id = intval($topic_id);
        $state    = $state ? 1 : 0;
        $this->db->query("UPDATE " . PREFIX . "_forum_topics SET is_locked = '$state' WHERE id = '$topic_id'");
        $this->logAction($state ? 'lock_topic' : 'unlock_topic', $topic_id);
        return true;
    }

    public function pinTopic($topic_id, $state = 1) {
        if(!$this->canDo('pin_topic')) return false;
        $topic_id = intval($topic_id);
        $state    = $state ? 1 : 0;
        $this->db->query("UPDATE " . PREFIX . "_forum_topics SET is_pinned = '$state' WHERE id = '$topic_id'");
        $this->logAction($state ? 'pin_topic' : 'unpin_topic', $topic_id);
        return true;
    }

    public function deleteTopic($topic_id) {
        if(!$this->canDo('delete_any')) return false;
        $topic_id = intval($topic_id);
        $topic    = $this->getTopicById($topic_id);
        if(!$topic) return false;
        // DÜZELTME: Konuyla birlikte mesajlar da siliniyor
        $this->db->query("UPDATE " . PREFIX . "_forum_posts SET is_deleted = '1' WHERE topic_id = '$topic_id'");
        $this->db->query("UPDATE " . PREFIX . "_forum_topics SET is_deleted = '1' WHERE id = '$topic_id'");
        $this->logAction('delete_topic', $topic_id);
        $this->updateCatStats($topic['cat_id']);
        return true;
    }

    public function incrementViews($topic_id) {
        $topic_id = intval($topic_id);
        $ip       = $_SERVER['REMOTE_ADDR'];
        $cacheKey = 'forum_view_' . $topic_id . '_' . md5($ip);
        if(!dle_cache($cacheKey, $this->config['skin'])) {
            $this->db->query("UPDATE " . PREFIX . "_forum_topics SET views = views + 1 WHERE id = '$topic_id'");
            create_cache($cacheKey, '1', $this->config['skin']);
        }
    }

    public function getPosts($topic_id, $page = 1, $per_page = 20) {
        $topic_id = intval($topic_id);
        $per_page = intval($per_page);
        $offset   = (max(1, intval($page)) - 1) * $per_page;
        return $this->db->super_query(
            "SELECT p.*,
             u.name as username, u.foto as avatar, u.user_group, u.reg_date,
             u.forum_points, u.forum_post_count, u.forum_rank_id,
             r.title as rank_title, r.color as rank_color, r.icon as rank_icon
             FROM " . PREFIX . "_forum_posts p
             LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = p.user_id
             LEFT JOIN " . PREFIX . "_forum_ranks r ON r.id = u.forum_rank_id
             WHERE p.topic_id = '$topic_id' AND p.is_deleted = '0' AND p.is_approved = '1'
             ORDER BY p.date ASC
             LIMIT $offset, $per_page",
            true
        );
    }

    public function countPosts($topic_id) {
        $topic_id = intval($topic_id);
        $r = $this->db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_posts WHERE topic_id = '$topic_id' AND is_deleted = '0'");
        return intval($r['c']);
    }

    public function addReply($topic_id, $text) {
        if(!$this->isLogged()) return ['error' => fl('forum_err_must_login')];
        if(!$this->canDo('reply_topic')) return ['error' => fl('forum_err_no_permission')];

        $topic_id = intval($topic_id);
        $topic    = $this->getTopicById($topic_id);
        if(!$topic) return ['error' => fl('forum_err_topic_not_found')];
        if($topic['is_locked'] && !$this->canDo('moderate')) return ['error' => fl('forum_err_topic_locked')];

        $text    = trim($text);
        $user_id = $this->getUserId();
        $minLen  = intval($this->getSetting('min_post_length') ?: 10);

        // DÜZELTME: Uzunluk kontrolü HAM metinde yapılmalı
        if(mb_strlen(strip_tags(html_entity_decode($text, ENT_QUOTES, 'UTF-8'))) < $minLen) return ['error' => sprintf(fl('forum_err_post_min'), $minLen)];

        $flood_time = intval($this->getForumSetting('flood_time') ?: 30);
        $flood_result = $this->checkFlood('reply', $flood_time);
        if($flood_result !== true) return $flood_result;

        $text_safe = $this->db->safesql($text);
        $ip = $this->db->safesql($_SERVER['REMOTE_ADDR']);

        $is_approved = $this->needsApproval($user_id) ? 0 : 1;
        $this->db->query("INSERT INTO " . PREFIX . "_forum_posts (topic_id, user_id, text, ip, date, is_approved) VALUES ('$topic_id', '$user_id', '$text_safe', '$ip', NOW(), '$is_approved')");
        $post_id = $this->db->insert_id();
        if(!$is_approved) return ['success' => true, 'post_id' => $post_id, 'pending' => true, 'message' => fl('forum_ok_approval_pending')];

        $this->db->query("UPDATE " . PREFIX . "_forum_topics SET replies = replies + 1, last_post_id = '$post_id', last_user_id = '$user_id', last_post_date = NOW() WHERE id = '$topic_id'");
        $this->updateCatStats($topic['cat_id']);

        $this->addPoints($user_id, intval($this->getSetting('points_new_reply') ?: 2));
        $this->updatePostCount($user_id);
        $this->updateUserRank($user_id);

        $subs = $this->db->super_query("SELECT user_id FROM " . PREFIX . "_forum_subs WHERE topic_id = '$topic_id'", true);
        foreach($subs as $sub) {
            $this->sendNotif($sub['user_id'], 'reply', $topic_id, $post_id);
        }

        $this->sendNotif($topic['user_id'], 'reply', $topic_id, $post_id);
        $this->parseMentions($text, $topic_id, $post_id);
        
        clear_cache("forum_topics_" . $topic['cat_id']);
        return ['success' => true, 'post_id' => $post_id];
    }

    public function editPost($post_id, $new_text) {
        if(!$this->isLogged()) return ['error' => fl('forum_err_must_login')];
        $post_id  = intval($post_id);
        $post     = $this->db->super_query("SELECT * FROM " . PREFIX . "_forum_posts WHERE id = '$post_id' AND is_deleted = '0'");
        if(!$post) return ['error' => fl('forum_err_post_not_found')];

        $own = $post['user_id'] == $this->getUserId();
        if(!$own && !$this->canDo('moderate')) return ['error' => fl('forum_err_no_permission')];

        $new_text = trim($new_text);
        if(mb_strlen(strip_tags(html_entity_decode($new_text, ENT_QUOTES, 'UTF-8'))) < 10) return ['error' => fl('forum_err_post_short')];

        // KRİTİK DÜZELTME: Eski metin DB'den ham gelse bile, yeni bir INSERT sorgusuna 
        // gömüleceği için MUTLAKA tekrar safesql ile escape edilmelidir. Aksi SQL syntax error verir.
        $old_text_safe = $this->db->safesql($post['text']);
        $this->db->query("INSERT INTO " . PREFIX . "_forum_edit_history (post_id, editor_id, old_text, date) VALUES ('$post_id', '" . $this->getUserId() . "', '$old_text_safe', NOW())");
        
        $new_text_safe = $this->db->safesql($new_text);
        $this->db->query("UPDATE " . PREFIX . "_forum_posts SET text = '$new_text_safe', updated_at = NOW() WHERE id = '$post_id'");
        
        if($own) $this->logAction('edit_own_post', $post['topic_id'], $post_id);
        else $this->logAction('edit_post_as_mod', $post['topic_id'], $post_id);
        clear_cache('forum_sticky_' . $post['topic_id']);
        return ['success' => true];
    }

    public function deletePost($post_id) {
        if(!$this->isLogged()) return ['error' => fl('forum_err_must_login')];
        $post_id = intval($post_id);
        $post    = $this->db->super_query("SELECT * FROM " . PREFIX . "_forum_posts WHERE id = '$post_id' AND is_deleted = '0'");
        if(!$post) return ['error' => fl('forum_err_post_not_found')];

        $own = $post['user_id'] == $this->getUserId();
        if(!$own && !$this->canDo('moderate')) return ['error' => fl('forum_err_no_permission')];

        $topic_id = intval($post['topic_id']);
        $topic = $this->getTopicById($topic_id);
        
        $this->db->query("UPDATE " . PREFIX . "_forum_posts SET is_deleted = '1' WHERE id = '$post_id'");
        $this->db->query("UPDATE " . PREFIX . "_forum_topics SET replies = GREATEST(replies - 1, 0) WHERE id = '$topic_id'");
        
        if($topic && $topic['last_post_id'] == $post_id) {
            $new_last = $this->db->super_query("SELECT id, user_id, date FROM " . PREFIX . "_forum_posts WHERE topic_id='$topic_id' AND is_deleted='0' ORDER BY date DESC LIMIT 1");
            if($new_last) {
                $this->db->query("UPDATE " . PREFIX . "_forum_topics SET last_post_id='{$new_last['id']}', last_user_id='{$new_last['user_id']}', last_post_date='{$new_last['date']}' WHERE id='$topic_id'");
            } else {
                $this->db->query("UPDATE " . PREFIX . "_forum_topics SET last_post_id='0', last_user_id='0', last_post_date=NOW() WHERE id='$topic_id'");
            }
            $this->updateCatStats($topic['cat_id']);
        }

        $this->logAction('delete_post', $topic_id, $post_id);
        clear_cache('forum_sticky_' . $topic_id);
        return ['success' => true];
    }

    public function toggleLike($post_id, $type = 'like') {
        if(!$this->isLogged()) return ['error' => fl('forum_err_must_login')];
        $post_id = intval($post_id);
        $type    = ($type === 'dislike') ? 'dislike' : 'like';
        $user_id = $this->getUserId();

        // Like flood koruması
        $flood_result = $this->checkFlood('like', 2);
        if($flood_result !== true) return $flood_result;

        $post = $this->db->super_query("SELECT user_id, topic_id, likes, dislikes FROM " . PREFIX . "_forum_posts WHERE id = '$post_id' AND is_deleted = '0'");
        if(!$post) return ['error' => fl('forum_err_post_not_found')];
        if($post['user_id'] == $user_id) return ['error' => fl('forum_err_own_like')];

        $check = $this->db->super_query("SELECT id, type FROM " . PREFIX . "_forum_likes WHERE post_id = '$post_id' AND user_id = '$user_id'");

        if($check) {
            if($check['type'] === $type) {
                $this->db->query("DELETE FROM " . PREFIX . "_forum_likes WHERE id = '{$check['id']}'");
                $field = ($type === 'like') ? 'likes' : 'dislikes';
                $this->db->query("UPDATE " . PREFIX . "_forum_posts SET $field = GREATEST($field - 1, 0) WHERE id = '$post_id'");
                $counts = $this->db->super_query("SELECT likes, dislikes FROM " . PREFIX . "_forum_posts WHERE id = '$post_id'");
                return ['status' => 'removed', 'type' => $type, 'likes' => intval($counts['likes']), 'dislikes' => intval($counts['dislikes'])];
            } else {
                $this->db->query("UPDATE " . PREFIX . "_forum_likes SET type = '$type' WHERE id = '{$check['id']}'");
                if($type === 'like') {
                    $this->db->query("UPDATE " . PREFIX . "_forum_posts SET likes = likes + 1, dislikes = GREATEST(dislikes - 1, 0) WHERE id = '$post_id'");
                } else {
                    $this->db->query("UPDATE " . PREFIX . "_forum_posts SET dislikes = dislikes + 1, likes = GREATEST(likes - 1, 0) WHERE id = '$post_id'");
                }
                $counts = $this->db->super_query("SELECT likes, dislikes FROM " . PREFIX . "_forum_posts WHERE id = '$post_id'");
                $status = ($type === 'like') ? 'liked' : 'disliked';
                return ['status' => $status, 'type' => $type, 'likes' => intval($counts['likes']), 'dislikes' => intval($counts['dislikes'])];
            }
        } else {
            $this->db->query("INSERT INTO " . PREFIX . "_forum_likes (post_id, user_id, type, date) VALUES ('$post_id', '$user_id', '$type', NOW())");
            if($type === 'like') {
                $this->db->query("UPDATE " . PREFIX . "_forum_posts SET likes = likes + 1 WHERE id = '$post_id'");
                $this->addPoints($post['user_id'], intval($this->getSetting('points_received_like') ?: 5));
                $this->sendNotif($post['user_id'], 'like', $post['topic_id'], $post_id);
                $this->updateUserRank($post['user_id']);
            } else {
                $this->db->query("UPDATE " . PREFIX . "_forum_posts SET dislikes = dislikes + 1 WHERE id = '$post_id'");
            }
            $counts = $this->db->super_query("SELECT likes, dislikes FROM " . PREFIX . "_forum_posts WHERE id = '$post_id'");
            $status = ($type === 'like') ? 'liked' : 'disliked';
            return ['status' => $status, 'type' => $type, 'likes' => intval($counts['likes']), 'dislikes' => intval($counts['dislikes'])];
        }
    }

    public function toggleFollow($topic_id) {
        if(!$this->isLogged()) return ['error' => fl('forum_err_must_login')];
        $topic_id = intval($topic_id);
        $user_id  = $this->getUserId();
        $check    = $this->db->super_query("SELECT id FROM " . PREFIX . "_forum_subs WHERE topic_id = '$topic_id' AND user_id = '$user_id'");
        if($check) {
            $this->db->query("DELETE FROM " . PREFIX . "_forum_subs WHERE id = '{$check['id']}'");
            return ['status' => 'unfollowed'];
        } else {
            $this->db->query("INSERT INTO " . PREFIX . "_forum_subs (topic_id, user_id, date) VALUES ('$topic_id', '$user_id', NOW())");
            return ['status' => 'followed'];
        }
    }

    public function isFollowing($topic_id) {
        if(!$this->isLogged()) return false;
        $topic_id = intval($topic_id);
        $user_id  = $this->getUserId();
        return (bool)$this->db->super_query("SELECT id FROM " . PREFIX . "_forum_subs WHERE topic_id = '$topic_id' AND user_id = '$user_id'");
    }

    public function sendNotif($to_user, $type, $topic_id, $post_id = 0, $extra = []) {
        $to_user  = intval($to_user);
        $from_user = $this->getUserId();
        if($to_user == $from_user || $to_user <= 0) return;
        $dup = $this->db->super_query("SELECT id FROM " . PREFIX . "_forum_notifs WHERE user_id = '$to_user' AND from_user = '$from_user' AND type = '" . $this->db->safesql($type) . "' AND topic_id = '" . intval($topic_id) . "' AND date > NOW() - INTERVAL 1 HOUR");
        if($dup) return;
        // JSON payload - genişletilebilir bildirim verisi
        $payload_data = [
            'type'     => $type,
            'topic_id' => intval($topic_id),
            'post_id'  => intval($post_id),
        ];
        // Ek veri varsa payload'a ekle (quote, mention, system)
        if(isset($extra) && is_array($extra)) {
            $payload_data = array_merge($payload_data, $extra);
        }
        $payload_json = $this->db->safesql(json_encode($payload_data, JSON_UNESCAPED_UNICODE));

        $this->db->query("INSERT INTO " . PREFIX . "_forum_notifs (user_id, from_user, type, topic_id, post_id, payload, date) VALUES ('$to_user', '$from_user', '" . $this->db->safesql($type) . "', '" . intval($topic_id) . "', '" . intval($post_id) . "', '$payload_json', NOW())");
    }


    public function notifyQuote($to_user, $topic_id, $post_id, $quote_preview = '') {
        $this->sendNotif($to_user, 'quote', $topic_id, $post_id, [
            'preview' => mb_substr(strip_tags($quote_preview), 0, 100),
        ]);
    }

    public function notifyMention($to_user, $topic_id, $post_id) {
        $this->sendNotif($to_user, 'mention', $topic_id, $post_id);
    }

    public function notifySystem($to_user, $message, $link = '') {
        $from_user = 0;
        $to_user   = intval($to_user);
        if($to_user <= 0) return;
        $payload_json = $this->db->safesql(json_encode([
            'type'    => 'system',
            'message' => mb_substr($message, 0, 255),
            'link'    => $link,
        ], JSON_UNESCAPED_UNICODE));
        $this->db->query(
            "INSERT INTO " . PREFIX . "_forum_notifs (user_id, from_user, type, topic_id, post_id, payload, date)
             VALUES ('$to_user', '0', 'system', '0', '0', '$payload_json', NOW())"
        );
    }

    public function getNotifs($user_id = null, $limit = 10) {
        $uid = $user_id ? intval($user_id) : $this->getUserId();
        return $this->db->super_query(
            "SELECT n.*, u.name as from_name, t.title as topic_title
             FROM " . PREFIX . "_forum_notifs n
             LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = n.from_user
             LEFT JOIN " . PREFIX . "_forum_topics t ON t.id = n.topic_id
             WHERE n.user_id = '$uid'
             ORDER BY n.date DESC LIMIT " . intval($limit),
            true
        );
    }

    public function countUnreadNotifs() {
        if(!$this->isLogged()) return 0;
        $uid = $this->getUserId();
        $r   = $this->db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_notifs WHERE user_id = '$uid' AND is_read = '0'");
        return intval($r['c']);
    }

    public function markNotifsRead() {
        $uid = $this->getUserId();
        $this->db->query("UPDATE " . PREFIX . "_forum_notifs SET is_read = '1' WHERE user_id = '$uid'");
    }

    public function markRead($topic_id, $post_id) {
        if(!$this->isLogged()) return;
        $topic_id = intval($topic_id);
        $post_id  = intval($post_id);
        $user_id  = $this->getUserId();
        // Integer değerler için tırnaklar kaldırıldı
        $exists   = $this->db->super_query("SELECT id FROM " . PREFIX . "_forum_readlog WHERE user_id = $user_id AND topic_id = $topic_id");
        if($exists) {
            $exists_id = intval($exists['id']);
            $this->db->query("UPDATE " . PREFIX . "_forum_readlog SET last_read_post = $post_id, date = NOW() WHERE id = $exists_id");
        } else {
            $this->db->query("INSERT INTO " . PREFIX . "_forum_readlog (user_id, topic_id, last_read_post, date) VALUES ($user_id, $topic_id, $post_id, NOW())");
        }
    }

    public function isUnread($topic_id, $last_post_id) {
        if(!$this->isLogged()) return false;
        $topic_id = intval($topic_id);
        $user_id  = $this->getUserId();
        // Integer optimizasyonu yapıldı
        $log      = $this->db->super_query("SELECT last_read_post FROM " . PREFIX . "_forum_readlog WHERE user_id = $user_id AND topic_id = $topic_id");
        if(!$log) return true;
        return intval($log['last_read_post']) < intval($last_post_id);
    }

    public function markAsRead($topic_id, $last_post_id) {
        if(!$this->isLogged()) return;
        $topic_id    = intval($topic_id);
        $last_post_id = intval($last_post_id);
        $user_id     = $this->getUserId();
        $now         = date('Y-m-d H:i:s');
        $this->db->query(
            "INSERT INTO " . PREFIX . "_forum_readlog (user_id, topic_id, last_read_post, date)
             VALUES ('$user_id', '$topic_id', '$last_post_id', '$now')
             ON DUPLICATE KEY UPDATE last_read_post='$last_post_id', date='$now'"
        );
    }

    public function markCatAsRead($cat_id) {
        if(!$this->isLogged()) return;
        $cat_id  = intval($cat_id);
        $user_id = $this->getUserId();
        $now     = date('Y-m-d H:i:s');
        // Kategorideki tüm konuları okunmuş say
        $topics = $this->db->super_query(
            "SELECT id, last_post_id FROM " . PREFIX . "_forum_topics
             WHERE cat_id='$cat_id' AND is_deleted='0'",
            true
        );
        if($topics) {
            foreach($topics as $t) {
                $tid = intval($t['id']);
                $lpid = intval($t['last_post_id']);
                $this->db->query(
                    "INSERT INTO " . PREFIX . "_forum_readlog (user_id, topic_id, last_read_post, date)
                     VALUES ('$user_id', '$tid', '$lpid', '$now')
                     ON DUPLICATE KEY UPDATE last_read_post='$lpid', date='$now'"
                );
            }
        }
    }

    public function cleanReadLog($days = 30) {
        $days = intval($days);
        $this->db->query(
            "DELETE FROM " . PREFIX . "_forum_readlog
             WHERE date < DATE_SUB(NOW(), INTERVAL $days DAY)"
        );
    }

    // === PREFIX FONKSIYONLARI ===

    public function getPrefixes($cat_id = 0) {
        $where = $cat_id > 0 ? "WHERE cat_id='" . intval($cat_id) . "' OR cat_id='0'" : "";
        return $this->db->super_query(
            "SELECT * FROM " . PREFIX . "_forum_prefixes $where ORDER BY posi ASC, id ASC",
            true
        );
    }

    public function getPrefixById($prefix_id) {
        $prefix_id = intval($prefix_id);
        if($prefix_id <= 0) return null;
        return $this->db->super_query(
            "SELECT * FROM " . PREFIX . "_forum_prefixes WHERE id='$prefix_id' LIMIT 1"
        );
    }

    public function formatPrefix($prefix) {
        if(!$prefix) return '';
        $title    = htmlspecialchars($prefix['title'], ENT_QUOTES, 'UTF-8');
        $color    = preg_replace('/[^a-zA-Z0-9#]/', '', $prefix['color']);
        $bg_color = preg_replace('/[^a-zA-Z0-9#]/', '', $prefix['bg_color']);
        return "<span class='df-prefix' style='color:{$color};background:{$bg_color};padding:2px 7px;border-radius:3px;font-size:11px;font-weight:600;margin-right:5px;'>{$title}</span>";
    }

    // === ETİKET (TAG) FONKSIYONLARI ===

    public function getTopicTags($topic_id) {
        $topic_id = intval($topic_id);
        return $this->db->super_query(
            "SELECT t.* FROM " . PREFIX . "_forum_tags t
             JOIN " . PREFIX . "_forum_topic_tags tt ON tt.tag_id = t.id
             WHERE tt.topic_id='$topic_id'
             ORDER BY t.name ASC",
            true
        );
    }

    public function setTopicTags($topic_id, $tags_str) {
        $topic_id = intval($topic_id);
        if($topic_id <= 0) return;

        // Önce eski etiketleri temizle, count'ları düşür
        $old_tags = $this->getTopicTags($topic_id);
        if($old_tags) {
            foreach($old_tags as $ot) {
                $this->db->query("UPDATE " . PREFIX . "_forum_tags SET count=GREATEST(0,count-1) WHERE id='" . intval($ot['id']) . "'");
            }
        }
        $this->db->query("DELETE FROM " . PREFIX . "_forum_topic_tags WHERE topic_id='$topic_id'");

        if(empty(trim($tags_str))) return;

        // Yeni etiketleri işle - max 5 etiket, max 30 karakter
        $raw_tags = array_slice(array_unique(array_map('trim', explode(',', $tags_str))), 0, 5);
        foreach($raw_tags as $tag_name) {
            $tag_name = mb_substr(preg_replace('/[^\p{L}\p{N}\s\-]/u', '', $tag_name), 0, 30);
            if(mb_strlen($tag_name) < 2) continue;
            $slug = mb_strtolower(str_replace(' ', '-', $tag_name));
            $safe_name = $this->db->safesql($tag_name);
            $safe_slug = $this->db->safesql($slug);

            // Etiket yoksa oluştur
            $this->db->query(
                "INSERT INTO " . PREFIX . "_forum_tags (name, slug, count)
                 VALUES ('$safe_name', '$safe_slug', 1)
                 ON DUPLICATE KEY UPDATE count=count+1, name='$safe_name'"
            );
            $tag_id = $this->db->insert_id();
            if(!$tag_id) {
                $row = $this->db->super_query("SELECT id FROM " . PREFIX . "_forum_tags WHERE slug='$safe_slug' LIMIT 1");
                $tag_id = $row ? intval($row['id']) : 0;
            }
            if($tag_id > 0) {
                $this->db->query(
                    "INSERT IGNORE INTO " . PREFIX . "_forum_topic_tags (topic_id, tag_id)
                     VALUES ('$topic_id', '$tag_id')"
                );
            }
        }
    }

    public function getTopicsByTag($tag_slug, $page = 1, $per_page = 25) {
        $tag_slug = $this->db->safesql($tag_slug);
        $per_page = intval($per_page);
        $offset   = (max(1, intval($page)) - 1) * $per_page;
        
        // KRİTİK DÜZELTME: c.name as cat_name ve c.alt_name as cat_slug eklendi.
        // LEFT JOIN _forum_cats eklendi.
        return $this->db->super_query(
            "SELECT t.*, u.name as username, u.foto as avatar, c.name as cat_name, c.alt_name as cat_slug
             FROM " . PREFIX . "_forum_topics t
             JOIN " . PREFIX . "_forum_topic_tags tt ON tt.topic_id = t.id
             JOIN " . PREFIX . "_forum_tags tg ON tg.id = tt.tag_id
             LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = t.user_id
             LEFT JOIN " . PREFIX . "_forum_cats c ON c.id = t.cat_id
             WHERE tg.slug='$tag_slug' AND t.is_deleted='0'
             ORDER BY t.last_post_date DESC
             LIMIT $offset, $per_page",
            true
        );
    }

    public function getPopularTags($limit = 20) {
        $limit = intval($limit);
        return $this->db->super_query(
            "SELECT * FROM " . PREFIX . "_forum_tags WHERE count > 0 ORDER BY count DESC LIMIT $limit",
            true
        );
    }

    // === YANITSIZ / AKTİF KONULAR ===

    public function getUnansweredTopics($page = 1, $per_page = 25) {
        $per_page = intval($per_page);
        $offset   = (max(1, intval($page)) - 1) * $per_page;
        return $this->db->super_query(
            "SELECT t.*, u.name as username, u.foto as avatar,
             c.name as cat_name, c.alt_name as cat_slug
             FROM " . PREFIX . "_forum_topics t
             LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = t.user_id
             LEFT JOIN " . PREFIX . "_forum_cats c ON c.id = t.cat_id
             WHERE t.replies='0' AND t.is_deleted='0' AND t.is_locked='0'
             ORDER BY t.date DESC
             LIMIT $offset, $per_page",
            true
        );
    }

    public function countUnansweredTopics() {
        $r = $this->db->super_query(
            "SELECT COUNT(*) as c FROM " . PREFIX . "_forum_topics
             WHERE replies='0' AND is_deleted='0' AND is_locked='0'"
        );
        return intval($r['c']);
    }

    public function getActiveTopics($page = 1, $per_page = 25, $days = 7) {
        $per_page = intval($per_page);
        $offset   = (max(1, intval($page)) - 1) * $per_page;
        $days     = intval($days);
        return $this->db->super_query(
            "SELECT t.*, u.name as username, u.foto as avatar,
             c.name as cat_name, c.alt_name as cat_slug,
             lu.name as last_username
             FROM " . PREFIX . "_forum_topics t
             LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = t.user_id
             LEFT JOIN " . USERPREFIX . "_users lu ON lu.user_id = t.last_user_id
             LEFT JOIN " . PREFIX . "_forum_cats c ON c.id = t.cat_id
             WHERE t.is_deleted='0'
             AND t.last_post_date > DATE_SUB(NOW(), INTERVAL $days DAY)
             ORDER BY t.last_post_date DESC
             LIMIT $offset, $per_page",
            true
        );
    }

    public function countActiveTopics($days = 7) {
        $days = intval($days);
        $r = $this->db->super_query(
            "SELECT COUNT(*) as c FROM " . PREFIX . "_forum_topics
             WHERE is_deleted='0'
             AND last_post_date > DATE_SUB(NOW(), INTERVAL $days DAY)"
        );
        return intval($r['c']);
    }

    public function addPoints($user_id, $points) {
        $user_id = intval($user_id);
        $points  = intval($points);
        if($user_id <= 0 || $points == 0) return;
        $this->db->query("UPDATE " . USERPREFIX . "_users SET forum_points = forum_points + $points WHERE user_id = '$user_id'");
        $this->updateUserRank($user_id);
    }

    public function updateUserRank($user_id) {
        $user_id = intval($user_id);
        $user    = $this->db->super_query("SELECT forum_points FROM " . USERPREFIX . "_users WHERE user_id = '$user_id'");
        if(!$user) return;
        $rank = $this->db->super_query(
            "SELECT id FROM " . PREFIX . "_forum_ranks WHERE points <= '" . intval($user['forum_points']) . "' ORDER BY points DESC LIMIT 1"
        );
        if($rank) {
            $this->db->query("UPDATE " . USERPREFIX . "_users SET forum_rank_id = '{$rank['id']}' WHERE user_id = '$user_id'");
        }
    }

    public function updatePostCount($user_id) {
        $user_id = intval($user_id);
        $cnt = $this->db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_posts WHERE user_id = '$user_id' AND is_deleted = '0'");
        $this->db->query("UPDATE " . USERPREFIX . "_users SET forum_post_count = '{$cnt['c']}' WHERE user_id = '$user_id'");
    }

    public function logAction($action, $topic_id = 0, $post_id = 0) {
        $mod_id   = $this->getUserId();
        $ip       = $this->db->safesql($_SERVER['REMOTE_ADDR']);
        $action   = $this->db->safesql($action);
        $topic_id = intval($topic_id);
        $post_id  = intval($post_id);
        $target   = $post_id > 0 ? 'post' : 'topic';
        $this->db->query("INSERT INTO " . PREFIX . "_forum_logs (mod_id, action, target_type, topic_id, post_id, date, ip) VALUES ('$mod_id', '$action', '$target', '$topic_id', '$post_id', NOW(), '$ip')");
    }

    public function reportPost($post_id, $reason) {
        if(!$this->isLogged()) return ['error' => fl('forum_err_must_login')];

        $post_id  = intval($post_id);
        $reason   = trim($reason);
        $user_id  = $this->getUserId();
        $reporter = $this->db->safesql($this->user['name']);

        if(mb_strlen($reason) < 5) return ['error' => fl('forum_err_complaint_short')];

        $dup = $this->db->super_query(
            "SELECT id FROM " . PREFIX . "_complaint WHERE `from` = '$reporter' AND text LIKE '[FORUM]%' AND news_id = '0'"
        );
        if($dup) return ['error' => fl('forum_err_complaint_dup')];

        $post = $this->db->super_query(
            "SELECT p.topic_id, t.title, t.alt_name FROM " . PREFIX . "_forum_posts p
             LEFT JOIN " . PREFIX . "_forum_topics t ON t.id = p.topic_id
             WHERE p.id = '$post_id' AND p.is_deleted = '0'"
        );
        if(!$post) return ['error' => fl('forum_err_post_not_found')];

        $raw_topic_link = forum_url('forum_topic', ['newsid' => $post['topic_id']]);
        // DÜZELTME: htmlspecialchars kaldırıldı, çift encode önlendi
        $raw_complaint_text = "[FORUM] Mesaj ID: #$post_id | Konu: {$post['title']} | Link: $raw_topic_link | Neden: $reason";
        $complaint_text = $this->db->safesql($raw_complaint_text);

        $this->db->query(
            "INSERT INTO " . PREFIX . "_complaint (news_id, `from`, text, date)
             VALUES ('0', '$reporter', '$complaint_text', NOW())"
        );

        return ['success' => true, 'msg' => fl('forum_ok_complaint')];
    }

    public function getAttachments($post_id) {
        $post_id = intval($post_id);
        return $this->db->super_query(
            "SELECT * FROM " . PREFIX . "_forum_attachments WHERE post_id = '$post_id' ORDER BY id ASC",
            true
        );
    }

    public function getTopicAttachments($topic_id) {
        $topic_id = intval($topic_id);
        return $this->db->super_query(
            "SELECT a.*, p.date as post_date FROM " . PREFIX . "_forum_attachments a
             LEFT JOIN " . PREFIX . "_forum_posts p ON p.id = a.post_id
             WHERE a.topic_id = '$topic_id' ORDER BY a.id ASC",
            true
        );
    }

    public function saveAttachment($post_id, $topic_id, $filename, $original_name, $filesize, $filetype, $file_path = '') {
        $post_id       = intval($post_id);
        $topic_id      = intval($topic_id);
        $user_id       = $this->getUserId();
        $filename      = $this->db->safesql($filename);
        $original_name = $this->db->safesql($original_name);
        $filesize      = intval($filesize);
        $filetype      = $this->db->safesql(strtolower($filetype));
        $is_image      = in_array($filetype, ['jpg','jpeg','png','gif','webp']) ? 1 : 0;

        // MIME tipi doğrulama
        $mime_type = '';
        if($file_path && file_exists($file_path)) {
            if(function_exists('finfo_open')) {
                $finfo     = finfo_open(FILEINFO_MIME_TYPE);
                $mime_type = finfo_file($finfo, $file_path);
                finfo_close($finfo);
            }
            // MIME ile uzantı uyuşuyor mu kontrol et
            $allowed_mimes = [
                'jpg'  => ['image/jpeg'],
                'jpeg' => ['image/jpeg'],
                'png'  => ['image/png'],
                'gif'  => ['image/gif'],
                'webp' => ['image/webp'],
                'pdf'  => ['application/pdf'],
                'zip'  => ['application/zip','application/x-zip-compressed'],
                'rar'  => ['application/x-rar-compressed','application/vnd.rar'],
                'doc'  => ['application/msword'],
                'docx' => ['application/vnd.openxmlformats-officedocument.wordprocessingml.document'],
            ];
            if(isset($allowed_mimes[$filetype]) && $mime_type) {
                if(!in_array($mime_type, $allowed_mimes[$filetype])) {
                    return ['error' => 'Dosya türü doğrulanamadı!'];
                }
            }
            // SHA256 hash - duplicate tespiti
            $file_hash = hash_file('sha256', $file_path);
        } else {
            $file_hash = '';
        }

        $mime_safe = $this->db->safesql($mime_type);
        $hash_safe = $this->db->safesql($file_hash);

        $this->db->query(
            "INSERT INTO " . PREFIX . "_forum_attachments (post_id, topic_id, user_id, filename, original_name, filesize, filetype, mime_type, file_hash, is_image, date)
             VALUES ('$post_id', '$topic_id', '$user_id', '$filename', '$original_name', '$filesize', '$filetype', '$mime_safe', '$hash_safe', '$is_image', NOW())"
        );
        return $this->db->insert_id();
    }

    public function deleteAttachment($attach_id) {
        $attach_id = intval($attach_id);
        $attach    = $this->db->super_query("SELECT * FROM " . PREFIX . "_forum_attachments WHERE id = '$attach_id'");
        if(!$attach) return false;

        $user_id = $this->getUserId();
        $own     = $attach['user_id'] == $user_id;
        if(!$own && !$this->canDo('moderate')) return false;

        $filepath = ROOT_DIR . '/uploads/forum/' . $attach['filename'];
        if(file_exists($filepath)) @unlink($filepath);

        $this->db->query("DELETE FROM " . PREFIX . "_forum_attachments WHERE id = '$attach_id'");
        return true;
    }

    public function incrementDownload($attach_id) {
        $attach_id = intval($attach_id);
        $this->db->query("UPDATE " . PREFIX . "_forum_attachments SET downloads = downloads + 1 WHERE id = '$attach_id'");
    }

    public function getStats() {
        $cached = dle_cache("forum_stats", $this->config['skin']);
        if($cached) return unserialize($cached);
        $stats = [
            'total_topics'     => intval($this->db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_topics WHERE is_deleted='0'")['c']),
            'total_posts'      => intval($this->db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_posts WHERE is_deleted='0'")['c']),
            'total_users'      => intval($this->db->super_query("SELECT COUNT(*) as c FROM " . USERPREFIX . "_users WHERE user_id > 0")['c']),
            'forum_members'    => intval($this->db->super_query("SELECT COUNT(*) as c FROM " . USERPREFIX . "_users WHERE forum_post_count > 0")['c']),
            'total_cats'       => intval($this->db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_cats")['c']),
            'total_likes'      => intval($this->db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_likes")['c']),
            'total_complaints' => intval($this->db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_complaint WHERE text LIKE '[FORUM]%'")['c']),
            // Son Uye: avatar + profil linki icin ek alanlar
            'last_user'        => $this->db->super_query("SELECT user_id, name, foto FROM " . USERPREFIX . "_users ORDER BY user_id DESC LIMIT 1"),
            // Son Mesaj: SEO URL icin alt_name + cat_slug eklendi
            'last_post'        => $this->db->super_query(
                "SELECT p.id, p.date, u.name as username, u.foto as user_avatar,
                 t.title as topic_title, t.id as topic_id, t.alt_name as topic_alt,
                 c.alt_name as cat_slug
                 FROM " . PREFIX . "_forum_posts p
                 LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = p.user_id
                 LEFT JOIN " . PREFIX . "_forum_topics t ON t.id = p.topic_id
                 LEFT JOIN " . PREFIX . "_forum_cats c ON c.id = t.cat_id
                 WHERE p.is_deleted = '0' ORDER BY p.date DESC LIMIT 1"
            ),
        ];
        create_cache("forum_stats", serialize($stats), $this->config['skin']);
        return $stats;
    }
    
    public function search($query, $page = 1, $per_page = 20) {
        $q_raw  = trim($query);
        if(mb_strlen($q_raw) < 3) return ['error' => fl('forum_err_search_min')];
        // DÜZELTME: LIKE joker karakterleri escape edilmeli
        $q      = $this->db->safesql(str_replace(['%', '_'], ['\%', '\_'], $q_raw)); 
        $offset = (max(1, intval($page)) - 1) * intval($per_page);
        $results = $this->db->super_query(
            "SELECT t.id, t.title, t.alt_name, t.date, t.replies, t.views, u.name as username
             FROM " . PREFIX . "_forum_topics t
             LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = t.user_id
             WHERE (t.title LIKE '%$q%') AND t.is_deleted = '0'
             ORDER BY t.date DESC
             LIMIT $offset, $per_page",
            true
        );
        return ['results' => $results];
    }

    public function sendPm($to_user, $title, $text) {
        if(!$this->isLogged()) return ['error' => fl('forum_err_must_login')];
        
        global $member_id, $dle_api;
        
        $from_user = $this->getUserId();
        $to_user   = intval($to_user);
        $title     = trim($title);
        $text      = trim($text);
        
        // Validasyon
        if($to_user <= 0 || $to_user == $from_user) return ['error' => fl('forum_err_pm_self')];
        if(mb_strlen($title) < 2) return ['error' => fl('forum_err_pm_title')];
        if(mb_strlen($text) < 5)  return ['error' => fl('forum_err_pm_text')];
        
        // DLE API yüklü değilse yükle (VFS Uyumlu)
        if(!isset($dle_api)) {
            include_once (DLEPlugins::Check(ENGINE_DIR . '/api/api.class.php'));
        }
        
        // Mesajı DLE'nin native conversations sistemine gönder
        // (forum_pm tablosu artık kullanılmıyor - v19+ uyumlu)
        $result = $dle_api->send_pm_to_user($to_user, $title, $text, $from_user);
        
        return $result ? ['success' => true] : ['error' => 'PM gönderilemedi'];
    }

    public function getPmInbox($user_id = null, $page = 1, $per_page = 20) {
        // ⚠️ LEGACY: forum_pm tablosu artık kullanılmıyor
        // DLE'nin kendi conversations sistemine yönlendir
        // Bu metod sadece backward compatibility için kalıyor
        return [];
    }

    public function deletePm($pm_id, $by = 'receiver') {
        // ⚠️ LEGACY: forum_pm tablosu artık kullanılmıyor
        // DLE'nin kendi conversations sistemini kullan
        // Eski kod kaldırıldı - native integration
        return true;
    }

    public function handleUpload($post_id, $topic_id) {
        if(empty($_FILES['forum_file']['name'][0])) return ['error' => fl('forum_err_upload_none')];
        if(!intval($this->getSetting('allow_attachments'))) return ['error' => fl('forum_err_upload_disabled')];
        $max_size    = intval($this->getSetting('max_attachment_size') ?: 5242880);
        $allowed_raw = $this->getSetting('allowed_filetypes') ?: 'jpg,jpeg,png,gif,webp,pdf,zip,rar';
        $allowed_ext = array_map('trim', explode(',', strtolower($allowed_raw)));
        $image_types = ['jpg','jpeg','png','gif','webp'];
        $upload_dir  = ENGINE_DIR . '/../uploads/forum/';
        $upload_url  = $this->config['http_home_url'] . 'uploads/forum/';
        
        if(!is_dir($upload_dir)) {
            mkdir($upload_dir, 0755, true);
            $htaccess_content = "<FilesMatch \"\.(php|phtml|php3|php4|php5|php7|cgi|pl|py|jsp|asp|sh)$\">\nOrder Allow,Deny\nDeny from all\n</FilesMatch>\nOptions -Indexes";
            @file_put_contents($upload_dir . '.htaccess', $htaccess_content);
        }
        
        $results = [];
        $files   = $_FILES['forum_file'];
        $count   = is_array($files['name']) ? count($files['name']) : 1;
        for($i = 0; $i < $count; $i++) {
            $tmp  = is_array($files['tmp_name']) ? $files['tmp_name'][$i] : $files['tmp_name'];
            $name = is_array($files['name'])     ? $files['name'][$i]     : $files['name'];
            $size = is_array($files['size'])     ? $files['size'][$i]     : $files['size'];
            $err  = is_array($files['error'])    ? $files['error'][$i]    : $files['error'];
            
            if($err !== UPLOAD_ERR_OK)  { $results[] = ['error' => sprintf(fl('forum_err_upload_error'), $name)]; continue; }
            if($size > $max_size)       { $results[] = ['error' => sprintf(fl('forum_err_upload_size'), $name, round($max_size/1024/1024,1))]; continue; }
            
            $ext = strtolower(pathinfo($name, PATHINFO_EXTENSION));
            if(!in_array($ext, $allowed_ext)) { $results[] = ['error' => sprintf(fl('forum_err_upload_ext'), $name)]; continue; }

            // MIME type doğrulaması — sadece uzantıya güvenmek yetmez
            if(function_exists('finfo_open')) {
                $finfo     = finfo_open(FILEINFO_MIME_TYPE);
                $real_mime = finfo_file($finfo, $tmp);
                finfo_close($finfo);
                $allowed_mimes = [
                    'jpg'  => ['image/jpeg'],
                    'jpeg' => ['image/jpeg'],
                    'png'  => ['image/png'],
                    'gif'  => ['image/gif'],
                    'webp' => ['image/webp'],
                    'pdf'  => ['application/pdf'],
                    'zip'  => ['application/zip', 'application/x-zip-compressed'],
                    'rar'  => ['application/x-rar-compressed', 'application/vnd.rar'],
                    'doc'  => ['application/msword'],
                    'docx' => ['application/vnd.openxmlformats-officedocument.wordprocessingml.document'],
                ];
                if(isset($allowed_mimes[$ext]) && !in_array($real_mime, $allowed_mimes[$ext])) {
                    $results[] = ['error' => sprintf(fl('forum_err_upload_ext'), $name)];
                    continue;
                }
            }
            
            $safe_name = time() . '_' . $this->getUserId() . '_' . bin2hex(random_bytes(4)) . '.' . $ext;
            $dest      = $upload_dir . $safe_name;
            $is_image  = in_array($ext, $image_types);
            
            if($is_image && function_exists('imagecreatefromstring')) {
                $src = @imagecreatefromstring(file_get_contents($tmp));
                if($src) {
                    $ow = imagesx($src); $oh = imagesy($src);
                    if($ow > 1920 || $oh > 1920) {
                        $r = min(1920/$ow, 1920/$oh);
                        $nw = intval($ow*$r); $nh = intval($oh*$r);
                        $ni = imagecreatetruecolor($nw, $nh);
                        if($ext === 'png') { imagealphablending($ni, false); imagesavealpha($ni, true); }
                        imagecopyresampled($ni, $src, 0,0,0,0,$nw,$nh,$ow,$oh);
                        imagedestroy($src); $src = $ni;
                    }
                    if(function_exists('imagewebp')) {
                        $safe_name = substr($safe_name, 0, strrpos($safe_name, '.')) . '.webp';
                        $dest = $upload_dir . $safe_name;
                        imagewebp($src, $dest, 82);
                    } else { 
                        imagejpeg($src, $dest, 85); 
                    }
                    imagedestroy($src);
                    $size = filesize($dest);
                } else { move_uploaded_file($tmp, $dest); }
            } else { move_uploaded_file($tmp, $dest); }
            
            $fe  = strtolower(pathinfo($safe_name, PATHINFO_EXTENSION));
            $att = $this->saveAttachment($post_id, $topic_id, $safe_name, $name, $size, $fe);
            $results[] = ['success' => true, 'id' => $att, 'url' => $upload_url . $safe_name, 'name' => $name, 'is_image' => in_array($fe, $image_types) ? 1 : 0];
        }
        return $results;
    }

    public function parseMentions($raw_text, $topic_id, $post_id) {
        preg_match_all('/@([a-zA-Z0-9_\-\.]{2,30})/', $raw_text, $m);
        if(empty($m[1])) return;
        foreach(array_unique($m[1]) as $uname) {
            $us = $this->db->safesql($uname);
            $u  = $this->db->super_query("SELECT user_id FROM " . USERPREFIX . "_users WHERE name='$us'");
            if($u) $this->sendNotif(intval($u['user_id']), 'mention', $topic_id, $post_id);
        }
    }

    public function bumpTopic($topic_id) {
        if(!$this->isLogged()) return ['error' => fl('forum_err_must_login')];
        $topic_id = intval($topic_id);
        $topic    = $this->getTopicById($topic_id);
        if(!$topic)             return ['error' => fl('forum_err_topic_not_found')];
        if($topic['is_locked']) return ['error' => fl('forum_err_bump_locked')];
        $uid = $this->getUserId();
        if($topic['user_id'] != $uid && !$this->canDo('moderate')) return ['error' => fl('forum_err_bump_owner')];
        
        $row = $this->db->super_query("SELECT last_bump_date FROM " . PREFIX . "_forum_topics WHERE id='$topic_id'");
        if($row && !empty($row['last_bump_date']) && $row['last_bump_date'] != '0000-00-00 00:00:00') {
            $cooldown = intval($this->getSetting('bump_cooldown_hours') ?: 24) * 3600;
            if((time() - strtotime($row['last_bump_date'])) < $cooldown) {
                $kalan = ceil(($cooldown - (time() - strtotime($row['last_bump_date']))) / 3600);
                return ['error' => sprintf(fl('forum_err_bump_cooldown'), $kalan)];
            }
        }
        $this->db->query("UPDATE " . PREFIX . "_forum_topics SET last_post_date=NOW(), last_bump_date=NOW() WHERE id='$topic_id'");
        $this->logAction('bump_topic', $topic_id, 0);
        return ['success' => true, 'message' => fl('forum_ok_bump')];
    }

    public function moveTopic($topic_id, $new_cat_id) {
        if(!$this->canDo('moderate')) return ['error' => fl('forum_err_no_permission')];
        $topic_id   = intval($topic_id);
        $new_cat_id = intval($new_cat_id);
        $topic      = $this->getTopicById($topic_id);
        if(!$topic)                         return ['error' => fl('forum_err_topic_not_found')];
        if(!$this->getCatById($new_cat_id)) return ['error' => fl('forum_err_move_cat')];
        $old = intval($topic['cat_id']);
        $this->db->query("UPDATE " . PREFIX . "_forum_topics SET cat_id='$new_cat_id' WHERE id='$topic_id'");
        $this->updateCatStats($old);
        $this->updateCatStats($new_cat_id);
        $this->logAction('move_topic', $topic_id, 0);
        clear_cache("forum_topics_" . $old);
        clear_cache("forum_topics_" . $new_cat_id);
        return ['success' => true];
    }

    public function needsApproval($user_id) {
        $threshold = intval($this->getSetting('approval_threshold') ?: 0);
        if($threshold <= 0) return false;
        $r = $this->db->super_query("SELECT forum_post_count FROM " . USERPREFIX . "_users WHERE user_id='" . intval($user_id) . "'");
        return (intval($r['forum_post_count'] ?? 0) < $threshold);
    }
    
    public function getPendingPosts($page = 1, $per = 20) {
        $offset = (max(1,$page)-1)*$per;
        return $this->db->super_query("SELECT p.*, u.name as username, t.title as topic_title FROM " . PREFIX . "_forum_posts p LEFT JOIN " . USERPREFIX . "_users u ON u.user_id=p.user_id LEFT JOIN " . PREFIX . "_forum_topics t ON t.id=p.topic_id WHERE p.is_approved='0' AND p.is_deleted='0' ORDER BY p.date DESC LIMIT $offset,$per", true);
    }
    public function countPendingPosts() {
        return intval($this->db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_posts WHERE is_approved='0' AND is_deleted='0'")['c']);
    }
    public function approvePost($post_id) {
        if(!$this->canDo('moderate')) return ['error' => fl('forum_err_no_permission')];
        $post_id = intval($post_id);
        $post = $this->db->super_query("SELECT * FROM " . PREFIX . "_forum_posts WHERE id='$post_id'");
        if(!$post) return ['error' => fl('forum_err_msg_not_found')];
        $this->db->query("UPDATE " . PREFIX . "_forum_posts SET is_approved='1' WHERE id='$post_id'");
        
        $t = $this->getTopicById($post['topic_id']);
        if($t) {
             $this->db->query("UPDATE " . PREFIX . "_forum_topics SET replies = replies + 1, last_post_date=NOW(), last_user_id='{$post['user_id']}', last_post_id='$post_id' WHERE id='{$post['topic_id']}'");
             $this->updateCatStats($t['cat_id']);
        }
        $this->logAction('approve_post', $post['topic_id'], $post_id);
        return ['success' => true];
    }
    public function rejectPost($post_id) {
        if(!$this->canDo('moderate')) return ['error' => fl('forum_err_no_permission')];
        $post_id = intval($post_id);
        $this->db->query("UPDATE " . PREFIX . "_forum_posts SET is_deleted='1',is_approved='0' WHERE id='$post_id'");
        $this->logAction('reject_post', 0, $post_id);
        return ['success' => true];
    }

    public function pagination($total, $per_page, $current, $base_url) {
        if($total <= $per_page) return '';
        $pages = ceil($total / $per_page);
        $safe_base_url = htmlspecialchars($base_url, ENT_QUOTES, 'UTF-8');
        $sep = (strpos($safe_base_url, '?') === false) ? '?' : '&';
        
        $html  = '<ul class="pagination pagination-sm no-margin">';
        if($current > 1) $html .= '<li><a href="' . $safe_base_url . $sep . 'page=' . ($current-1) . '">«</a></li>';
        for($i = 1; $i <= $pages; $i++) {
            $active = $i == $current ? 'class="active"' : '';
            $html  .= "<li $active><a href=\"{$safe_base_url}{$sep}page={$i}\">{$i}</a></li>";
        }
        if($current < $pages) $html .= '<li><a href="' . $safe_base_url . $sep . 'page=' . ($current+1) . '">»</a></li>';
        $html .= '</ul>';
        return $html;
    }
}
?>]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/forum/init.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
/*
=====================================================
 DLE Native Forum (Nexus) - by Dlehub
-----------------------------------------------------
 https://github.com/dlehub
-----------------------------------------------------
 File: engine/forum/init.php (Router & i18n Loader)
=====================================================
*/

if(!defined('DATALIFEENGINE')) die("Hacking attempt!");

// SEO Helper Fonksiyonları
include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/forum.helper.php'));

// --- ZİYARETÇİ OTURUM TAKIBI ---
if(!$is_logged && !isset($_SESSION['forum_visit_start'])) {
    $_SESSION['forum_visit_start'] = time();
}

/**
 * 1. GLOBAL DEĞİŞKENLERİ HAZIRLA
 * DLE içerisindeki ana değişkenleri içeri aktarıyoruz ki 
 * sidebar ve sistem fonksiyonları hatasız çalışsın.
 */
global $db, $config, $member_id, $is_logged, $tpl, $metatags, $lang, $user_group;

/**
 * 2. ÇOK DİLLİ (i18n) SİSTEMİ YÜKLE
 * DLE'nin o anki seçili diline göre (TR/EN/RU) dosyayı çeker.
 */
$selected_lang = (isset($config['langs']) && $config['langs'] != "") ? $config['langs'] : 'Turkish';
$_forum_lng_path = ROOT_DIR . '/language/' . $selected_lang . '/forum.lng.php';

// Sidebar bozulmaması için dosyayı yüklemeden önce global $lang diyoruz
if (file_exists($_forum_lng_path)) {
    include (DLEPlugins::Check($_forum_lng_path));
} else {
    // Fallback: Eğer seçili dilde dosya yoksa ana dile (Türkçe) dön
    $_forum_fallback = ROOT_DIR . '/language/Turkish/forum.lng.php';
    if (file_exists($_forum_fallback)) include (DLEPlugins::Check($_forum_fallback));
}

// $lang dizisinin varlığını garanti altına al
if (!isset($lang) || !is_array($lang)) $lang = array();

/**
 * 3. fl() HELPER FONKSİYONU (Eksiksiz Sürüm)
 * Dil anahtarlarını çekmek ve sprintf ile dinamik veri basmak için.
 */
if (!function_exists('fl')) {
    function fl($key, ...$args) {
        global $lang;
        $val = (isset($lang[$key]) && $lang[$key] !== '') ? $lang[$key] : '';
        if ($val === '' && count($args) === 1 && is_string($args[0]) && $args[0] !== '') {
            return $args[0];
        }
        return ($val !== '' && count($args) > 0) ? sprintf($val, ...$args) : ($val ?: $key);
    }
}

/**
 * 4. ÇEKİRDEK SINIF VE BAŞLATICI
 */
if(!class_exists('Forum')) {
    include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/classes/forum.class.php'));
}
$Forum = new Forum($db, $member_id, $config);

/**
 * 5. ROUTER (AKSIYON YÖNETİMİ)
 * PHP 8+ uyumlu, tüm case'leri kapsayan yapı.
 */
$raw_action = (isset($_GET['action']) && is_string($_GET['action'])) ? $_GET['action'] : 
              ((isset($_POST['action']) && is_string($_POST['action'])) ? $_POST['action'] : 'main');

// Güvenlik: Sadece harf, rakam ve alt çizgi (LFI/RFI koruması)
$action = preg_replace('/[^a-z0-9_]/', '', strtolower($raw_action));

switch ($action) {
    
    // Kategori ve Konu Listesi
    case 'category':
        include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/actions/category.php'));
        break;

    // Konu Detayı ve Mesajlar
    case 'topic':
        include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/actions/topic.php'));
        break;

    // Yeni Konu Açma Formu/İşlemi
    case 'new_topic':
        include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/actions/new_topic.php'));
        break;

    // Forum İçi Arama
    case 'search':
        include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/actions/search.php'));
        break;

    // Özel Mesajlar (Conversations)
    case 'pm':
        include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/actions/pm.php'));
        break;

    // Kullanıcı Bildirimleri
    case 'notifications':
        include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/actions/notifications.php'));
        break;

    // Kullanıcı Forum Mesajları
    case 'user_posts':
        include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/actions/user_posts.php'));
        break;

    // Yanıtlanmamış Konular
    case 'unanswered':
        include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/actions/unanswered.php'));
        break;

    // Aktif Konular
    case 'active':
        include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/actions/active.php'));
        break;

    // Etiket Sayfası
    case 'tag':
        include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/actions/tag.php'));
        break;

    // Beğeni, Bump, Takip, Silme gibi tüm AJAX işlemleri
    case 'ajax':
        include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/actions/ajax.php'));
        break;

    // Ana Sayfa (Kategori Listesi)
    case 'main':
    default:
        include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/actions/main.php'));
        break;
}
?>]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/forum/actions/ajax.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
/*
=====================================================
 DLE Native Forum (Nexus) - by Dlehub
-----------------------------------------------------
 https://github.com/dlehub
-----------------------------------------------------
 Copyright (c) 2024-2026 Dlehub & Elegance
=====================================================
 This code is protected by copyright
=====================================================
 File: engine/forum/actions/ajax.php
-----------------------------------------------------
 Use: AJAX isteklerini (beğeni, takip, bildirim, konu aksiyonları) işleme
=====================================================
*/
if(!defined('DATALIFEENGINE')) die("Hacking attempt!");

function jout($data) {
    if (ob_get_length()) ob_clean(); // Buffer temizle: önceki uyarı/boşluk JSON'ı bozmasın
    header('Content-Type: application/json; charset=utf-8');
    echo json_encode($data, JSON_UNESCAPED_UNICODE | JSON_PARTIAL_OUTPUT_ON_ERROR);
    exit;
}
if(!$is_logged) jout(['error' => fl('forum_err_must_login')]);
 $token = isset($_POST['csrf']) ? $_POST['csrf'] : '';
 $token = $token ?: (isset($_REQUEST['user_hash']) ? $_REQUEST['user_hash'] : '');
 $act   = isset($_POST['act']) ? $_POST['act'] : '';

if(!$Forum->verifyCsrf($token)) jout(['error' => 'Güvenlik doğrulaması başarısız!']);

switch($act) {

    case 'like':
        $post_id = intval($_POST['post_id'] ?? 0);
        $ltype   = (isset($_POST['ltype']) && $_POST['ltype'] === 'dislike') ? 'dislike' : 'like';
        jout($Forum->toggleLike($post_id, $ltype));
        break;

    case 'follow':
        $topic_id = intval($_POST['topic_id'] ?? 0);
        jout($Forum->toggleFollow($topic_id));
        break;

    case 'mark_read':
        $topic_id = intval($_POST['topic_id'] ?? 0);
        $post_id  = intval($_POST['post_id']  ?? 0);
        $Forum->markRead($topic_id, $post_id);
        jout(['success' => true]);
        break;

    case 'notif_count':
        jout(['count' => $Forum->countUnreadNotifs()]);
        break;

    case 'notifs':
        $Forum->markNotifsRead();
        $notifs = $Forum->getNotifs($Forum->getUserId(), 15);
        jout(['notifs' => $notifs]);
        break;

    case 'edit_post':
        $post_id  = intval($_POST['post_id']  ?? 0);
        $new_text = trim($_POST['text'] ?? '');
        jout($Forum->editPost($post_id, $new_text));
        break;

    case 'delete_post':
        $post_id = intval($_POST['post_id'] ?? 0);
        jout($Forum->deletePost($post_id));
        break;

    case 'delete_topic':
        if(!$Forum->canDo('delete_any')) jout(['error' => fl('forum_err_no_permission')]);
        $topic_id = intval($_POST['topic_id'] ?? 0);
        jout(['success' => $Forum->deleteTopic($topic_id)]);
        break;

    case 'lock_topic':
        $topic_id = intval($_POST['topic_id'] ?? 0);
        $state    = intval($_POST['state']    ?? 0);
        jout($Forum->lockTopic($topic_id, $state));
        break;

    case 'pin_topic':
        $topic_id = intval($_POST['topic_id'] ?? 0);
        $state    = intval($_POST['state']    ?? 0);
        jout($Forum->pinTopic($topic_id, $state));
        break;

    case 'send_pm':
        // DÜZELTME: PHP 8 dizi gönderimini engellemek için is_string() eklendi
        $to_name = (isset($_POST['to_user']) && is_string($_POST['to_user'])) ? trim($_POST['to_user']) : '';
        
        if(empty($to_name)) {
            jout(['error' => fl('forum_err_pm_invalid')]);
        }
        
        $to_row  = $db->super_query("SELECT user_id FROM " . USERPREFIX . "_users WHERE name='" . $db->safesql($to_name) . "'");
        
        if(!$to_row) {
            jout(['error' => fl('forum_err_pm_notfound')]);
        }
        
        $title = trim($_POST['title'] ?? '');
        $text  = trim($_POST['text']  ?? '');
        
        jout($Forum->sendPm($to_row['user_id'], $title, $text));
        break;

    case 'delete_pm':
        $pm_id = intval($_POST['pm_id'] ?? 0);
        // DÜZELTME: Sadece 'sender' veya 'receiver' kabul edilmeli, diğer türlü veriler reddedilmeli
        $raw_by = (isset($_POST['by']) && is_string($_POST['by'])) ? strtolower(trim($_POST['by'])) : 'receiver';
        $by = in_array($raw_by, ['sender', 'receiver']) ? $raw_by : 'receiver';
        
        jout($Forum->deletePm($pm_id, $by));
        break;

    case 'new_topic':
        $cat_id = intval($_POST['cat_id'] ?? 0);
        $title  = trim($_POST['title']    ?? '');
        $text   = trim($_POST['text']     ?? '');
        jout($Forum->createTopic($cat_id, $title, $text));
        break;

    case 'delete_attachment':
        $attach_id = intval($_POST['attach_id'] ?? 0);
        jout($Forum->deleteAttachment($attach_id));
        break;

    case 'download_attachment':
        $attach_id = intval($_POST['attach_id'] ?? 0);
        $Forum->incrementDownload($attach_id);
        jout(['success' => true]);
        break;

    case 'bump_topic':
        $topic_id = intval($_POST['topic_id'] ?? 0);
        jout($Forum->bumpTopic($topic_id));
        break;

    case 'move_topic':
        if(!$Forum->canDo('moderate')) jout(['error' => fl('forum_err_no_permission')]);
        jout($Forum->moveTopic(intval($_POST['topic_id'] ?? 0), intval($_POST['new_cat_id'] ?? 0)));
        break;

    case 'upload':
        jout($Forum->handleUpload(intval($_POST['post_id'] ?? 0), intval($_POST['topic_id'] ?? 0)));
        break;

    case 'approve_post':
        jout($Forum->approvePost(intval($_POST['post_id'] ?? 0)));
        break;

    case 'reject_post':
        jout($Forum->rejectPost(intval($_POST['post_id'] ?? 0)));
        break;

    default:
        jout(['error' => fl('forum_err_invalid_action')]);
}
?>]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/inc/forum.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
/*
=====================================================
 DLE Native Forum (Nexus) - by Dlehub
-----------------------------------------------------
 https://github.com/dlehub
-----------------------------------------------------
 Copyright (c) 2024-2026 Dlehub & Elegance
=====================================================
 This code is protected by copyright
=====================================================
 File: engine/inc/forum.php
-----------------------------------------------------
 Use: Forum Yönetim Paneli (Admin Panel) - Kategori, Rütbe, Ayar ve Log Yönetimi
=====================================================
*/
if(!defined('DATALIFEENGINE') || !defined('LOGGED_IN')) die("Hacking attempt!");

// Grup 1 (Admin) ve Grup 2 (Editör) erişimi
if(!in_array($member_id['user_group'], [1, 2])) msg("error", $lang['addnews_denied'], $lang['db_denied']);

// SEO Fonksiyonlarını Admin Panel İçin de Yükle
if (!function_exists('forum_url')) {
    include_once(DLEPlugins::Check(ENGINE_DIR . '/forum/forum.helper.php'));
}

if(!class_exists('Forum')) {
    include_once(DLEPlugins::Check(ENGINE_DIR . '/forum/classes/forum.class.php'));
}
$Forum = new Forum($db, $member_id, $config);

// ─── ADMIN DİL DOSYASI YÜKLE ───
$forum_admin_lang = isset($selected_language) ? $selected_language : $config['langs'];
$_forum_lang_file = DLEPlugins::Check(ROOT_DIR . '/language/' . $forum_admin_lang . '/forum.lng.php');
if (file_exists($_forum_lang_file)) {
    include_once($_forum_lang_file);
} else {
    $_forum_lang_fallback = DLEPlugins::Check(ROOT_DIR . '/language/Turkish/forum.lng.php');
    if (file_exists($_forum_lang_fallback)) include_once($_forum_lang_fallback);
}

// $lang dizisinin varlığını garanti altına al
if (!isset($lang) || !is_array($lang)) {
    $lang = array();
}

if (!function_exists('fl')) {
    function fl($key, ...$args) {
        global $lang;
        $val = (isset($lang[$key]) && $lang[$key] !== '') ? $lang[$key] : '';
        if ($val === '' && count($args) === 1 && is_string($args[0]) && $args[0] !== '') {
            return $args[0];
        }
        return ($val !== '' && count($args) > 0) ? sprintf($val, ...$args) : ($val ?: $key);
    }
}
$current_version = '1.1.3';

// ─── GÜNCELLEME KONTROLÜ (GitHub) ───
$update_data = dle_cache('forum_update_info', 86400); 
if (!$update_data) {
    $remote_url = 'https://raw.githubusercontent.com/dle-hub/dle-native-forum/master/release.json';
    
    // Güvenli Remote JSON Fetch (Curl + SSL Verify + Timeout)
    $remote_json = false;
    if(function_exists('curl_init')) {
        $ch = curl_init($remote_url);
        curl_setopt_array($ch, [
            CURLOPT_RETURNTRANSFER => true,
            CURLOPT_TIMEOUT        => 5,
            CURLOPT_CONNECTTIMEOUT => 3,
            CURLOPT_SSL_VERIFYPEER => true,
            CURLOPT_USERAGENT      => 'DLE-NativeForum/'.$current_version
        ]);
        $remote_json = curl_exec($ch);
        curl_close($ch);
    }
    
    if ($remote_json) {
        $data = json_decode($remote_json, true);
        if ($data && isset($data['version'])) {
            $update_data = $data['version'];
            create_cache('forum_update_info', $update_data, 86400);
        }
    }
}

$version_tag = '';
if ($update_data) {
    if (version_compare($update_data, $current_version, '>')) {
        $version_tag = '<span style="display:inline-block; padding:2px 8px; background:#ef4444; color:#fff; border-radius:4px; font-size:10px; margin-left:10px; vertical-align:middle; font-weight:700;">' . fl('forum_adm_ver_new', $update_data) . '</span>';
    } else {
        $version_tag = '<span style="display:inline-block; padding:2px 8px; background:#10b981; color:#fff; border-radius:4px; font-size:10px; margin-left:10px; vertical-align:middle; font-weight:700;">' . fl('forum_adm_ver_ok') . '</span>';
    }
}

// ----------------------------------------------------------------
// HELPER: Güvenli HTML çıktısı
// ----------------------------------------------------------------
function safe_out($str) {
    return htmlspecialchars((string)$str, ENT_QUOTES, 'UTF-8');
}

// HELPER: Sayfalandırma
function renderPagination($total, $per_page, $current_page, $base_url) {
    $pages = ceil($total / $per_page);
    if($pages <= 1) return '';
    $html = '<div class="panel-footer"><ul class="pagination pagination-sm no-margin">';
    for($i = 1; $i <= $pages; $i++) {
        $active = ($i == $current_page) ? 'class="active"' : '';
        $html .= "<li $active><a href='{$base_url}&page=$i'>$i</a></li>";
    }
    $html .= '</ul></div>';
    return $html;
}

// ----------------------------------------------------------------
// POST İŞLEMLERİ
// ----------------------------------------------------------------
if(isset($_POST['save_action'])) {
    // GÜVENLİK: Strict CSRF kontrolü
    if(!isset($_POST['user_hash']) || !hash_equals((string)$dle_login_hash, (string)$_POST['user_hash'])) {
        die("Hacking attempt!");
    }

    $save = $_POST['save_action'];

    // ---- KATEGORİ KAYDET ----
    if($save == 'save_cat') {
        $cat_id = intval($_POST['cat_id']);
        $name   = $db->safesql(trim($_POST['name']));
        $alt    = $db->safesql(totranslit(trim($_POST['name']), true, false));
        $desc   = $db->safesql(trim($_POST['description']));
        $icon   = $db->safesql(trim($_POST['icon']));
        $posi   = intval($_POST['posi']);
        $pid    = intval($_POST['parent_id']);

        if($cat_id > 0) {
            $db->query("UPDATE " . PREFIX . "_forum_cats SET name='$name', alt_name='$alt', description='$desc', icon='$icon', posi='$posi', parent_id='$pid' WHERE id='$cat_id'");
            clear_cache("forum_cats_0");
            clear_cache("forum_cats_" . $cat_id);
            msg("info", fl('forum_success'), fl('forum_adm_cat_saved'), "?mod=forum&action=cats");
        } else {
            $exists = $db->super_query("SELECT id FROM " . PREFIX . "_forum_cats WHERE alt_name='$alt'");
            if($exists) $alt .= '-' . time();
            $db->query("INSERT INTO " . PREFIX . "_forum_cats (parent_id,name,alt_name,description,icon,posi) VALUES ('$pid','$name','$alt','$desc','$icon','$posi')");
            clear_cache("forum_cats_0");
            msg("info", fl('forum_success'), fl('forum_adm_cat_added'), "?mod=forum&action=cats");
        }
    }

    // ---- KATEGORİ SİL ----
    if($save == 'delete_cat') {
        $cat_id = intval($_POST['cat_id']);

        $subcats = $db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_cats WHERE parent_id='$cat_id'");
        if(intval($subcats['c']) > 0) {
            msg("error", fl('forum_error'), fl('forum_adm_cat_del_subs'), "?mod=forum&action=cats");
        } else {
            $topics = $db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_topics WHERE cat_id='$cat_id'");
            if(intval($topics['c']) > 0) {
                msg("error", fl('forum_error'), fl('forum_adm_cat_del_topics'), "?mod=forum&action=cats");
            } else {
                $db->query("DELETE FROM " . PREFIX . "_forum_cats WHERE id='$cat_id'");
                clear_cache("forum_cats_0");
                clear_cache("forum_cats_" . $cat_id);
                msg("info", fl('forum_success'), fl('forum_adm_cat_del'), "?mod=forum&action=cats");
            }
        }
    }

    // ---- SIRALAMA KAYDET ----
    if($save == 'save_cat_order') {
        if(isset($_POST['cat_order']) && is_array($_POST['cat_order'])) {
            foreach($_POST['cat_order'] as $cid => $cpos) {
                $cid = intval($cid);
                $cpos = intval($cpos);
                $db->query("UPDATE " . PREFIX . "_forum_cats SET posi='$cpos' WHERE id='$cid'");
            }
            clear_cache("forum_cats_0");
            msg("info", fl('forum_success'), fl('forum_order_ok'), "?mod=forum&action=cats");
        }
    }

    // ---- KONU DÜZENLE (YENİ EKLENDİ VE DLE EDİTÖRÜ UYUMLU YAPILDI) ----
    if($save == 'save_edit_topic') {
        $tid = intval($_POST['topic_id']);
        $pid = intval($_POST['post_id']);
        $title = $db->safesql(htmlspecialchars(strip_tags(trim($_POST['title'])), ENT_QUOTES, 'UTF-8'));
        $cat_id = intval($_POST['cat_id']);
        $is_pinned = intval($_POST['is_pinned'] ?? 0);
        $is_locked = intval($_POST['is_locked'] ?? 0);
        
        // v1.1.3 Yeni Alanlar
        $prefix_id = intval($_POST['prefix_id'] ?? 0);
        $description = $db->safesql(mb_substr(trim($_POST['topic_description'] ?? ''), 0, 500));
        $tags = trim($_POST['tags'] ?? '');
        
        // DLE editörü 'short_story' değişkenini post eder
        $text = isset($_POST['short_story']) ? $db->safesql(trim($_POST['short_story'])) : '';
        
        if($tid > 0 && $title != '') {
            $db->query("UPDATE " . PREFIX . "_forum_topics SET title='$title', cat_id='$cat_id', is_pinned='$is_pinned', is_locked='$is_locked', prefix_id='$prefix_id', description='$description' WHERE id='$tid'");
            
            // Etiketleri senkronize et
            $Forum->setTopicTags($tid, $tags);
            
            if($pid > 0 && $text != '') {
                $db->query("UPDATE " . PREFIX . "_forum_posts SET text='$text' WHERE id='$pid'");
                // İlk mesaj düzenlendi — Sticky OP cache'ini temizle
                clear_cache('forum_sticky_' . $tid);
            }
            
            clear_cache("forum");
            msg("info", fl('forum_success'), fl('forum_adm_topic_updated'), "?mod=forum&action=topics");
        } else {
            msg("error", fl('forum_error'), fl('forum_adm_topic_title_empty'), "javascript:history.go(-1)");
        }
    }

    // ---- RÜTBE KAYDET ----
    if($save == 'save_rank') {
        $rank_id    = intval($_POST['rank_id']);
        $title      = $db->safesql(trim($_POST['title']));
        $points     = intval($_POST['points']);
        $color      = $db->safesql(trim($_POST['color']));
        $icon       = $db->safesql(trim($_POST['icon']));
        $badge_text = $db->safesql(trim($_POST['badge_text']));

        if($rank_id > 0) {
            $db->query("UPDATE " . PREFIX . "_forum_ranks SET title='$title', points='$points', color='$color', icon='$icon', badge_text='$badge_text' WHERE id='$rank_id'");
        } else {
            $db->query("INSERT INTO " . PREFIX . "_forum_ranks (title,points,color,icon,badge_text) VALUES ('$title','$points','$color','$icon','$badge_text')");
        }
        msg("info", fl('forum_success'), fl('forum_adm_rank_saved'), "?mod=forum&action=ranks");
    }

    // ---- RÜTBE SİL ----
    if($save == 'delete_rank') {
        $rank_id = intval($_POST['rank_id']);
        $db->query("DELETE FROM " . PREFIX . "_forum_ranks WHERE id='$rank_id'");
        msg("info", fl('forum_success'), fl('forum_adm_rank_del'), "?mod=forum&action=ranks");
    }

    // ---- AYARLAR KAYDET ----
    if($save == 'save_settings') {
        $settings_list = [
            'posts_per_page','topics_per_page','allow_guests_view','min_post_length',
            'max_post_length','flood_time','allow_attachments','max_attachment_size',
            'points_new_topic','points_new_reply','points_received_like',
            'approval_threshold','bump_cooldown_hours'
        ];
        
        foreach($settings_list as $f) {
            if(isset($_POST[$f])) {
                $val = intval($_POST[$f]);
                $db->query("INSERT INTO " . PREFIX . "_forum_settings (name,value) VALUES ('$f','$val') ON DUPLICATE KEY UPDATE value='$val'");
            }
        }
        
        if (isset($_POST['allowed_filetypes'])) {
            $val = $db->safesql(trim($_POST['allowed_filetypes']));
            $db->query("INSERT INTO " . PREFIX . "_forum_settings (name,value) VALUES ('allowed_filetypes','$val') ON DUPLICATE KEY UPDATE value='$val'");
        }
        
        if (isset($_POST['clean_readlog_days'])) {
            $val = intval($_POST['clean_readlog_days']);
            $db->query("INSERT INTO " . PREFIX . "_forum_settings (name,value) VALUES ('clean_readlog_days','$val') ON DUPLICATE KEY UPDATE value='$val'");
        }

        clear_cache("forum_stats");
        msg("info", fl('forum_success'), fl('forum_adm_settings_saved'), "?mod=forum&action=settings");
    }

    // ---- ONAY/RED ----
    if($save == 'approve_post') {
        $Forum->approvePost(intval($_POST['post_id'] ?? 0));
        msg("info", fl('forum_success'), fl('forum_adm_pending_ok'), "?mod=forum&action=pending");
    }
    if($save == 'reject_post') {
        $Forum->rejectPost(intval($_POST['post_id'] ?? 0));
        msg("info", fl('forum_success'), fl('forum_adm_pending_reject'), "?mod=forum&action=pending");
    }

    // ---- TOPLU MODERASYON ----
    if($save == 'bulk_topics') {
        $bulk_action = isset($_POST['bulk_action']) ? preg_replace('/[^a-z_]/', '', $_POST['bulk_action']) : '';
        $topic_ids   = isset($_POST['topic_ids']) ? array_map('intval', (array)$_POST['topic_ids']) : [];
        $topic_ids   = array_filter($topic_ids);

        if(!empty($topic_ids) && !empty($bulk_action)) {
            foreach($topic_ids as $tid) {
                switch($bulk_action) {
                    case 'delete':
                        $Forum->deleteTopic($tid);
                        break;
                    case 'lock':
                        $db->query("UPDATE " . PREFIX . "_forum_topics SET is_locked='1' WHERE id='$tid'");
                        break;
                    case 'unlock':
                        $db->query("UPDATE " . PREFIX . "_forum_topics SET is_locked='0' WHERE id='$tid'");
                        break;
                    case 'pin':
                        $db->query("UPDATE " . PREFIX . "_forum_topics SET is_pinned='1' WHERE id='$tid'");
                        break;
                    case 'unpin':
                        $db->query("UPDATE " . PREFIX . "_forum_topics SET is_pinned='0' WHERE id='$tid'");
                        break;
                    case 'move':
                        $new_cat = intval($_POST['move_cat_id'] ?? 0);
                        if($new_cat > 0) {
                            $old_cat_row = $db->super_query("SELECT cat_id FROM " . PREFIX . "_forum_topics WHERE id='$tid'");
                            $db->query("UPDATE " . PREFIX . "_forum_topics SET cat_id='$new_cat' WHERE id='$tid'");
                            if($old_cat_row) $Forum->updateCatStats(intval($old_cat_row['cat_id']));
                            $Forum->updateCatStats($new_cat);
                        }
                        break;
                }
            }
            clear_cache("forum_stats");
        }
        msg("info", fl('forum_success'), fl('forum_adm_bulk_done'), "?mod=forum&action=topics");
    }

    // ---- PREFIX KAYDET ----
    if($save == 'save_prefix') {
        $prefix_id = intval($_POST['prefix_id'] ?? 0);
        $cat_id    = intval($_POST['cat_id'] ?? 0);
        $title     = $db->safesql(trim($_POST['title'] ?? ''));
        $color     = $db->safesql(trim($_POST['color'] ?? '#3498db'));
        $bg_color  = $db->safesql(trim($_POST['bg_color'] ?? '#ebf5fb'));
        $posi      = intval($_POST['posi'] ?? 0);

        if($prefix_id > 0) {
            $db->query("UPDATE " . PREFIX . "_forum_prefixes SET cat_id='$cat_id', title='$title', color='$color', bg_color='$bg_color', posi='$posi' WHERE id='$prefix_id'");
            msg("info", fl('forum_success'), fl('forum_adm_prefix_saved'), "?mod=forum&action=prefixes");
        } else {
            $db->query("INSERT INTO " . PREFIX . "_forum_prefixes (cat_id, title, color, bg_color, posi) VALUES ('$cat_id','$title','$color','$bg_color','$posi')");
            msg("info", fl('forum_success'), fl('forum_adm_prefix_added'), "?mod=forum&action=prefixes");
        }
    }

    // ---- PREFIX SİL ----
    if($save == 'delete_prefix') {
        $prefix_id = intval($_POST['prefix_id'] ?? 0);
        $db->query("DELETE FROM " . PREFIX . "_forum_prefixes WHERE id='$prefix_id'");
        msg("info", fl('forum_success'), fl('forum_adm_prefix_del'), "?mod=forum&action=prefixes");
    }

    // ---- ETİKET KAYDET (Düzenle) ----
    if($save == 'save_tag') {
        $tag_id = intval($_POST['tag_id'] ?? 0);
        $name   = $db->safesql(trim($_POST['name'] ?? ''));
        $slug   = $db->safesql(totranslit($name, true, false));
        if($tag_id > 0) {
            $db->query("UPDATE " . PREFIX . "_forum_tags SET name='$name', slug='$slug' WHERE id='$tag_id'");
            msg("info", fl('forum_success'), fl('forum_adm_tag_saved'), "?mod=forum&action=tags");
        }
    }

    // ---- ETİKET SİL ----
    if($save == 'delete_tag') {
        $tag_id = intval($_POST['tag_id'] ?? 0);
        // Etiket kullanımını temizle
        $db->query("DELETE FROM " . PREFIX . "_forum_topic_tags WHERE tag_id='$tag_id'");
        $db->query("DELETE FROM " . PREFIX . "_forum_tags WHERE id='$tag_id'");
        msg("info", fl('forum_success'), fl('forum_adm_tag_del'), "?mod=forum&action=tags");
    }

    // ---- İSTATİSTİK YENİDEN HESAPLA ----
    if($save == 'recalc_stats') {
        $lock_file = ENGINE_DIR . '/cache/forum_recalc.lock';
        if(file_exists($lock_file) && (time() - filemtime($lock_file)) < 300) {
            msg("warning", fl('forum_warning'), fl('forum_adm_recalc_running'), "?mod=forum");
        } else {
            @file_put_contents($lock_file, time());
            @set_time_limit(300);
            @ignore_user_abort(true);

            $cats = $db->super_query("SELECT id FROM " . PREFIX . "_forum_cats", true);
            foreach($cats as $cat) $Forum->updateCatStats($cat['id']);

            $users = $db->super_query("SELECT user_id FROM " . USERPREFIX . "_users WHERE forum_post_count > 0 OR forum_points > 0", true);
            foreach($users as $u) {
                $Forum->updatePostCount($u['user_id']);
                $Forum->updateUserRank($u['user_id']);
            }
            @unlink($lock_file);
            clear_cache("forum_stats");
            msg("info", fl('forum_success'), fl('forum_adm_recalc_done'), "?mod=forum");
        }
    }

    // ---- CACHE TEMİZLE ----
    if($save == 'clear_cache') {
        $caches = ['forum_stats','forum_cats_0','forum_topics'];
        foreach($caches as $c) clear_cache($c);
        $days = intval($db->super_query("SELECT value FROM " . PREFIX . "_forum_settings WHERE name='clean_readlog_days'")['value'] ?? 30);
        $Forum->cleanReadLog($days);
        msg("info", fl('forum_success'), fl('forum_adm_cache_cleared'), "?mod=forum");
    }

    // ---- YASAKLI KELİME KAYDET ----
    if($save == 'save_word') {
        $word_id     = intval($_POST['word_id']);
        $word        = $db->safesql(trim($_POST['word']));
        $replacement = $db->safesql(trim($_POST['replacement']));
        if($word_id > 0) {
            $db->query("UPDATE " . PREFIX . "_forum_banned_words SET word='$word', replacement='$replacement' WHERE id='$word_id'");
        } else {
            $db->query("INSERT INTO " . PREFIX . "_forum_banned_words (word,replacement) VALUES ('$word','$replacement')");
        }
        msg("info", fl('forum_success'), fl('forum_adm_word_saved'), "?mod=forum&action=words");
    }

    // ---- YASAKLI KELİME SİL ----
    if($save == 'delete_word') {
        $word_id = intval($_POST['word_id']);
        $db->query("DELETE FROM " . PREFIX . "_forum_banned_words WHERE id='$word_id'");
        msg("info", fl('forum_success'), fl('forum_adm_word_del'), "?mod=forum&action=words");
    }

    // ---- ŞİKAYET SİL ----
    if($save == 'delete_complaint') {
        $cid = intval($_POST['complaint_id']);
        $db->query("DELETE FROM " . PREFIX . "_complaint WHERE id = '$cid'");
        msg("info", fl('forum_success'), fl('forum_adm_complaint_del'), "?mod=forum&action=complaints");
    }
}

// ----------------------------------------------------------------
// Header + Navbar
// ----------------------------------------------------------------
$action = isset($_GET['action']) ? trim($_GET['action']) : 'main';
$allowed_actions = ['main','cats','ranks','words','complaints','logs','settings','topics','pending','quick_mod','prefixes','tags', 'edit_topic'];
if(!in_array($action, $allowed_actions)) $action = 'main';

// Editör scriptlerini yükle
if ($action == 'edit_topic') {
    if(!isset($js_array)) $js_array = array();
    $js_array[] = "public/editor/tiny_mce/tinymce.min.js";
    $js_array[] = "public/fileuploader/plupload/plupload.full.min.js";
    $js_array[] = "public/fileuploader/plupload/i18n/{$lang['language_code']}.js";
}

echoheader("<i class='fa fa-comments-o'></i> " . fl('forum_admin_title', 'DLE Native Forum'), fl('forum_admin_subtitle', 'Admin Panel'));

$act_main        = ($action == 'main')       ? 'class="active"' : '';
$act_cats        = ($action == 'cats')       ? 'class="active"' : '';
$act_ranks       = ($action == 'ranks')      ? 'class="active"' : '';
$act_logs        = ($action == 'logs')       ? 'class="active"' : '';
$act_set         = ($action == 'settings')   ? 'class="active"' : '';
$act_words       = ($action == 'words')      ? 'class="active"' : '';
$act_complaints  = ($action == 'complaints') ? 'class="active"' : '';
$act_topics      = ($action == 'topics' || $action == 'edit_topic') ? 'class="active"' : '';
$act_pending     = ($action == 'pending')    ? 'class="active"' : '';
$act_prefixes    = ($action == 'prefixes')   ? 'class="active"' : '';
$act_tags        = ($action == 'tags')       ? 'class="active"' : '';

$nav_pc = $Forum->countPendingPosts();
$nav_pending_badge = ($nav_pc > 0) ? '<span class="badge bg-red">'.$nav_pc.'</span>' : '';

$csrf_input = '<input type="hidden" name="user_hash" value="' . $dle_login_hash . '">';

$_n1 = fl('forum_adm_nav_summary');
$_n2 = fl('forum_adm_nav_cats');
$_n3 = fl('forum_adm_nav_ranks');
$_n4 = fl('forum_adm_nav_words');
$_n5 = fl('forum_adm_nav_complaints');
$_n6 = fl('forum_adm_nav_topics');
$_n7 = fl('forum_adm_nav_pending');
$_n8 = fl('forum_adm_nav_logs');
$_n9 = fl('forum_adm_nav_settings');
$_n10 = fl('forum_adm_nav_prefixes'); 
$_n11 = fl('forum_adm_nav_tags');     

echo <<<HTML
<div class="navbar navbar-default navbar-component navbar-xs">
    <ul class="nav navbar-nav visible-xs-block">
        <li class="full-width text-center">
            <a data-toggle="collapse" data-target="#navbar-filter"><i class="fa fa-bars"></i></a>
        </li>
    </ul>
    <div class="navbar-collapse collapse" id="navbar-filter">
        <ul class="nav navbar-nav">
            <li {$act_main}><a href="?mod=forum"><i class="fa fa-dashboard position-left"></i> {$_n1}</a></li>
            <li {$act_cats}><a href="?mod=forum&action=cats"><i class="fa fa-folder position-left"></i> {$_n2}</a></li>
            <li {$act_ranks}><a href="?mod=forum&action=ranks"><i class="fa fa-star position-left"></i> {$_n3}</a></li>
            <li {$act_prefixes}><a href="?mod=forum&action=prefixes"><i class="fa fa-tag position-left"></i> {$_n10}</a></li>
            <li {$act_tags}><a href="?mod=forum&action=tags"><i class="fa fa-tags position-left"></i> {$_n11}</a></li>
            <li {$act_words}><a href="?mod=forum&action=words"><i class="fa fa-ban position-left"></i> {$_n4}</a></li>
            <li {$act_complaints}><a href="?mod=forum&action=complaints"><i class="fa fa-flag position-left"></i> {$_n5}</a></li>
            <li {$act_topics}><a href="?mod=forum&action=topics"><i class="fa fa-list position-left"></i> {$_n6}</a></li>
            <li {$act_pending}><a href="?mod=forum&action=pending"><i class="fa fa-clock-o position-left"></i> {$_n7} {$nav_pending_badge}</a></li>
            <li {$act_logs}><a href="?mod=forum&action=logs"><i class="fa fa-shield position-left"></i> {$_n8}</a></li>
            <li {$act_set}><a href="?mod=forum&action=settings"><i class="fa fa-cog position-left"></i> {$_n9}</a></li>
        </ul>
    </div>
</div>
HTML;

$_L_SAVE   = fl('forum_save');
$L_CANCEL  = fl('forum_cancel');
$L_ACTION  = fl('forum_action');
$L_CONFIRM = fl('forum_confirm_delete');

// ----------------------------------------------------------------
// Switch: Action
// ----------------------------------------------------------------
switch($action) {

    case 'quick_mod':
        $id   = intval($_GET['id']);
        $type = trim($_GET['type']);
        $hash = trim($_GET['hash']);
        
        if($hash !== (string)$dle_login_hash) die("Hacking attempt!");
        
        if($id > 0) {
            if($type == 'lock') {
                $db->query("UPDATE " . PREFIX . "_forum_topics SET is_locked = 1 - is_locked WHERE id = '$id'");
                $t = $db->super_query("SELECT is_locked FROM " . PREFIX . "_forum_topics WHERE id = '$id'");
                $Forum->logAction($t['is_locked'] ? 'lock_topic' : 'unlock_topic', $id);
            } elseif($type == 'pin') {
                $db->query("UPDATE " . PREFIX . "_forum_topics SET is_pinned = 1 - is_pinned WHERE id = '$id'");
                $t = $db->super_query("SELECT is_pinned FROM " . PREFIX . "_forum_topics WHERE id = '$id'");
                $Forum->logAction($t['is_pinned'] ? 'pin_topic' : 'unpin_topic', $id);
            }
        }
        
        $refer = isset($_SERVER['HTTP_REFERER']) ? $_SERVER['HTTP_REFERER'] : "?mod=forum";
        header("Location: " . $refer);
        die();
    break;

    case 'cats':
        $edit_cat = null;
        if(isset($_GET['edit'])) {
            $edit_cat = $db->super_query("SELECT * FROM " . PREFIX . "_forum_cats WHERE id='" . intval($_GET['edit']) . "'");
        }
        
        $all_cats = $db->super_query("SELECT * FROM " . PREFIX . "_forum_cats ORDER BY parent_id ASC, posi ASC", true);
        if(!is_array($all_cats)) $all_cats = [];
        $total_cats = count($all_cats);

        if(!function_exists('buildCatTree')) {
            function buildCatTree($cats, $parent_id = 0) {
                $tree = [];
                foreach ($cats as $cat) {
                    if ($cat['parent_id'] == $parent_id) {
                        $cat['children'] = buildCatTree($cats, $cat['id']);
                        $tree[] = $cat;
                    }
                }
                return $tree;
            }
        }

        if(!function_exists('renderCatTreeNestable')) {
            function renderCatTreeNestable($tree, $csrf_input, $L_CONFIRM, $hash) {
                $html = '';
                foreach ($tree as $cat) {
                    $has_children = !empty($cat['children']);
                    $safe_icon = preg_match('/^[a-zA-Z0-9\- ]+$/', $cat['icon'] ?? '') ? $cat['icon'] : 'fa-folder';
                    
                    $html .= "<li class=\"dd-item\" data-id=\"{$cat['id']}\">";
                    $html .= "<div class=\"dd-handle\"></div>";
                    $html .= "<div class=\"dd-content\">";
                    
                    $html .= "<b>ID:{$cat['id']}</b> <i class=\"fa {$safe_icon}\" style=\"color:#64748b; margin:0 6px;\"></i> <strong>" . safe_out($cat['name']) . "</strong>";
                    if(!empty($cat['description'])) {
                        $html .= " <small class=\"text-muted\">— " . safe_out(mb_substr($cat['description'], 0, 80)) . "</small>";
                    }
                    if(!empty($cat['is_private'])) {
                        $html .= " <i class=\"fa fa-lock text-danger position-left\" title=\"" . fl('forum_adm_priv_cat') . "\"></i>";
                    }

                    $html .= "<div class=\"pull-right\">";
                    $html .= "<div class=\"visible-md-inline-block visible-lg-inline-block mr-20\" style=\"display:inline-block;\">";
                    $html .= "<span class=\"badge bg-slate-600 mr-10\" title=\"Konular\"><i class=\"fa fa-comments-o\"></i> " . intval($cat['topic_count']) . "</span> ";
                    $html .= "<span class=\"badge bg-slate-500\" title=\"Mesajlar\"><i class=\"fa fa-reply\"></i> " . intval($cat['post_count']) . "</span>";
                    $html .= "</div>";
                    
                    $html .= "<a href=\"?mod=forum&action=cats&edit={$cat['id']}\"><i title=\"Düzenle\" class=\"fa fa-pencil-square-o\"></i></a>&nbsp;&nbsp;";
                    $html .= "<a onclick=\"return confirm('{$L_CONFIRM}');\" href=\"?mod=forum&user_hash={$hash}&action=cats&delete={$cat['id']}\"><i title=\"Sil\" class=\"fa fa-trash-o text-danger\"></i></a>";
                    $html .= "</div>"; 
                    $html .= "</div>"; 
                    
                    if ($has_children) {
                        $html .= "<ol class=\"dd-list\">" . renderCatTreeNestable($cat['children'], $csrf_input, $L_CONFIRM, $hash) . "</ol>";
                    }
                    $html .= "</li>";
                }
                return $html;
            }
        }

        $cat_tree = buildCatTree($all_cats);
        $H_TITLE = fl('forum_adm_cat_manage');
        $H_ADD   = fl('forum_adm_cat_add');

        echo <<<HTML
<div class="panel panel-default">
  <div class="panel-heading">
    {$H_TITLE} <span class="badge bg-slate-600">{$total_cats}</span>
    <div class="heading-elements">
        <ul class="icons-list">
            <li><a href="?mod=forum&action=cats&add=1"><i class="fa fa-plus-circle position-left"></i> {$H_ADD}</a></li>
        </ul>
    </div>
  </div>
  <div class="panel-body">
HTML;

        if ($total_cats > 0) {
            $safe_confirm = htmlspecialchars($L_CONFIRM ?? 'Silmek istediginize emin misiniz?', ENT_QUOTES);
            echo '<div class="dd" id="nestable"><ol class="dd-list">';
            echo renderCatTreeNestable($cat_tree, $csrf_input ?? '', $safe_confirm, $dle_login_hash);
            echo '</ol></div>';
        } else {
            echo '<div class="alert alert-info mb-0">Henüz hiç kategori eklenmemiş.</div>';
        }

        echo <<<HTML
  </div>
  <div class="panel-footer">
      <button class="btn bg-primary-600 btn-sm btn-raised position-left nestable-action" data-action="expand-all">Tümünü Aç</button>
      <button class="btn bg-primary-600 btn-sm btn-raised position-left nestable-action" data-action="collapse-all">Tümünü Kapat</button>
      <a href="?mod=forum&action=cats&add=1" class="btn bg-teal btn-sm btn-raised"><i class="fa fa-plus-circle position-left"></i> {$H_ADD}</a>
  </div>
</div>
HTML;

        $dle_login_hash_js = $dle_login_hash;
        echo <<<JS
<script>
    jQuery(function($){
        $('.dd').nestable({
            maxDepth: 500
        });

        $('.dd-handle a').on('mousedown', function(e){
            e.stopPropagation();
        });

        $('.dd-handle a').on('touchstart', function(e){
            e.stopPropagation();
        });

        $('.nestable-action').on('click', function(e) {
            var target = $(e.target),
                action = target.data('action');
            if (action === 'expand-all') {
                $('.dd').nestable('expandAll');
            }
            if (action === 'collapse-all') {
                $('.dd').nestable('collapseAll');
            }
        });
        
        $('#nestable').nestable().on('change',function(){
            var order = window.JSON.stringify($('.dd').nestable('serialize'));
            ShowLoading('');
            $.post('index.php?mod=forum&action=save_cat_order_ajax', {
                order: order,
                user_hash: '{$dle_login_hash_js}'
            }, function(data){
                HideLoading('');
                if (data != 'ok') {
                    DLEPush.error('Sıralama kaydedilemedi!');
                } else {
                    DLEPush.info('Kategori sıralaması güncellendi.', 'Başarılı');
                }
            });
        });

        setTimeout(function(){
            $('.dd').nestable('collapseAll');
        }, 300);
    });
</script>
JS;

        if(isset($_GET['add']) || $edit_cat) {
            $f = $edit_cat ? $edit_cat : ['id'=>0,'parent_id'=>0,'name'=>'','alt_name'=>'','description'=>'','icon'=>'fa-folder','is_private'=>0,'allowed_groups'=>'','read_groups'=>'','write_groups'=>'','posi'=>0];
            
            $build_opts = function($parent_id = 0, $level = 0) use (&$all_cats, &$build_opts, $f) {
                $html = '';
                $pfx  = str_repeat('&nbsp;&nbsp;&nbsp;&#8627; ', $level);
                foreach($all_cats as $c) {
                    if($c['parent_id'] == $parent_id) {
                        if($f['id'] > 0 && $f['id'] == $c['id']) continue;
                        $sel   = ($f['parent_id'] == $c['id']) ? ' selected' : '';
                        $html .= "<option value='{$c['id']}'{$sel}>{$pfx}" . safe_out($c['name']) . "</option>";
                        $html .= $build_opts($c['id'], $level + 1);
                    }
                }
                return $html;
            };
            $cats_opts  = $build_opts(0, 0);
            $form_title = $f['id'] > 0 ? fl('forum_adm_cat_edit') : fl('forum_adm_cat_new');
            $L_SAVE_BTN = $f['id'] > 0 ? $_L_SAVE : fl('forum_adm_cat_add');
            $is_private_chk = !empty($f['is_private']) ? ' checked' : '';

            $L_PH_CAT_NAME = fl('forum_ph_cat_name');
            $L_PH_CAT_DESC = fl('forum_ph_cat_desc');
            $L_MAIN_CAT = fl('forum_main_cat');
            $L_ADM_PRIV_CAT = fl('forum_adm_priv_cat');
            $L_ADM_PRIV_CAT_DESC = fl('forum_adm_priv_cat_desc');

            echo <<<FORM
<div class="panel panel-flat" style="margin-top:20px;">
    <div class="panel-heading"><h5 class="panel-title">{$form_title}</h5></div>
    <div class="panel-body">
        <form method="post">
            {$csrf_input}
            <input type="hidden" name="save_action" value="save_cat">
            <input type="hidden" name="cat_id" value="{$f['id']}">
            <input type="hidden" name="posi" value="{$f['posi']}">
            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        <label>Kategori Adı <span class="text-danger">*</span></label>
                        <input type="text" name="name" class="form-control" value="{$f['name']}" required placeholder="{$L_PH_CAT_NAME}">
                    </div>
                    <div class="form-group">
                        <label>Açıklama</label>
                        <input type="text" name="description" class="form-control" value="{$f['description']}" placeholder="{$L_PH_CAT_DESC}">
                    </div>
                    <div class="form-group">
                        <label>Üst Kategori</label>
                        <select name="parent_id" class="form-control">
                            <option value="0">— {$L_MAIN_CAT} —</option>
                            {$cats_opts}
                        </select>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label>İkon <small class="text-muted">(FontAwesome sınıfı, örn: fa-folder)</small></label>
                        <div class="input-group">
                            <span class="input-group-addon"><i id="forum_icon_preview" class="fa fa-folder"></i></span>
                            <input type="text" name="icon" id="forum_icon_input" class="form-control" value="{$f['icon']}" placeholder="fa-folder" pattern="[a-zA-Z0-9\- ]+">
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="checkbox-inline">
                            <input type="checkbox" name="is_private" value="1"{$is_private_chk}>
                            <strong>{$L_ADM_PRIV_CAT}</strong> <small class="text-muted">({$L_ADM_PRIV_CAT_DESC})</small>
                        </label>
                    </div>
                    <div class="form-group">
                        <label>İzin Verilen Gruplar <small class="text-muted">(virgülle, örn: 2,3)</small></label>
                        <input type="text" name="allowed_groups" class="form-control" value="{$f['allowed_groups']}" placeholder="2,3">
                    </div>
                    <div class="form-group">
                        <label>Okuma Grupları</label>
                        <input type="text" name="read_groups" class="form-control" value="{$f['read_groups']}" placeholder="2,3">
                    </div>
                    <div class="form-group">
                        <label>Yazma Grupları</label>
                        <input type="text" name="write_groups" class="form-control" value="{$f['write_groups']}" placeholder="2,3">
                    </div>
                </div>
            </div>
            <hr>
            <button type="submit" class="btn bg-teal btn-raised"><i class="fa fa-save position-left"></i> {$L_SAVE_BTN}</button>
            <a href="?mod=forum&action=cats" class="btn btn-default btn-raised">İptal</a>
        </form>
    </div>
</div>
<script>
$(function(){
    var \$inp = $('#forum_icon_input'), \$prev = $('#forum_icon_preview');
    function updateIcon(){ \$prev.attr('class', 'fa ' + $.trim(\$inp.val())); }
    \$inp.on('input', updateIcon);
    updateIcon();
});
</script>
FORM;
        }
        break;

    case 'save_cat_order_ajax':
        if(!isset($_POST['user_hash']) || $_POST['user_hash'] != $dle_login_hash) {
            die("Hacking attempt!");
        }
        
        $order = json_decode($_POST['order'], true);
        
        if(is_array($order)) {
            $posi = 0;
            $updateOrder = function($items, $parent_id = 0) use (&$updateOrder, &$posi, $db) {
                foreach($items as $item) {
                    $id = intval($item['id']);
                    if($id == $parent_id) continue;
                    $db->query("UPDATE " . PREFIX . "_forum_cats SET posi='$posi', parent_id='$parent_id' WHERE id='$id'");
                    $posi++;
                    if(!empty($item['children'])) {
                        $updateOrder($item['children'], $id);
                    }
                }
            };
            $updateOrder($order, 0);
            clear_cache();
            echo "ok";
        } else {
            echo "error";
        }
        exit;
        break;

    case 'ranks':
        $edit_rank = null;
        if(isset($_GET['edit'])) {
            $edit_rank = $db->super_query("SELECT * FROM " . PREFIX . "_forum_ranks WHERE id='" . intval($_GET['edit']) . "'");
        }
        $ranks = $db->super_query("SELECT * FROM " . PREFIX . "_forum_ranks ORDER BY points ASC", true);

        $H_TITLE = fl('forum_adm_rank_manage');
        $H_ADD   = fl('forum_adm_rank_add');
        $T_RANK  = fl('forum_adm_nav_ranks');
        $T_PNTS  = fl('forum_adm_rank_points');
        $T_CLR   = fl('forum_adm_rank_color');
        $T_ICN   = fl('forum_adm_rank_icon');

        echo '<div class="panel panel-default">';
        echo '<div class="panel-heading">' . $H_TITLE;
        echo '<div class="heading-elements"><a href="?mod=forum&action=ranks&add=1" class="btn bg-teal btn-sm btn-raised"><i class="fa fa-plus position-left"></i> ' . $H_ADD . '</a></div></div>';
        echo '<div class="table-responsive"><table class="table table-striped table-xs table-hover">';
        echo '<thead><tr><th>ID</th><th>' . $T_RANK . '</th><th>' . $T_PNTS . '</th><th>' . $T_CLR . '</th><th>' . $T_ICN . '</th><th class="text-center">' . $L_ACTION . '</th></tr></thead><tbody>';

        if($ranks) {
            foreach($ranks as $row) {
                $safe_icon  = preg_match('/^[a-zA-Z0-9\- ]+$/', $row['icon']) ? $row['icon'] : 'fa-user';
                $safe_color = preg_match('/^#[0-9a-fA-F]{3,8}$/', $row['color']) ? $row['color'] : '#333';
                echo "<tr>
                    <td>{$row['id']}</td>
                    <td><i class='fa {$safe_icon}' style='color:{$safe_color}'></i> <strong style='color:{$safe_color}'>" . safe_out($row['title']) . "</strong></td>
                    <td><span class='badge bg-slate-600'>{$row['points']}</span></td>
                    <td><span style='background:{$safe_color};color:#fff;padding:2px 8px;border-radius:3px'>" . safe_out($row['color']) . "</span></td>
                    <td>" . safe_out($row['icon']) . "</td>
                    <td class='text-center'>
                        <a href='?mod=forum&action=ranks&edit={$row['id']}' class='text-primary'><i class='fa fa-pencil'></i></a>
                        &nbsp;
                        <form method='post' style='display:inline' onsubmit='return confirm(\"{$L_CONFIRM}\")'>
                            $csrf_input
                            <input type='hidden' name='save_action' value='delete_rank'>
                            <input type='hidden' name='rank_id' value='{$row['id']}'>
                            <button type='submit' class='btn btn-link text-danger p-0'><i class='fa fa-trash'></i></button>
                        </form>
                    </td>
                </tr>";
            }
        }
        echo '</tbody></table></div></div>';

        if(isset($_GET['add']) || $edit_rank) {
            $f = $edit_rank ? $edit_rank : ['id'=>0,'title'=>'','points'=>0,'color'=>'#2980b9','icon'=>'fa-user','badge_text'=>''];
            $form_title = $f['id'] > 0 ? fl('forum_adm_rank_edit') : fl('forum_adm_rank_new');
            $L_RANK_NAME   = fl('forum_adm_rank_name');
            $L_RANK_POINTS = fl('forum_adm_rank_points');
            $L_RANK_COLOR  = fl('forum_adm_rank_color');
            $L_RANK_ICON   = fl('forum_adm_rank_icon');
            $L_RANK_BADGE  = fl('forum_adm_rank_badge');

            echo <<<RFORM
<div class="panel panel-flat">
    <div class="panel-heading"><h5>{$form_title}</h5></div>
    <div class="panel-body">
        <form method="post">
            $csrf_input
            <input type="hidden" name="save_action" value="save_rank">
            <input type="hidden" name="rank_id" value="{$f['id']}">
            <div class="row">
                <div class="col-md-4">
                    <div class="form-group"><label>{$L_RANK_NAME}</label><input type="text" name="title" class="form-control" value="{$f['title']}" required></div>
                </div>
                <div class="col-md-4">
                    <div class="form-group"><label>{$L_RANK_POINTS}</label><input type="number" name="points" class="form-control" value="{$f['points']}"></div>
                </div>
                <div class="col-md-4">
                     <div class="form-group"><label>{$L_RANK_COLOR}</label><input type="color" name="color" class="form-control" value="{$f['color']}"></div>
                </div>
            </div>
            <div class="row">
                 <div class="col-md-6">
                    <div class="form-group"><label>{$L_RANK_ICON}</label><input type="text" name="icon" class="form-control" value="{$f['icon']}" placeholder="fa-star" pattern="[a-zA-Z0-9\- ]+"></div>
                </div>
                 <div class="col-md-6">
                    <div class="form-group"><label>{$L_RANK_BADGE}</label><input type="text" name="badge_text" class="form-control" value="{$f['badge_text']}" placeholder=""></div>
                </div>
            </div>
            <button type="submit" class="btn bg-teal btn-raised"><i class="fa fa-save position-left"></i> {$_L_SAVE}</button>
            <a href="?mod=forum&action=ranks" class="btn btn-default">{$L_CANCEL}</a>
        </form>
    </div>
</div>
RFORM;
        }
        break;

    case 'prefixes':
        $edit_prefix = null;
        if(isset($_GET['edit'])) {
            $edit_prefix = $db->super_query("SELECT * FROM " . PREFIX . "_forum_prefixes WHERE id='" . intval($_GET['edit']) . "'");
        }
        $prefixes = $db->super_query("SELECT p.*, c.name as cat_name FROM " . PREFIX . "_forum_prefixes p LEFT JOIN " . PREFIX . "_forum_cats c ON p.cat_id = c.id ORDER BY p.posi ASC", true);
        if(!is_array($prefixes)) $prefixes = [];
        
        $H_TITLE = fl('forum_adm_prefixes');
        $H_ADD   = fl('forum_adm_prefix_add');
        $T_TITLE = fl('forum_adm_prefix_title');
        $T_CAT   = fl('forum_adm_prefix_cat');
        $T_COLOR = fl('forum_adm_prefix_color');
        $T_ORDER = fl('forum_adm_prefix_order');
        
        echo '<div class="panel panel-default">';
        echo '<div class="panel-heading">' . $H_TITLE;
        echo '<div class="heading-elements"><a href="?mod=forum&action=prefixes&add=1" class="btn bg-teal btn-sm btn-raised"><i class="fa fa-plus position-left"></i> ' . $H_ADD . '</a></div></div>';
        echo '<div class="table-responsive"><table class="table table-striped table-xs table-hover">';
        echo '<thead><tr><th>ID</th><th>' . $T_TITLE . '</th><th>' . $T_CAT . '</th><th>' . $T_COLOR . '</th><th>' . $T_ORDER . '</th><th class="text-center">' . $L_ACTION . '</th></tr></thead><tbody>';
        
        if($prefixes) {
            foreach($prefixes as $row) {
                $pf_id    = intval($row['id']);
                $pf_title = safe_out($row['title']);
                $pf_cat   = safe_out($row['cat_name'] ?: 'Tümü');
                $pf_color = safe_out($row['color']);
                $pf_bg    = safe_out($row['bg_color']);
                $pf_posi  = intval($row['posi']);
                
                echo "<tr>
                    <td>{$pf_id}</td>
                    <td><span class='df-prefix' style='color:{$pf_color};background:{$pf_bg};padding:2px 7px;border-radius:3px;font-size:11px;font-weight:600;'>{$pf_title}</span></td>
                    <td>{$pf_cat}</td>
                    <td><i class='fa fa-square' style='color:{$pf_color}'></i> / <i class='fa fa-square' style='color:{$pf_bg}'></i></td>
                    <td>{$pf_posi}</td>
                    <td class='text-center'>
                        <a href='?mod=forum&action=prefixes&edit={$pf_id}' class='text-primary'><i class='fa fa-pencil'></i></a>
                        &nbsp;
                        <form method='post' style='display:inline' onsubmit='return confirm(\"{$L_CONFIRM}\")'>
                            $csrf_input
                            <input type='hidden' name='save_action' value='delete_prefix'>
                            <input type='hidden' name='prefix_id' value='{$pf_id}'>
                            <button type='submit' class='btn btn-link text-danger p-0'><i class='fa fa-trash'></i></button>
                        </form>
                    </td>
                </tr>";
            }
        }
        echo '</tbody></table></div></div>';
        
        if(isset($_GET['add']) || $edit_prefix) {
            $f = $edit_prefix ? $edit_prefix : ['id'=>0,'cat_id'=>0,'title'=>'','color'=>'#3498db','bg_color'=>'#ebf5fb','posi'=>0];
            $form_title = $f['id'] > 0 ? fl('forum_adm_prefix_edit') : fl('forum_adm_prefix_new');
            
            $cats_all = $db->super_query("SELECT id, name FROM " . PREFIX . "_forum_cats ORDER BY posi ASC", true);
            $cat_opts = '<option value="0">Tüm Kategoriler</option>';
            if($cats_all) {
                foreach($cats_all as $cc) {
                    $sel = ($f['cat_id'] == $cc['id']) ? 'selected' : '';
                    $cat_opts .= "<option value='{$cc['id']}' $sel>" . safe_out($cc['name']) . "</option>";
                }
            }
            
            echo <<<PFORM
<div class="panel panel-flat">
    <div class="panel-heading"><h5>{$form_title}</h5></div>
    <div class="panel-body">
        <form method="post">
            $csrf_input
            <input type="hidden" name="save_action" value="save_prefix">
            <input type="hidden" name="prefix_id" value="{$f['id']}">
            <div class="row">
                <div class="col-md-4">
                    <div class="form-group"><label>{$T_TITLE}</label><input type="text" name="title" class="form-control" value="{$f['title']}" required></div>
                </div>
                <div class="col-md-4">
                    <div class="form-group"><label>{$T_CAT}</label><select name="cat_id" class="form-control">{$cat_opts}</select></div>
                </div>
                <div class="col-md-2">
                    <div class="form-group"><label>Yazı Rengi</label><input type="color" name="color" class="form-control" value="{$f['color']}"></div>
                </div>
                <div class="col-md-2">
                    <div class="form-group"><label>Arkaplan</label><input type="color" name="bg_color" class="form-control" value="{$f['bg_color']}"></div>
                </div>
            </div>
            <button type="submit" class="btn bg-teal btn-raised"><i class="fa fa-save position-left"></i> {$_L_SAVE}</button>
            <a href="?mod=forum&action=prefixes" class="btn btn-default">{$L_CANCEL}</a>
        </form>
    </div>
</div>
PFORM;
        }
        break;

    case 'tags':
        $edit_tag = null;
        if(isset($_GET['edit'])) {
            $edit_tag = $db->super_query("SELECT * FROM " . PREFIX . "_forum_tags WHERE id='" . intval($_GET['edit']) . "'");
        }
        $tags = $db->super_query("SELECT * FROM " . PREFIX . "_forum_tags ORDER BY name ASC", true);
        if(!is_array($tags)) $tags = [];
        
        $H_TITLE = fl('forum_adm_tags_manage');
        $T_NAME  = fl('forum_adm_tag_name');
        $T_SLUG  = fl('forum_adm_tag_slug');
        $T_COUNT = fl('forum_adm_tag_count');
        
        echo '<div class="panel panel-default">';
        echo '<div class="panel-heading">' . $H_TITLE . ' <span class="badge bg-slate-600">' . count($tags) . '</span></div>';
        echo '<div class="table-responsive"><table class="table table-striped table-xs table-hover">';
        echo '<thead><tr><th>ID</th><th>' . $T_NAME . '</th><th>' . $T_SLUG . '</th><th>' . $T_COUNT . '</th><th class="text-center">' . $L_ACTION . '</th></tr></thead><tbody>';
        
        if($tags) {
            foreach($tags as $row) {
                $tid   = intval($row['id']);
                $tname = safe_out($row['name']);
                $tslug = safe_out($row['slug']);
                $tcnt  = intval($row['count']);
                
                echo "<tr>
                    <td>{$tid}</td>
                    <td><span class='df-tag' style='display:inline-block;font-size:11px;padding:2px 8px;border-radius:12px;background:#e2e8f0;color:#475569;'>#{$tname}</span></td>
                    <td><code>{$tslug}</code></td>
                    <td><span class='badge bg-slate-600'>{$tcnt}</span></td>
                    <td class='text-center'>
                        <a href='?mod=forum&action=tags&edit={$tid}' class='text-primary'><i class='fa fa-pencil'></i></a>
                        &nbsp;
                        <form method='post' style='display:inline' onsubmit='return confirm(\"{$L_CONFIRM}\")'>
                            $csrf_input
                            <input type='hidden' name='save_action' value='delete_tag'>
                            <input type='hidden' name='tag_id' value='{$tid}'>
                            <button type='submit' class='btn btn-link text-danger p-0'><i class='fa fa-trash'></i></button>
                        </form>
                    </td>
                </tr>";
            }
        }
        echo '</tbody></table></div></div>';
        
        if(isset($_GET['edit']) && $edit_tag) {
            $f = $edit_tag;
            $form_title = fl('forum_adm_tag_edit');
            echo <<<TFORM
<div class="panel panel-flat">
    <div class="panel-heading"><h5>{$form_title}</h5></div>
    <div class="panel-body">
        <form method="post">
            $csrf_input
            <input type="hidden" name="save_action" value="save_tag">
            <input type="hidden" name="tag_id" value="{$f['id']}">
            <div class="form-group">
                <label>{$T_NAME}</label>
                <input type="text" name="name" class="form-control" value="{$f['name']}" required>
            </div>
            <button type="submit" class="btn bg-teal btn-raised"><i class="fa fa-save position-left"></i> {$_L_SAVE}</button>
            <a href="?mod=forum&action=tags" class="btn btn-default">{$L_CANCEL}</a>
        </form>
    </div>
</div>
TFORM;
        }
        break;

    case 'words':
        $edit_word = null;
        if(isset($_GET['edit'])) {
            $edit_word = $db->super_query("SELECT * FROM " . PREFIX . "_forum_banned_words WHERE id='" . intval($_GET['edit']) . "'");
        }
        $words = $db->super_query("SELECT * FROM " . PREFIX . "_forum_banned_words ORDER BY id ASC", true);
        
        $H_TITLE = fl('forum_adm_word_manage');
        $H_ADD   = fl('forum_adm_word_add');
        $T_WORD  = fl('forum_adm_word_word');
        $T_REPL  = fl('forum_adm_word_repl');

        echo '<div class="panel panel-default">';
        echo '<div class="panel-heading">' . $H_TITLE;
        echo '<div class="heading-elements"><a href="?mod=forum&action=words&add=1" class="btn bg-teal btn-sm btn-raised"><i class="fa fa-plus position-left"></i> ' . $H_ADD . '</a></div></div>';
        echo '<div class="table-responsive"><table class="table table-striped table-xs">';
        echo '<thead><tr><th>ID</th><th>' . $T_WORD . '</th><th>' . $T_REPL . '</th><th class="text-center">' . $L_ACTION . '</th></tr></thead><tbody>';
        if($words) {
            foreach($words as $row) {
                echo "<tr>
                    <td>{$row['id']}</td>
                    <td><code>" . safe_out($row['word']) . "</code></td>
                    <td>" . safe_out($row['replacement']) . "</td>
                    <td class='text-center'>
                        <a href='?mod=forum&action=words&edit={$row['id']}' class='text-primary'><i class='fa fa-pencil'></i></a>
                        &nbsp;
                        <form method='post' style='display:inline' onsubmit='return confirm(\"{$L_CONFIRM}\")'>
                            $csrf_input
                            <input type='hidden' name='save_action' value='delete_word'>
                            <input type='hidden' name='word_id' value='{$row['id']}'>
                            <button type='submit' class='btn btn-link text-danger p-0'><i class='fa fa-trash'></i></button>
                        </form>
                    </td>
                </tr>";
            }
        }
        echo '</tbody></table></div></div>';

        if(isset($_GET['add']) || $edit_word) {
            $f = $edit_word ? $edit_word : ['id'=>0,'word'=>'','replacement'=>''];
            $form_title = $f['id'] > 0 ? fl('forum_adm_word_edit') : fl('forum_adm_word_new');
            $L_WORD_LBL = fl('forum_adm_word_word');
            $L_REPL_LBL = fl('forum_adm_word_repl');

            echo <<<WFORM
<div class="panel panel-flat">
    <div class="panel-heading"><h5>{$form_title}</h5></div>
    <div class="panel-body">
        <form method="post">
            $csrf_input
            <input type="hidden" name="save_action" value="save_word">
            <input type="hidden" name="word_id" value="{$f['id']}">
            <div class="row">
                <div class="col-md-5">
                    <div class="form-group"><label>{$L_WORD_LBL}</label><input type="text" name="word" class="form-control" value="{$f['word']}" required></div>
                </div>
                <div class="col-md-5">
                    <div class="form-group"><label>{$L_REPL_LBL}</label><input type="text" name="replacement" class="form-control" value="{$f['replacement']}" placeholder="***"></div>
                </div>
            </div>
            <button type="submit" class="btn bg-teal btn-raised"><i class="fa fa-save position-left"></i> {$_L_SAVE}</button>
            <a href="?mod=forum&action=words" class="btn btn-default">{$L_CANCEL}</a>
        </form>
    </div>
</div>
WFORM;
        }
        break;

    case 'complaints':
        $page_c   = isset($_GET['page']) ? max(1, intval($_GET['page'])) : 1;
        $per_c    = 25;
        $offset_c = ($page_c - 1) * $per_c;

        $total_c    = intval($db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_complaint WHERE text LIKE '[FORUM]%'")['c']);
        $complaints = $db->super_query(
            "SELECT * FROM " . PREFIX . "_complaint WHERE text LIKE '[FORUM]%' ORDER BY date DESC LIMIT $offset_c, $per_c",
            true
        );

        $H_TITLE = fl('forum_adm_complaints_title');
        $T_FROM  = fl('forum_adm_complaint_from');
        $T_CONT  = fl('forum_adm_complaint_content');
        $T_DATE  = fl('forum_date');

        echo '<div class="panel panel-default">';
        echo '<div class="panel-heading">' . $H_TITLE . ' <span class="badge bg-red">' . $total_c . '</span></div>';
        echo '<div class="table-responsive"><table class="table table-striped table-xs table-hover">';
        echo '<thead><tr><th>ID</th><th>' . $T_FROM . '</th><th>' . $T_CONT . '</th><th>' . $T_DATE . '</th><th class="text-center">' . $L_ACTION . '</th></tr></thead><tbody>';
        if($complaints) {
            foreach($complaints as $row) {
                $c_date = is_numeric($row['date']) ? date("d.m.Y H:i", $row['date']) : safe_out($row['date']);
                echo "<tr>
                    <td>{$row['id']}</td>
                    <td><strong>" . safe_out($row['from']) . "</strong></td>
                    <td><small>" . safe_out($row['text']) . "</small></td>
                    <td><span class='badge bg-slate-600'>{$c_date}</span></td>
                    <td class='text-center'>
                        <form method='post' style='display:inline' onsubmit='return confirm(\"{$L_CONFIRM}\")'>
                            $csrf_input
                            <input type='hidden' name='save_action' value='delete_complaint'>
                            <input type='hidden' name='complaint_id' value='{$row['id']}'>
                            <button type='submit' class='btn btn-link text-danger p-0'><i class='fa fa-trash'></i></button>
                        </form>
                    </td>
                </tr>";
            }
        }
        echo '</tbody></table></div>';
        echo renderPagination($total_c, $per_c, $page_c, '?mod=forum&action=complaints');
        echo '</div>';
        break;

    case 'logs':
        $page     = isset($_GET['page']) ? max(1, intval($_GET['page'])) : 1;
        $per_page = 30;
        $offset   = ($page - 1) * $per_page;
        $total    = intval($db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_logs")['c']);
        $logs     = $db->super_query(
            "SELECT l.*, u.name as mod_name FROM " . PREFIX . "_forum_logs l
             LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = l.mod_id
             ORDER BY l.date DESC LIMIT $offset, $per_page",
            true
        );

        $action_labels = [
            'lock_topic'       => '<span class="label bg-orange">' . fl('forum_log_lock') . '</span>',
            'unlock_topic'     => '<span class="label bg-green">' . fl('forum_log_unlock') . '</span>',
            'pin_topic'        => '<span class="label bg-teal">' . fl('forum_log_pin') . '</span>',
            'unpin_topic'      => '<span class="label bg-grey">' . fl('forum_log_unpin') . '</span>',
            'delete_topic'     => '<span class="label bg-red">' . fl('forum_log_del_topic') . '</span>',
            'delete_post'      => '<span class="label bg-red">' . fl('forum_log_del_post') . '</span>',
            'edit_post_as_mod' => '<span class="label bg-purple">' . fl('forum_log_edit_mod') . '</span>',
            'edit_own_post'    => '<span class="label bg-slate">' . fl('forum_log_edit_own') . '</span>',
            'move_topic'       => '<span class="label bg-blue">' . fl('forum_log_move') . '</span>',
            'bump_topic'       => '<span class="label bg-indigo">' . fl('forum_log_bump') . '</span>',
        ];

        $H_TITLE = fl('forum_adm_logs_title');
        $T_MOD   = fl('forum_adm_log_mod');
        $T_ACT   = fl('forum_adm_log_action');
        $T_TID   = fl('forum_adm_log_topic');
        $T_PID   = fl('forum_adm_log_post');
        $T_IP    = fl('forum_adm_log_ip');
        $T_DATE  = fl('forum_date');

        echo '<div class="panel panel-default">';
        echo '<div class="panel-heading">' . $H_TITLE . ' <span class="badge bg-slate-600">' . $total . '</span></div>';
        echo '<div class="table-responsive"><table class="table table-striped table-xs">';
        echo '<thead><tr><th>ID</th><th>' . $T_MOD . '</th><th>' . $T_ACT . '</th><th>' . $T_TID . '</th><th>' . $T_PID . '</th><th>' . $T_IP . '</th><th>' . $T_DATE . '</th></tr></thead><tbody>';
        if($logs) {
            foreach($logs as $row) {
                $lbl = isset($action_labels[$row['action']]) ? $action_labels[$row['action']] : "<code>" . safe_out($row['action']) . "</code>";
                echo "<tr>
                    <td>{$row['id']}</td>
                    <td><strong>" . safe_out($row['mod_name']) . "</strong></td>
                    <td>{$lbl}</td>
                    <td>" . ($row['topic_id'] > 0 ? "<a href='" . forum_url('forum_topic', ['newsid' => intval($row['topic_id'])]) . "' target='_blank'>#{$row['topic_id']}</a>" : '-') . "</td>
                    <td>" . ($row['post_id'] > 0 ? "#{$row['post_id']}" : '-') . "</td>
                    <td><code>" . safe_out($row['ip']) . "</code></td>
                    <td>" . safe_out($row['date']) . "</td>
                </tr>";
            }
        }
        echo '</tbody></table></div>';
        echo renderPagination($total, $per_page, $page, '?mod=forum&action=logs');
        echo '</div>';
        break;

    case 'settings':
        $settings_list = [
            'posts_per_page'       => ['forum_set_posts_per_page', '20'],
            'topics_per_page'      => ['forum_set_topics_per_page', '25'],
            'allow_guests_view'    => ['forum_set_guest_view', '1'],
            'min_post_length'      => ['forum_set_min_post', '10'],
            'max_post_length'      => ['forum_set_max_post', '10000'],
            'flood_time'           => ['forum_set_flood', '30'],
            'allow_attachments'    => ['forum_set_allow_attach', '1'],
            'max_attachment_size'  => ['forum_set_max_attach', '5242880'],
            'allowed_filetypes'    => ['forum_set_allowed_ext', 'jpg,jpeg,png,gif,webp,pdf,zip,rar'],
            'points_new_topic'     => ['forum_set_points_topic', '10'],
            'points_new_reply'     => ['forum_set_points_reply', '2'],
            'points_received_like' => ['forum_set_points_like', '5'],
            'approval_threshold'   => ['forum_set_approval', '0'],
            'bump_cooldown_hours'  => ['forum_set_bump_cd', '24'],
            'sticky_op'            => ['forum_set_sticky_op', '1'],
        ];
        
        $current_settings = [];
        $all_s = $db->super_query("SELECT name, value FROM " . PREFIX . "_forum_settings", true);
        foreach($all_s as $s) $current_settings[$s['name']] = $s['value'];

        $H_TITLE = fl('forum_adm_settings_title');
        $H_MAINT = fl('forum_adm_maintenance');
        $B_RECALC = fl('forum_adm_recalc');
        $B_CLEAR  = fl('forum_adm_clear_cache');
        $L_CLEAN_DAYS = fl('forum_clean_readlog_days', 'Okuma geçmişi temizleme (gün)');

        echo '<div class="panel panel-flat">';
        echo '<div class="panel-heading"><h5>' . $H_TITLE . '</h5></div>';
        echo '<div class="panel-body"><form method="post">'.$csrf_input.'<input type="hidden" name="save_action" value="save_settings"><div class="row">';
        foreach($settings_list as $key => $info) {
            $val = isset($current_settings[$key]) ? htmlspecialchars($current_settings[$key], ENT_QUOTES, 'UTF-8') : $info[1];
            $lbl = fl($info[0]);
            echo "<div class='col-md-4'><div class='form-group'><label>{$lbl}</label><input type='text' name='$key' class='form-control' value='$val'></div></div>";
        }
        $clean_days = isset($current_settings['clean_readlog_days']) ? intval($current_settings['clean_readlog_days']) : 30;
        echo "<div class='col-md-4'><div class='form-group'><label>{$L_CLEAN_DAYS}</label><input type='number' name='clean_readlog_days' class='form-control' value='{$clean_days}'></div></div>";
        echo '</div><button type="submit" class="btn bg-teal btn-raised"><i class="fa fa-save position-left"></i> ' . $_L_SAVE . '</button></form></div></div>';

        echo '<div class="panel panel-flat">';
        echo '<div class="panel-heading"><h5>' . $H_MAINT . '</h5></div>';
        echo '<div class="panel-body">';
        echo '<form method="post" style="display:inline">'.$csrf_input.'<input type="hidden" name="save_action" value="recalc_stats"><button type="submit" class="btn btn-default btn-raised"><i class="fa fa-calculator position-left"></i> ' . $B_RECALC . '</button></form> ';
        echo '<form method="post" style="display:inline">'.$csrf_input.'<input type="hidden" name="save_action" value="clear_cache"><button type="submit" class="btn btn-default btn-raised"><i class="fa fa-refresh position-left"></i> ' . $B_CLEAR . '</button></form>';
        echo '</div></div>';
        break;

    // --- BURASI %100 Orijinal "editnews" (DLE) Klonuna Çevrilen Konular Listesi ---
    case 'topics':
        $tp      = isset($_GET['page']) ? max(1,intval($_GET['page'])) : 1;
        $news_per_page = 50; 
        $toff    = ($tp-1)*$news_per_page;
        $ttotal  = intval($db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_topics WHERE is_deleted='0'")['c']);
        
        $tlist   = $db->super_query(
            "SELECT t.id,t.title,t.replies,t.views,t.is_pinned,t.is_locked,t.date,t.cat_id,
             u.name as username, c.name as cat_name
             FROM " . PREFIX . "_forum_topics t
             LEFT JOIN " . USERPREFIX . "_users u ON u.user_id=t.user_id
             LEFT JOIN " . PREFIX . "_forum_cats c ON c.id=t.cat_id
             WHERE t.is_deleted='0' ORDER BY t.is_pinned DESC, t.date DESC LIMIT $toff,$news_per_page", true);

        // Toplu taşıma için kategori listesi
        $all_cats_bulk = $db->super_query("SELECT id, name FROM " . PREFIX . "_forum_cats ORDER BY posi ASC", true);
        $bulk_cat_opts = '<option value="0">' . fl('forum_adm_sel_cat') . '</option>';
        if($all_cats_bulk) {
            foreach($all_cats_bulk as $bc) {
                $bulk_cat_opts .= '<option value="' . intval($bc['id']) . '">' . safe_out($bc['name']) . '</option>';
            }
        }

        $entries = "";
        $entries_showed = 0;
        if($tlist) {
            foreach($tlist as $row) {
                $tid = intval($row['id']);
                $date = date("d.m.Y", strtotime($row['date']));
                $title = safe_out($row['title']);
                $t_url = forum_url('forum_topic', ['newsid' => $tid]);
                
                $status_icons = "";
                if($row['is_pinned']) $status_icons .= "<span class=\"badge badge-danger position-left\">" . fl('forum_pin') . "</span>";
                if($row['is_locked']) $status_icons .= "<i class=\"fa fa-lock position-left text-muted\"></i>";
                
                $cat_name = safe_out($row['cat_name']);
                $username = safe_out($row['username']);
                $views = intval($row['views']);
                $replies = intval($row['replies']);

                $erlaub = "<span class=\"text-success\"><b><i class=\"fa fa-check-circle\"></i></b></span>";

                $entries .= "<tr>";
                // 1. Sütun: Tarih (Tıklanabilir)
                $entries .= "<td class=\"hidden-xs hidden-sm text-nowrap cursor-pointer\" onclick=\"document.location = '?mod=forum&action=edit_topic&id={$tid}'; return false;\">{$date}</td>";
                // 2. Sütun: Başlık ve İkonlar (Tıklanabilir)
                $entries .= "<td class=\"cursor-pointer\" onclick=\"document.location = '?mod=forum&action=edit_topic&id={$tid}'; return false;\">{$status_icons}<a title='" . fl('forum_edit') . "' href=\"?mod=forum&action=edit_topic&id={$tid}\">{$title}</a></td>";
                // 3. Sütun: Görüntülenme
                $entries .= "<td class=\"hidden-xs text-nowrap text-center\"><a data-original-title=\"" . fl('forum_views') . "\" class=\"tip\" href=\"{$t_url}\" target=\"_blank\">{$views}</a></td>";
                // 4. Sütun: Cevaplar
                $comm_link = "<div class=\"btn-group\"><a href=\"{$t_url}\" target=\"_blank\" class=\"tip\" data-original-title=\"" . fl('forum_replies') . "\">{$replies}</a></div>";
                $entries .= "<td class=\"hidden-xs text-nowrap text-center\" style=\"text-align: center\">{$comm_link}</td>";
                // 5. Sütun: Onay Durumu
                $entries .= "<td style=\"text-align: center\" class=\"cursor-pointer\" onclick=\"document.location = '?mod=forum&action=edit_topic&id={$tid}'; return false;\">{$erlaub}</td>";
                // 6. Sütun: Kategori
                $entries .= "<td class=\"hidden-xs cursor-pointer text-center\" onclick=\"document.location = '?mod=forum&action=edit_topic&id={$tid}'; return false;\">{$cat_name}</td>";
                // 7. Sütun: Yazar
                $entries .= "<td class=\"hidden-xs hidden-sm\">{$username}</td>";
                // 8. Sütun: Checkbox (Toplu İşlem İçin)
                $entries .= "<td style=\"text-align: center\"><input name=\"topic_ids[]\" value=\"{$tid}\" type=\"checkbox\" class=\"icheck\"></td>";
                $entries .= "</tr>";
                
                $entries_showed++;
            }
        }

        echo <<<JSCRIPT
<script>
function ckeck_uncheck_all() {
    var frm = document.edittopics;
    for (var i=0;i<frm.elements.length;i++) {
        var elmnt = frm.elements[i];
        if (elmnt.type=='checkbox') {
            if(frm.master_box.checked == true){ elmnt.checked=false; $(elmnt).parents('tr').removeClass('warning'); }
            else{ elmnt.checked=true; $(elmnt).parents('tr').addClass('warning'); }
        }
    }
    if(frm.master_box.checked == true){ frm.master_box.checked = false; }
    else{ frm.master_box.checked = true; }
    
    $(frm.master_box).parents('tr').removeClass('warning');
    $.uniform.update();
}
$(function() {
    $('.table').find('tr > td:last-child').find('input[type=checkbox]').on('change', function() {
        if($(this).is(':checked')) {
            $(this).parents('tr').addClass('warning');
        }
        else {
            $(this).parents('tr').removeClass('warning');
        }
    });
    
    $('[name="bulk_action"]').on('change', function() {
        if($(this).val() == 'move') {
            $('#move_cat_container').show();
        } else {
            $('#move_cat_container').hide();
        }
    });
});
</script>
JSCRIPT;

        if( $entries_showed == 0 ) {
            $L_TOPIC_LIST = fl('forum_adm_topic_list');
            $L_NO_TOPICS  = fl('forum_adm_no_topics');
            echo <<<HTML
<div class="panel panel-default">
  <div class="panel-heading">
    {$L_TOPIC_LIST}
  </div>
  <div class="panel-body">
    <div style="display: table;min-height:100px;width:100%;">
      <div class="text-center" style="display: table-cell;vertical-align:middle;">{$L_NO_TOPICS}</div>
    </div>
   </div>
</div>
HTML;
        } else {
            $L_ACTION_SEL   = fl('forum_adm_action_select');
            $L_TOPIC_LIST   = fl('forum_adm_topic_list');
            $L_FOUND        = fl('forum_adm_topics_found');
            $L_TH_TITLE     = fl('forum_adm_topic_title');
            $L_TH_VIEWS     = fl('forum_views');
            $L_TH_REPLIES   = fl('forum_replies');
            $L_TH_CAT       = fl('forum_adm_topic_cat');
            $L_TH_AUTHOR    = fl('forum_adm_topics_author');
            $L_TH_SEL_ALL   = fl('forum_adm_select_all');
            $L_OPT_DEL      = fl('forum_delete');
            $L_OPT_LOCK     = fl('forum_lock');
            $L_OPT_UNLOCK   = fl('forum_unlock');
            $L_OPT_PIN      = fl('forum_pin');
            $L_OPT_UNPIN    = fl('forum_adm_unpin');
            $L_OPT_MOVE     = fl('forum_bulk_move');
            $L_APPLY        = fl('forum_adm_apply');
            $L_BULK_CONFIRM = fl('forum_adm_bulk_confirm');
            echo <<<HTML
<form action="?mod=forum&action=topics" method="post" name="edittopics">
<input type="hidden" name="user_hash" value="{$dle_login_hash}" />
<input type="hidden" name="save_action" value="bulk_topics" />
<div class="panel panel-default">
  <div class="panel-heading">
    {$L_TOPIC_LIST} ({$ttotal} {$L_FOUND})
  </div>

    <table class="table table-striped table-xs table-hover">
      <thead>
      <tr>
        <th class="hidden-xs hidden-sm" style="width: 80px;">&nbsp;</th>
        <th>{$L_TH_TITLE}</th>
        <th class="hidden-xs text-center" style="width: 60px;"><i class="fa fa-eye tip" data-original-title="{$L_TH_VIEWS}"></i></th>
        <th class="hidden-xs text-center" style="width: 60px;"><i class="fa fa-comment-o tip" data-original-title="{$L_TH_REPLIES}"></i></th>
        <th style="width: 30px;text-align:center;">&nbsp;</th>
        <th class="hidden-xs text-center">{$L_TH_CAT}</th>
        <th class="hidden-xs hidden-sm" style="max-width: 140px">{$L_TH_AUTHOR}</th>
        <th style="width: 40px"><input type="checkbox" name="master_box" title="{$L_TH_SEL_ALL}" onclick="javascript:ckeck_uncheck_all();" class="icheck"></th>
      </tr>
      </thead>
      <tbody>
        {$entries}
      </tbody>
    </table>

    <div class="panel-footer">
      <div class="pull-right">
        <span id="move_cat_container" style="display:none;" class="position-left">
            <select name="move_cat_id" class="uniform" style="display:inline-block;">
                {$bulk_cat_opts}
            </select>
        </span>
        <select name="bulk_action" class="uniform position-left">
            <option value="">{$L_ACTION_SEL}</option>
            <option value="delete">{$L_OPT_DEL}</option>
            <option value="lock">{$L_OPT_LOCK}</option>
            <option value="unlock">{$L_OPT_UNLOCK}</option>
            <option value="pin">{$L_OPT_PIN}</option>
            <option value="unpin">{$L_OPT_UNPIN}</option>
            <option value="move">{$L_OPT_MOVE}</option>
        </select>
        <input class="btn bg-teal btn-sm btn-raised" type="submit" value="{$L_APPLY}" onclick="return confirm('{$L_BULK_CONFIRM}');">
      </div>
    </div>
</div>
HTML;

            // Pagination DLE native
            $all_count_news = $ttotal;
            $start_from = $toff;
            $npp_nav = "";

            if( $all_count_news > $news_per_page ) {
                $enpages_count = @ceil( $all_count_news / $news_per_page );
                $enpages = "";

                if( $enpages_count <= 10 ) {
                    for($j = 1; $j <= $enpages_count; $j ++) {
                        if( $j != $tp ) {
                            $enpages .= "<li><a href=\"?mod=forum&action=topics&page={$j}\">$j</a></li>";
                        } else {
                            $enpages .= "<li class=\"active\"><span>$j</span></li>";
                        }
                    }
                    $npp_nav .= $enpages;
                } else {
                    $start = 1;
                    $end = 10;
                    if( $tp > 4 ) {
                        $start = $tp - 3;
                        $end = $start + 9;
                        if( $end > $enpages_count ) {
                            $start = $enpages_count - 10;
                            $end = $enpages_count;
                        }
                    }

                    if( $start > 2 ) {
                        $enpages .= "<li><a href=\"?mod=forum&action=topics&page=1\">1</a></li> <li><span>...</span></li>";
                    }

                    for($j = $start; $j <= $end; $j ++) {
                        if( $j != $tp ) {
                            $enpages .= "<li><a href=\"?mod=forum&action=topics&page={$j}\">{$j}</a></li>";
                        } else {
                            $enpages .= "<li class=\"active\"><span>{$j}</span></li>";
                        }
                    }

                    if($end < $enpages_count) {
                         $enpages .= "<li><span>...</span></li>";
                         $enpages .= "<li><a href=\"?mod=forum&action=topics&page={$enpages_count}\">{$enpages_count}</a></li>";
                    }
                    $npp_nav .= $enpages;
                }

                echo "<ul class=\"pagination pagination-sm mb-20\">".$npp_nav."</ul>";
            }

            echo "</form>";
        }
        break;

    // --- BURASI EKSİK OLAN KONU DÜZENLEME (EDIT TOPIC) SAYFASI ---
    case 'edit_topic':
        $topic_id = intval($_GET['id']);
        $topic = $db->super_query("SELECT * FROM " . PREFIX . "_forum_topics WHERE id='$topic_id'");
        
        if(!$topic) {
            msg("error", fl('forum_error'), fl('forum_adm_topic_not_found'), "?mod=forum&action=topics");
        }

        // Kategorileri çek
        $all_cats = $db->super_query("SELECT id, name, parent_id FROM " . PREFIX . "_forum_cats ORDER BY parent_id, posi", true);
        $cat_opts = '';
        if($all_cats) {
            foreach($all_cats as $c) {
                $sel = ($c['id'] == $topic['cat_id']) ? 'selected' : '';
                $pfx = ($c['parent_id'] > 0) ? '&nbsp;&nbsp;↳ ' : '';
                $cat_opts .= "<option value='{$c['id']}' {$sel}>{$pfx}" . safe_out($c['name']) . "</option>";
            }
        }
        
        // Prefixleri çek
        $prefixes = $Forum->getPrefixes($topic['cat_id']);
        $prefix_opts = '<option value="0">' . fl('forum_no_prefix') . '</option>';
        if($prefixes) {
            foreach($prefixes as $pf) {
                $sel = ($topic['prefix_id'] == $pf['id']) ? 'selected' : '';
                $prefix_opts .= "<option value='{$pf['id']}' {$sel}>" . safe_out($pf['title']) . "</option>";
            }
        }
        
        // Mevcut etiketleri virgüllü string yap
        $tags_array = $Forum->getTopicTags($topic_id);
        $tags_val = '';
        if($tags_array) {
            $tags_val = implode(', ', array_column($tags_array, 'name'));
        }

        // İlk mesajı bul
        $first_post = $db->super_query("SELECT id, text FROM " . PREFIX . "_forum_posts WHERE topic_id='$topic_id' ORDER BY id ASC LIMIT 1");
        $post_text = $first_post ? $first_post['text'] : ''; 
        $post_id = $first_post ? intval($first_post['id']) : 0;

        $chk_pin = $topic['is_pinned'] ? 'checked' : '';
        $chk_lock = $topic['is_locked'] ? 'checked' : '';
        
        // DLE'nin orijinal editörünü (WYSIWYG) sayfaya entegre et
        $row = ['short_story' => $post_text];
        ob_start();
        include(DLEPlugins::Check(ENGINE_DIR . '/editor/shortnews.php'));
        $editor_html = ob_get_clean();

        // Dil sabitleri
        $L_EDIT_TOPIC    = fl('forum_adm_edit_topic');
        $L_BACK          = fl('forum_adm_back');
        $L_TITLE         = fl('forum_adm_topic_title');
        $L_PREFIX        = fl('forum_adm_topic_prefix');
        $L_DESC          = fl('forum_adm_topic_desc');
        $L_TAGS          = fl('forum_adm_topic_tags');
        $L_CAT           = fl('forum_adm_topic_cat');
        $L_CONTENT       = fl('forum_adm_topic_content');
        $L_OPTIONS       = fl('forum_adm_topic_options');
        $L_PIN           = fl('forum_adm_pin_topic');
        $L_LOCK          = fl('forum_adm_lock_topic');
        $L_SAVE          = fl('forum_save');
        $L_CANCEL        = fl('forum_cancel');
        $L_PH_TAGS       = fl('forum_ph_tags'); // Heredoc icin degiskene al

        echo <<<HTML
<div class="panel panel-flat">
    <div class="panel-heading">
        <h5 class="panel-title"><i class="fa fa-pencil-square-o position-left"></i> {$L_EDIT_TOPIC}: {$topic['title']}</h5>
        <div class="heading-elements">
            <ul class="icons-list">
                <li><a href="?mod=forum&action=topics"><i class="fa fa-arrow-left position-left"></i> {$L_BACK}</a></li>
            </ul>
        </div>
    </div>
    <div class="panel-body">
        <form method="post" action="" class="form-horizontal" name="addnews" id="addnews">
            {$csrf_input}
            <input type="hidden" name="save_action" value="save_edit_topic">
            <input type="hidden" name="topic_id" value="{$topic_id}">
            <input type="hidden" name="post_id" value="{$post_id}">
            
            <div class="form-group">
                <label class="control-label col-md-2">{$L_TITLE}</label>
                <div class="col-md-10">
                    <input type="text" name="title" class="form-control" value="{$topic['title']}" required>
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-md-2">{$L_PREFIX}</label>
                <div class="col-md-10">
                    <select name="prefix_id" class="uniform form-control" style="max-width:250px;">{$prefix_opts}</select>
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-md-2">{$L_DESC}</label>
                <div class="col-md-10">
                    <input type="text" name="topic_description" class="form-control" value="{$topic['description']}" maxlength="500" placeholder="{$L_DESC}...">
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-md-2">{$L_TAGS}</label>
                <div class="col-md-10">
                    <input type="text" name="tags" class="form-control" value="{$tags_val}" placeholder="{$L_PH_TAGS}">
                </div>
            </div>
            
            <div class="form-group">
                <label class="control-label col-md-2">{$L_CAT}</label>
                <div class="col-md-10">
                    <select name="cat_id" class="uniform form-control" style="max-width:350px;">
                        {$cat_opts}
                    </select>
                </div>
            </div>
            
            <div class="form-group editor-group">
                <label class="control-label col-md-2">{$L_CONTENT}</label>
                <div class="col-md-10">
                    {$editor_html}
                </div>
            </div>
            
            <div class="form-group">
                <label class="control-label col-md-2">{$L_OPTIONS}</label>
                <div class="col-md-10">
                    <div class="checkbox">
                        <label><input type="checkbox" name="is_pinned" value="1" {$chk_pin} class="icheck"> {$L_PIN}</label>
                    </div>
                    <div class="checkbox">
                        <label><input type="checkbox" name="is_locked" value="1" {$chk_lock} class="icheck"> {$L_LOCK}</label>
                    </div>
                </div>
            </div>
            
            <hr>
            <div class="form-group">
                <label class="control-label col-md-2"></label>
                <div class="col-md-10">
                    <button type="submit" class="btn bg-teal btn-raised"><i class="fa fa-save position-left"></i> {$L_SAVE}</button>
                    <a href="?mod=forum&action=topics" class="btn btn-default btn-raised">{$L_CANCEL}</a>
                </div>
            </div>
        </form>
    </div>
</div>
HTML;
        break;

    case 'pending':
        $pp      = isset($_GET['page']) ? max(1,intval($_GET['page'])) : 1;
        $pper    = 20;
        $pending = $Forum->getPendingPosts($pp, $pper);
        $ptotal  = $Forum->countPendingPosts();

        $H_TITLE = fl('forum_adm_pending_title');
        $T_USER  = fl('forum_adm_pending_user');
        $T_TOPIC = fl('forum_topic');
        $T_PREV  = fl('forum_adm_pending_preview');
        $T_DATE  = fl('forum_date');
        $B_APP   = fl('forum_adm_pending_approve');
        $B_REJ   = fl('forum_adm_pending_reject_btn');

        echo '<div class="panel panel-default">';
        echo '<div class="panel-heading"><h5><i class="fa fa-clock-o position-left"></i> ' . $H_TITLE . ' <span class="badge bg-red">' . $ptotal . '</span></h5></div>';
        if(empty($pending)) {
            echo '<div class="panel-body"><div class="alert alert-success no-border"><i class="fa fa-check-circle"></i> ' . fl('forum_adm_pending_empty') . '</div></div>';
        } else {
            echo '<div class="table-responsive"><table class="table table-striped table-hover table-xs">';
            echo '<thead><tr><th>#</th><th>' . $T_USER . '</th><th>' . $T_TOPIC . '</th><th>' . $T_PREV . '</th><th>' . $T_DATE . '</th><th class="text-center">' . $L_ACTION . '</th></tr></thead><tbody>';
            foreach($pending as $row) {
                $pid   = intval($row['id']);
                $ptid  = intval($row['topic_id']);
                $prev  = safe_out(mb_strimwidth(strip_tags($row['text']), 0, 100, '...'));
                
                $L_DEL_CONF = fl('forum_confirm_del_post');

                echo '<tr>'
                   . "<td>$pid</td>"
                   . "<td><strong>" . safe_out($row['username']) . "</strong></td>"
                   . "<td><a href='" . forum_url('forum_topic', ['newsid' => $ptid]) . "' target='_blank'>" . safe_out($row['topic_title']) . "</a></td>"
                   . "<td><small class='text-muted'>$prev</small></td>"
                   . "<td><small>" . safe_out($row['date']) . "</small></td>"
                   . "<td class='text-center' style='white-space:nowrap'>"
                   . "<form method='post' style='display:inline'>"
                   . $csrf_input
                   . "<input type='hidden' name='save_action' value='approve_post'>"
                   . "<input type='hidden' name='post_id' value='$pid'>"
                   . "<button type='submit' class='btn btn-xs bg-green btn-raised'><i class='fa fa-check'></i> {$B_APP}</button>"
                   . "</form> "
                   . "<form method='post' style='display:inline' onsubmit='return confirm(\"{$L_DEL_CONF}\")'>"
                   . $csrf_input
                   . "<input type='hidden' name='save_action' value='reject_post'>"
                   . "<input type='hidden' name='post_id' value='$pid'>"
                   . "<button type='submit' class='btn btn-xs bg-red btn-raised'><i class='fa fa-times'></i> {$B_REJ}</button>"
                   . "</form></td></tr>";
            }
            echo '</tbody></table></div>';
            $ppages = ceil($ptotal/$pper);
            if($ppages > 1) {
                echo '<div class="panel-footer text-right">';
                for($p=1;$p<=$ppages;$p++) {
                    $ac = ($p==$pp) ? 'bg-teal text-white' : 'btn-default';
                    echo "<a href='?mod=forum&action=pending&page=$p' class='btn btn-xs $ac btn-raised' style='margin:2px'>$p</a>";
                }
                echo '</div>';
            }
        }
        echo '</div>';
        break;

    default:
        $stats        = $Forum->getStats();
        $last_name    = $stats['last_user'] ? safe_out($stats['last_user']['name']) : '-';
        $s_topics     = intval($stats['total_topics']);
        $s_posts      = intval($stats['total_posts']);
        $s_users      = intval($stats['total_users']);
        $s_members    = intval($stats['forum_members']);
        $s_cats       = intval($stats['total_cats']);
        $s_likes      = intval($stats['total_likes']);
        $s_complaints = intval($stats['total_complaints']);
        $s_logs       = intval($db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_logs")['c']);

        $last_post    = $stats['last_post'];
        $lp_user      = $last_post ? safe_out($last_post['username']) : '-';
        $lp_topic     = $last_post ? safe_out($last_post['topic_title']) : '-';
        $lp_topic_id  = $last_post ? intval($last_post['topic_id']) : 0;
        $lp_date      = $last_post ? safe_out($last_post['date']) : '-';

        $recent_topics = $db->super_query(
            "SELECT t.id, t.title, t.date, t.replies, t.views, t.is_locked, t.is_pinned, u.name as username
             FROM " . PREFIX . "_forum_topics t
             LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = t.user_id
             WHERE t.is_deleted = '0'
             ORDER BY t.date DESC LIMIT 10", true
        );

        $recent_posts = $db->super_query(
            "SELECT p.id, p.date, p.topic_id, u.name as username, t.title as topic_title
             FROM " . PREFIX . "_forum_posts p
             LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = p.user_id
             LEFT JOIN " . PREFIX . "_forum_topics t ON t.id = p.topic_id
             WHERE p.is_deleted = '0'
             ORDER BY p.date DESC LIMIT 5", true
        );

        $top_users = $db->super_query(
            "SELECT name, forum_post_count, forum_points FROM " . USERPREFIX . "_users
             WHERE forum_post_count > 0 ORDER BY forum_post_count DESC LIMIT 5", true
        );

        $L_STAT_TOPICS = fl('forum_adm_stat_topics');
        $L_STAT_POSTS  = fl('forum_adm_stat_posts');
        $L_STAT_USERS  = fl('forum_adm_stat_users');
        $L_STAT_WRTR   = fl('forum_adm_stat_writers');
        $L_STAT_COMP   = fl('forum_adm_stat_complaints');
        $L_STAT_LREG   = fl('forum_adm_stat_last_reg');

        echo <<<HTML
<div class="row">
    <div class="col-md-2">
        <div class="panel panel-body bg-slate-600 has-bg-image">
            <div class="media no-margin">
                <div class="media-body"><h3 class="no-margin">{$s_topics}</h3><span class="text-uppercase text-size-mini">{$L_STAT_TOPICS}</span></div>
                <div class="media-right media-middle"><i class="fa fa-comments-o fa-3x opacity-75"></i></div>
            </div>
        </div>
    </div>
    <div class="col-md-2">
        <div class="panel panel-body bg-teal-400 has-bg-image">
            <div class="media no-margin">
                <div class="media-body"><h3 class="no-margin">{$s_posts}</h3><span class="text-uppercase text-size-mini">{$L_STAT_POSTS}</span></div>
                <div class="media-right media-middle"><i class="fa fa-reply-all fa-3x opacity-75"></i></div>
            </div>
        </div>
    </div>
    <div class="col-md-2">
        <div class="panel panel-body bg-slate-500 has-bg-image">
            <div class="media no-margin">
                <div class="media-body"><h3 class="no-margin">{$s_users}</h3><span class="text-uppercase text-size-mini">{$L_STAT_USERS}</span></div>
                <div class="media-right media-middle"><i class="fa fa-users fa-3x opacity-75"></i></div>
            </div>
        </div>
    </div>
    <div class="col-md-2">
        <div class="panel panel-body bg-green-400 has-bg-image">
            <div class="media no-margin">
                <div class="media-body"><h3 class="no-margin">{$s_members}</h3><span class="text-uppercase text-size-mini">{$L_STAT_WRTR}</span></div>
                <div class="media-right media-middle"><i class="fa fa-pencil fa-3x opacity-75"></i></div>
            </div>
        </div>
    </div>
    <div class="col-md-2">
        <div class="panel panel-body bg-orange-400 has-bg-image" style="background:#e67e22;">
            <div class="media no-margin">
                <div class="media-body"><h3 class="no-margin">{$s_complaints}</h3><span class="text-uppercase text-size-mini">{$L_STAT_COMP}</span></div>
                <div class="media-right media-middle"><i class="fa fa-flag fa-3x opacity-75"></i></div>
            </div>
        </div>
    </div>
    <div class="col-md-2">
        <div class="panel panel-body bg-purple-400 has-bg-image">
            <div class="media no-margin">
                <div class="media-body"><h6 class="no-margin">{$last_name}</h6><span class="text-uppercase text-size-mini">{$L_STAT_LREG}</span></div>
                <div class="media-right media-middle"><i class="fa fa-user-plus fa-3x opacity-75"></i></div>
            </div>
        </div>
    </div>
</div>
HTML;

        if($last_post) {
            $L_LPOST = fl('forum_adm_last_post');
            $L_LBY   = fl('forum_adm_last_post_by');
            echo "<div class='alert alert-info no-border' style='padding:10px 15px; margin-bottom:15px;'>
                <i class='fa fa-clock-o position-left'></i>
                <strong>{$L_LPOST}:</strong>
                <a href='" . forum_url('forum_topic', ['newsid' => intval($lp_topic_id)]) . "' target='_blank'>{$lp_topic}</a>
                &nbsp;&mdash;&nbsp; <strong>{$lp_user}</strong> {$L_LBY} &nbsp;&mdash;&nbsp; {$lp_date}
            </div>";
        }

        $dash_pc = $Forum->countPendingPosts();
        $dash_pending_text = ($dash_pc > 0) ? "($dash_pc)" : "";

        $L_REC_TOPICS = fl('forum_adm_recent_topics');
        $L_REC_POSTS  = fl('forum_adm_recent_posts');
        $T_TOPIC = fl('forum_topic');
        $T_AUTH  = fl('forum_adm_topics_author');
        $T_REP   = fl('forum_reply');
        $T_VIEW  = fl('forum_views');
        $T_DATE  = fl('forum_date');
        $T_STAT  = fl('forum_status');

        echo <<<HTML
<div class="row">
    <div class="col-md-8">
        <div class="panel panel-default">
            <div class="panel-heading">{$L_REC_TOPICS}</div>
            <div class="table-responsive">
                <table class="table table-striped table-xs table-hover">
                    <thead><tr><th>{$T_TOPIC}</th><th>{$T_AUTH}</th><th>{$T_REP}</th><th>{$T_VIEW}</th><th>{$T_DATE}</th><th>{$T_STAT}</th></tr></thead>
                    <tbody>
HTML;
        if($recent_topics) {
            $L_PIN = fl('forum_pin');
            $L_LCK = fl('forum_lock');
            foreach($recent_topics as $row) {
                $drid = intval($row['id']);
                $l_url = "?mod=forum&action=quick_mod&type=lock&id=$drid&hash=$dle_login_hash";
                $p_url = "?mod=forum&action=quick_mod&type=pin&id=$drid&hash=$dle_login_hash";
                
                $lock_btn = $row['is_locked'] ? '<a href="'.$l_url.'" class="label bg-red" style="font-size:9px; text-transform:uppercase;">' . $L_LCK . '</a> ' : '<a href="'.$l_url.'" class="label bg-slate" style="font-size:9px; text-transform:uppercase; opacity:0.5;">' . $L_LCK . '</a> ';
                $pin_btn  = $row['is_pinned'] ? '<a href="'.$p_url.'" class="label bg-teal" style="font-size:9px; text-transform:uppercase;">' . $L_PIN . '</a> ' : '<a href="'.$p_url.'" class="label bg-slate" style="font-size:9px; text-transform:uppercase; opacity:0.5;">' . $L_PIN . '</a> ';

                echo '<tr>'
                   . "<td><a href='" . forum_url('forum_topic', ['newsid' => $drid]) . "' target='_blank'>" . safe_out($row['title']) . "</a><br><div style='margin-top:4px;'>$pin_btn $lock_btn</div></td>"
                   . "<td><strong>" . safe_out($row['username']) . "</strong></td>"
                   . "<td><span class='badge bg-slate-600'>{$row['replies']}</span></td>"
                   . "<td><span class='badge bg-slate'>{$row['views']}</span></td>"
                   . "<td><small>" . safe_out($row['date']) . "</small></td>"
                   . "<td>"
                   . "<a href='" . forum_url('forum_topic', ['newsid' => $drid]) . "' target='_blank' class='btn btn-xs btn-default btn-raised'><i class='fa fa-eye'></i></a> "
                   . "</td></tr>";
            }
        }
        echo '</tbody></table></div></div>';

        echo <<<HTML
        <div class="panel panel-default">
            <div class="panel-heading">{$L_REC_POSTS}</div>
            <div class="table-responsive">
                <table class="table table-striped table-xs">
                    <thead><tr><th>{$T_TOPIC}</th><th>{$T_AUTH}</th><th>{$T_DATE}</th></tr></thead>
                    <tbody>
HTML;
        if($recent_posts) {
            foreach($recent_posts as $row) {
                $rp_tid = intval($row['topic_id']);
                echo "<tr>
                    <td><a href='" . forum_url('forum_topic', ['newsid' => $rp_tid]) . "' target='_blank'>" . safe_out($row['topic_title']) . "</a></td>
                    <td>" . safe_out($row['username']) . "</td>
                    <td><small>" . safe_out($row['date']) . "</small></td>
                </tr>";
            }
        }
        echo '</tbody></table></div></div>';
        echo '</div>';

        $L_TOP_USERS = fl('forum_adm_top_users');
        $L_Q_ACT     = fl('forum_adm_quick_actions');

        echo <<<HTML
    <div class="col-md-4">
        <div class="panel panel-default">
            <div class="panel-heading">{$L_TOP_USERS}</div>
            <ul class="list-group no-border">
HTML;
        if($top_users) {
            foreach($top_users as $i => $u) {
                $medal = ['🥇','🥈','🥉','4.','5.'][$i] ?? ($i+1).'.';
                echo "<li class='list-group-item'>
                    <span class='pull-right'><span class='badge bg-slate-600'>{$u['forum_post_count']}</span> <span class='badge bg-teal'>{$u['forum_points']}</span></span>
                    {$medal} <strong>" . safe_out($u['name']) . "</strong>
                </li>";
            }
        }
        echo '</ul></div>';

        $B_RECALC = fl('forum_adm_recalc');
        $B_CLEAR  = fl('forum_adm_clear_cache');

        echo <<<HTML
        <div class="panel panel-default">
            <div class="panel-heading">{$L_Q_ACT}</div>
            <div class="panel-body">
                <a href="?mod=forum&action=cats" class="btn btn-default btn-sm btn-raised btn-block" style="margin-bottom:8px;"><i class="fa fa-folder position-left"></i> {$_n2}</a>
                <a href="?mod=forum&action=ranks" class="btn btn-default btn-sm btn-raised btn-block" style="margin-bottom:8px;"><i class="fa fa-star position-left"></i> {$_n3}</a>
                <a href="?mod=forum&action=prefixes" class="btn btn-default btn-sm btn-raised btn-block" style="margin-bottom:8px;"><i class="fa fa-tag position-left"></i> {$_n10}</a>
                <a href="?mod=forum&action=tags" class="btn btn-default btn-sm btn-raised btn-block" style="margin-bottom:8px;"><i class="fa fa-tags position-left"></i> {$_n11}</a>
                <a href="?mod=forum&action=complaints" class="btn btn-default btn-sm btn-raised btn-block" style="margin-bottom:8px;"><i class="fa fa-flag position-left"></i> {$_n5} ({$s_complaints})</a>
                <a href="?mod=forum&action=logs" class="btn btn-default btn-sm btn-raised btn-block" style="margin-bottom:8px;"><i class="fa fa-shield position-left"></i> {$_n8} ({$s_logs})</a>
                <a href="?mod=forum&action=topics" class="btn btn-default btn-sm btn-raised btn-block" style="margin-bottom:8px;"><i class="fa fa-list position-left"></i> {$_n6}</a>
                <a href="?mod=forum&action=pending" class="btn btn-warning btn-sm btn-raised btn-block" style="margin-bottom:8px;"><i class="fa fa-clock-o position-left"></i> {$_n7} {$dash_pending_text}</a>
                <form method="post" style="margin-top:10px;">
                    $csrf_input
                    <input type="hidden" name="save_action" value="recalc_stats">
                    <button type="submit" class="btn btn-warning btn-sm btn-raised btn-block"><i class="fa fa-calculator position-left"></i> {$B_RECALC}</button>
                </form>
                <form method="post" style="margin-top:8px;">
                    $csrf_input
                    <input type="hidden" name="save_action" value="clear_cache">
                    <button type="submit" class="btn btn-default btn-sm btn-raised btn-block"><i class="fa fa-refresh position-left"></i> {$B_CLEAR}</button>
                </form>
            </div>
        </div>
    </div>
</div>
HTML;
        break;
}

echo <<<HTML
<div style="margin-top:30px; border-top:1px solid #e2e8f0; padding-top:20px; text-align:center; color:#64748b; font-size:12px; line-height:1.6;">
    DLE Native Forum - Nexus Ultimate Core <span style="color:#1e293b; font-weight:700;">v1.1.3</span> {$version_tag}<br>
    Developed by <a href="https://dlehub.com.tr/" target="_blank" style="font-weight:800; color:#0ea5e9; text-decoration:none;">Dlehub</a> & <span style="font-weight:800; color:#1e293b;">Elegance</span>
</div>
HTML;

echofooter();
?>
]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/forum/actions/topic.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
/*
=====================================================
 DLE Native Forum (Nexus) - by Dlehub
-----------------------------------------------------
 https://github.com/dlehub
-----------------------------------------------------
 Copyright (c) 2024-2026 Dlehub & Elegance
=====================================================
 This code is protected by copyright
=====================================================
 File: engine/forum/actions/topic.php
-----------------------------------------------------
 Use: Konu görüntüleme, cevap yazma ve moderasyon (Topic View & Actions)
=====================================================
*/
if (!defined('DATALIFEENGINE')) {
    die("Hacking attempt!");
}

$topic_id = isset($_GET['id']) ? intval($_GET['id']) : 0;
$topic    = $Forum->getTopicById($topic_id);

if (!$topic) {
    msgbox("Hata", "Konu bulunamadı veya silinmiş.");
    return;
}

// --- OKUNDU İŞARETLE ---
if($is_logged) {
    $Forum->markAsRead($topic_id, intval($topic['last_post_id'] ?? 0));
}

// --- GET İŞLEMLERİ TEMİZLENDİ (Moderasyon artık AJAX POST ile yapılıyor) ---

$parse          = new ParseFilter();
$parse->safe_mode = true;
$parse->edit_mode = false;
$parse->wysiwyg   = false;

// --- Yasaklı Kelimeler ---
$banned_words = dle_cache("forum_banned_words", $config['skin']);
if (!$banned_words) {
    $banned_words = $db->super_query("SELECT word, replacement FROM " . PREFIX . "_forum_banned_words", true);
    create_cache("forum_banned_words", serialize($banned_words), $config['skin']);
} else {
    $banned_words = unserialize($banned_words);
}

function applyBannedWords($text, $banned_words) {
    if (!is_string($text)) {
        return '';
    }
    foreach ($banned_words as $bw) {
        $word        = (isset($bw['word']) && is_string($bw['word'])) ? $bw['word'] : '';
        $replacement = (isset($bw['replacement']) && is_string($bw['replacement']) && $bw['replacement'] !== '') ? $bw['replacement'] : '***';
        if ($word !== '') {
            $text = str_ireplace($word, $replacement, $text);
        }
    }
    return $text;
}

// --- Düzenleme Hazırlığı ---
$edit_post_id  = 0;
$edit_post_text = '';

if (isset($_GET['edit_post']) && $is_logged) {
    $ep_id = intval($_GET['edit_post']);
    $ep    = $db->super_query("SELECT text, user_id FROM " . PREFIX . "_forum_posts WHERE id='{$ep_id}' AND topic_id='{$topic_id}' AND is_deleted='0'");
    if ($ep && ($ep['user_id'] == $member_id['user_id'] || $Forum->canDo('moderate'))) {
        $edit_post_id   = $ep_id;
        $edit_post_text = $ep['text'];
    }
}

// --- CEVAP GÖNDERME (POST) ---
$reply_error = '';

if (isset($_POST['submit_reply']) && $is_logged) {
    if (!$Forum->verifyCsrf($_POST['csrf_token'] ?? '')) {
        $reply_error = "Güvenlik doğrulaması başarısız!";
    } elseif ($topic['is_locked'] && !$Forum->canDo('moderate')) {
        $reply_error = "Bu konu kilitli!";
    } else {
        // PHP 8 ZIRHI: $_POST değerleri her zaman string olmayabilir,
        // dizi (array) geldiğinde trim/mb_strlen Fatal Error verir.
        $raw_content = isset($_POST['comments']) ? $_POST['comments'] : (isset($_POST['reply_content']) ? $_POST['reply_content'] : '');

        if (!is_string($raw_content)) {
            $reply_error = fl('forum_err_msg_format');
            $raw_content = '';
        } else {
            $reply_content = applyBannedWords($raw_content, $banned_words);

            $edit_pid = isset($_POST['edit_post_id']) ? intval($_POST['edit_post_id']) : 0;

            if ($edit_pid > 0) {
                // Düzenleme: banned words zaten yukarıda applyBannedWords ile uygulanmış.
                $clean_text = trim(str_replace(' ', '', strip_tags(html_entity_decode($reply_content, ENT_QUOTES, 'UTF-8'))));
                $clean_len  = mb_strlen($clean_text, 'UTF-8');

                if ($clean_len < 3) {
                    $reply_error = fl('forum_err_msg_len');
                } else {
                    $op = $Forum->editPost($edit_pid, $reply_content);
                    if (isset($op['error'])) {
                        $reply_error = $op['error'];
                    } else {
                        header("Location: " . forum_url('forum_topic', ['newsid' => $topic_id]) . "#post-" . $edit_pid);
                        exit;
                    }
                }
            } else {
                // Yeni cevap: banned words zaten yukarıda uygulanmış.
                $result = $Forum->addReply($topic_id, $reply_content);
                if (isset($result['error'])) {
                    $reply_error = $result['error'];
                } elseif (isset($result['pending'])) {
                    $reply_error = '<span style="color:#10b981">✓ ' . $result['message'] . '</span>';
                } else {
                    header("Location: " . forum_url('forum_topic', ['newsid' => $topic_id]) . "#post-" . $result['post_id']);
                    exit;
                }
            }
        }
    }
}

// --- View Sayacı (Anti-Refresh: Cookie Tabanlı 1 Saat Zamanlayıcı) ---
$view_cookie_name = 'forum_view_' . $topic_id;
$view_cookie_seen = false;

if (isset($_COOKIE[$view_cookie_name]) && is_string($_COOKIE[$view_cookie_name])) {
    $cookie_timestamp = intval($_COOKIE[$view_cookie_name]);
    if ((time() - $cookie_timestamp) < 3600) {
        $view_cookie_seen = true;
    }
}

if (!$view_cookie_seen) {
    $db->query("UPDATE " . PREFIX . "_forum_topics SET views=views+1 WHERE id='{$topic_id}'");
    setcookie($view_cookie_name, (string) time(), time() + 3600, '/', '', false, true);
}

// --- Okundu İşaretle ---
$last_post_row = $db->super_query("SELECT id FROM " . PREFIX . "_forum_posts WHERE topic_id='{$topic_id}' AND is_deleted='0' ORDER BY id DESC LIMIT 1");
if ($last_post_row && $is_logged) {
    $Forum->markRead($topic_id, $last_post_row['id']);
}

// --- Takip Durumu ---
$is_following = $is_logged ? $Forum->isFollowing($topic_id) : false;

// --- SAYFALAMA ---
$posts_per_page = intval($Forum->getSetting('posts_per_page') ?: 20);
$current_page   = isset($_GET['page']) ? max(1, intval($_GET['page'])) : 1;
$total_posts    = intval($db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_posts WHERE topic_id='{$topic_id}' AND is_deleted='0'")['c']);
$total_pages    = max(1, ceil($total_posts / $posts_per_page));
$offset         = ($current_page - 1) * $posts_per_page;

// --- MESAJLARI ÇEK ---
$posts = $db->super_query(
    "SELECT p.*, u.name as username, u.foto as avatar, u.user_group, u.signature, u.forum_post_count, u.forum_points,
     r.title as rank_title, r.color as rank_color, r.icon as rank_icon
     FROM " . PREFIX . "_forum_posts p
     LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = p.user_id
     LEFT JOIN " . PREFIX . "_forum_ranks r ON r.id = u.forum_rank_id
     WHERE p.topic_id = '{$topic_id}' AND p.is_deleted = '0'
     ORDER BY p.date ASC LIMIT {$offset}, {$posts_per_page}",
    true
);

$post_ids = array();
foreach ($posts as $p) {
    $post_ids[] = intval($p['id']);
}
$post_ids_str = count($post_ids) > 0 ? implode(',', $post_ids) : '0';

// 1. Ekler
$attachments_raw     = $db->super_query("SELECT * FROM " . PREFIX . "_forum_attachments WHERE post_id IN ({$post_ids_str}) ORDER BY id ASC", true);
$attachments_grouped = array();
if ($attachments_raw) {
    foreach ($attachments_raw as $att) {
        $attachments_grouped[$att['post_id']][] = $att;
    }
}

// 2. Beğeniler
$user_likes = array();
if ($is_logged) {
    $likes_raw = $db->super_query("SELECT post_id, type FROM " . PREFIX . "_forum_likes WHERE post_id IN ({$post_ids_str}) AND user_id='" . intval($member_id['user_id']) . "'", true);
    if ($likes_raw) {
        foreach ($likes_raw as $l) {
            $user_likes[$l['post_id']] = $l['type'];
        }
    }
}

$noavatar  = rtrim($config['http_home_url'], '/') . '/templates/' . $config['skin'] . '/dleimages/noavatar.png';
$posts_html = '';
$csrf      = $Forum->csrfToken();
$ajax_url  = forum_url('forum_ajax');

// ============================================================
// JAVASCRIPT - Tüm frontend fonksiyonları
// ============================================================
$posts_html .= '<script>
var FORUM_CSRF = "' . $csrf . '";
var FORUM_AJAX = "' . $ajax_url . '";
var FORUM_TOPIC = ' . $topic_id . ';
var FORUM_CONFIRM_DEL_POST = ' . json_encode(fl('forum_confirm_del_post'), JSON_UNESCAPED_UNICODE) . ';

function forumToast(msg, type) {
    var colors = {
        success: {bg:"#10b981", icon:"OK"},
        error:   {bg:"#ef4444", icon:"X"},
        info:    {bg:"#475569", icon:"i"},
        warning: {bg:"#f59e0b", icon:"!"}
    };
    var c = colors[type] || colors.info;
    var wrap = document.getElementById("df-toast-wrap");
    if (!wrap) {
        wrap = document.createElement("div");
        wrap.id = "df-toast-wrap";
        wrap.style.cssText = "position:fixed;top:20px;right:20px;z-index:99999;display:flex;flex-direction:column;gap:8px;pointer-events:none;";
        document.body.appendChild(wrap);
    }
    var t = document.createElement("div");
    t.style.cssText = "display:flex;align-items:center;gap:10px;padding:12px 18px;border-radius:12px;background:" + c.bg + ";color:#fff;font-size:13px;font-weight:700;box-shadow:0 8px 24px rgba(0,0,0,0.25);opacity:0;transform:translateX(40px);transition:all .3s ease;pointer-events:auto;max-width:320px;";
    t.innerHTML = "<span style=\'width:20px;height:20px;border-radius:50%;background:rgba(255,255,255,0.25);display:inline-flex;align-items:center;justify-content:center;font-size:11px;flex-shrink:0;\'" + ">" + c.icon + "</span><span>" + msg + "</span>";
    wrap.appendChild(t);
    requestAnimationFrame(function() {
        t.style.opacity = "1";
        t.style.transform = "translateX(0)";
    });
    setTimeout(function() {
        t.style.opacity = "0";
        t.style.transform = "translateX(40px)";
        setTimeout(function() {
            if (t.parentNode) {
                t.parentNode.removeChild(t);
            }
        }, 350);
    }, 3000);
}

function forumLike(postId, ltype, btn) {
    btn.disabled = true;
    var parent = btn.parentNode;
    var allBtns = parent ? parent.querySelectorAll("button[data-type]") : [];
    fetch(FORUM_AJAX, {
        method: "POST",
        headers: {"Content-Type": "application/x-www-form-urlencoded"},
        body: "action=ajax&act=like&post_id=" + postId + "&ltype=" + ltype + "&csrf=" + FORUM_CSRF
    }).then(function(r) {
        return r.json();
    }).then(function(d) {
        btn.disabled = false;
        if (d.error) {
            forumToast(d.error, "error");
            return;
        }
        allBtns.forEach(function(x) {
            if (x.getAttribute("data-type") === "like") {
                x.querySelector(".like-count").textContent = d.likes;
                x.classList.toggle("active-like", d.status === "liked");
            } else {
                x.querySelector(".dislike-count").textContent = d.dislikes;
                x.classList.toggle("active-dislike", d.status === "disliked");
            }
        });
        if (d.status === "liked") {
            forumToast("Mesaj be\u011fenildi!", "success");
        } else if (d.status === "disliked") {
            forumToast("Mesaj be\u011fenilmedi.", "warning");
        } else {
            forumToast("Oy kald\u0131r\u0131ld\u0131.", "info");
        }
    }).catch(function() {
        btn.disabled = false;
        forumToast("Ba\u011flant\u0131 hatas\u0131!", "error");
    });
}

function forumFollow(btn) {
    fetch(FORUM_AJAX, {
        method: "POST",
        headers: {"Content-Type": "application/x-www-form-urlencoded"},
        body: "action=ajax&act=follow&topic_id=" + FORUM_TOPIC + "&csrf=" + FORUM_CSRF
    }).then(function(r) {
        return r.json();
    }).then(function(d) {
        if (d.error) {
            forumToast(d.error, "error");
            return;
        }
        var f = d.status === "followed";
        btn.classList.toggle("following", f);
        btn.querySelector(".follow-text").textContent = f ? "Takipten \u00c7\u0131k" : "Takip Et";
        forumToast(f ? "Konu takibe al\u0131nd\u0131." : "Konu takibi b\u0131rak\u0131ld\u0131.", "info");
    }).catch(function() {
        forumToast("Ba\u011flant\u0131 hatas\u0131!", "error");
    });
}

// Alıntı fonksiyonu - PHP tarafında json_encode ile sanitize edilmiş veriler gelir.
function forumQuote(username, postText) {
    var quotedText = "[quote=" + username + "]" + postText + "[/quote]\\n";
    if (typeof tinymce !== "undefined" && tinymce.get("comments")) {
        tinymce.get("comments").execCommand("mceInsertContent", false, quotedText);
    } else {
        var area = document.getElementById("comments");
        if (area) {
            area.value += quotedText;
            area.focus();
        }
    }
    var el = document.getElementById("reply");
    if (el) {
        el.scrollIntoView({ behavior: "smooth" });
    }
}

function loadNotifCount() {
    fetch(FORUM_AJAX, {
        method: "POST",
        headers: {"Content-Type": "application/x-www-form-urlencoded"},
        body: "action=ajax&act=notif_count&csrf=" + FORUM_CSRF
    }).then(function(r) {
        return r.json();
    }).then(function(d) {
        var b = document.getElementById("df-notif-badge");
        if (b) {
            b.textContent = d.count || 0;
            b.style.display = d.count > 0 ? "inline-flex" : "none";
        }
    });
}

document.addEventListener("DOMContentLoaded", function() {
    if (typeof FORUM_CSRF !== "undefined") {
        loadNotifCount();
    }
});

function forumBump(btn) {
    btn.disabled = true;
    btn.innerHTML = "<i class=\'fa fa-spinner fa-spin\'></i>";
    fetch(FORUM_AJAX, {
        method: "POST",
        headers: {"Content-Type": "application/x-www-form-urlencoded"},
        body: "action=ajax&act=bump_topic&topic_id=" + FORUM_TOPIC + "&csrf=" + FORUM_CSRF
    }).then(function(r) {
        return r.json();
    }).then(function(d) {
        btn.disabled = false;
        btn.innerHTML = "<i class=\'fa fa-arrow-up\'></i> Bump";
        if (d.error) {
            forumToast(d.error, "error");
        } else {
            forumToast(d.message || "Konu yukar\u0131 ta\u015f\u0131nd\u0131!", "success");
        }
    }).catch(function() {
        btn.disabled = false;
        btn.innerHTML = "<i class=\'fa fa-arrow-up\'></i> Bump";
        forumToast("Ba\u011flant\u0131 hatas\u0131!", "error");
    });
}

// Konu Kilitleme/Sabitleme AJAX Köprüsü
function forumMod(action, state) {
    fetch(FORUM_AJAX, {
        method: "POST",
        headers: {"Content-Type": "application/x-www-form-urlencoded"},
        body: "action=ajax&act=" + action + "&topic_id=" + FORUM_TOPIC + "&state=" + state + "&csrf=" + FORUM_CSRF
    }).then(r => r.json()).then(d => {
        if(d.error) { forumToast(d.error, "error"); }
        else { 
            forumToast("'.fl('forum_js_mod_ok').'", "success");
            setTimeout(() => location.reload(), 1000); 
        }
    });
}

// Konu Silme AJAX Köprüsü
function forumDeleteTopic() {
    if(!confirm("'.fl('forum_confirm_del_topic').'")) return;
    
    fetch(FORUM_AJAX, {
        method: "POST",
        headers: {"Content-Type": "application/x-www-form-urlencoded"},
        body: "action=ajax&act=delete_topic&topic_id=" + FORUM_TOPIC + "&csrf=" + FORUM_CSRF
    }).then(r => r.json()).then(d => {
        if(d.success) {
            forumToast("'.fl('forum_js_del_ok').'", "success");
            setTimeout(() => location.href = "index.php?do=forum", 1200);
        } else {
            forumToast(d.error || "'.fl('forum_error').'", "error");
        }
    });
}

// Mesaj Silme AJAX Köprüsü
function forumDeletePost(postId) {
    if(!confirm(FORUM_CONFIRM_DEL_POST)) return;
    
    fetch(FORUM_AJAX, {
        method: "POST",
        headers: {"Content-Type": "application/x-www-form-urlencoded"},
        body: "action=ajax&act=delete_post&post_id=" + postId + "&csrf=" + FORUM_CSRF
    }).then(r => r.json()).then(d => {
        if(d.success) {
            forumToast("' . fl('forum_js_msg_del_ok') . '", "success");
            setTimeout(() => location.reload(), 1000);
        } else {
            forumToast(d.error || "Hata oluştu", "error");
        }
    });
}
</script>
<style>.active-like { color: #f59e0b !important; } .active-dislike { color: #ef4444 !important; } .following { color: #10b981 !important; }
/* Nexus Sticky OP Styles */
.df-post-sticky { border: 1px solid var(--df-accent, #3b82f6) !important; background: rgba(59,130,246,0.05); margin-bottom: 25px !important; position: relative; border-radius: 8px; }
.df-sticky-label { background: var(--df-accent, #3b82f6); color: #fff; font-size: 10px; font-weight: 800; padding: 4px 12px; display: inline-block; border-radius: 0 0 8px 0; text-transform: uppercase; letter-spacing: 0.5px; }
.df-op-badge { display: inline-block; margin-left: 6px; }
</style>';

// ============================================================
// MESAJ HTML OLUŞTURMA DÖNGÜSÜ
// ============================================================
$post_counter  = $offset;
$is_first_page = ($offset == 0);

foreach ($posts as $idx => $p) {
    $post_counter++;
    $p_id   = intval($p['id']);
    $p_uid  = intval($p['user_id']);
    $p_user = htmlspecialchars($p['username'] ? $p['username'] : fl('forum_guest'), ENT_QUOTES, 'UTF-8');
    $p_date = date('d.m.Y, H:i', strtotime($p['date']));

    if ($p['updated_at']) {
        $p_updated = '<span class="df-edited-hint">(düzenlendi: ' . date('d.m.Y H:i', strtotime($p['updated_at'])) . ')</span>';
    } else {
        $p_updated = '';
    }

    $p_avatar  = $p['avatar'] ? rtrim($config['http_home_url'], '/') . '/' . $p['avatar'] : $noavatar;
    $p_profile = $config['http_home_url'] . 'user/' . urlencode($p['username'] ? $p['username'] : fl('forum_guest')) . '/';
    $is_op     = ($is_first_page && $idx === 0);

    // Grup renkleri
    $group_id    = intval($p['user_group']);
    $group_title = 'Üye';
    $group_color = '#475569';

    if ($group_id == 1) {
        $group_title = 'Yönetici';
        $group_color = '#e11d48';
    } elseif ($group_id == 2) {
        $group_title = 'Editör';
        $group_color = '#d97706';
    } elseif ($group_id == 3) {
        $group_title = 'Yazar';
        $group_color = '#059669';
    }

    // Forum rütbe badge'i
    $rank_html = '';
    if (!empty($p['rank_title'])) {
        $rc        = htmlspecialchars($p['rank_color'] ? $p['rank_color'] : '#475569', ENT_QUOTES, 'UTF-8');
        $rank_icon = htmlspecialchars($p['rank_icon'] ? $p['rank_icon'] : 'fa-user', ENT_QUOTES, 'UTF-8');
        $rank_name = htmlspecialchars($p['rank_title'], ENT_QUOTES, 'UTF-8');
        $rank_html = '<span class="df-rank-badge" style="color:' . $rc . ';border-color:' . $rc . '30;background:' . $rc . '15"><i class="fa ' . $rank_icon . '"></i>' . $rank_name . '</span>';
    }

    // XSS: safe_mode=true zaten ayarlandı, ek güvence olarak allow_html=false
    $p_text = $parse->BB_Parse(stripslashes($p['text']), false);

    $p_sig = '';
    if (!empty($p['signature'])) {
        $p_sig = '<div class="df-signature">' . $parse->BB_Parse(stripslashes($p['signature']), false) . '</div>';
    }

    // --- Ekler ---
    $attachments_html = '';
    if (isset($attachments_grouped[$p_id]) && is_array($attachments_grouped[$p_id])) {
        $atts = $attachments_grouped[$p_id];
        $attachments_html .= '<div class="df-attachments"><div class="df-attach-label"><i class="fa fa-paperclip mr-1"></i> Ekler (' . count($atts) . ')</div><div class="df-attach-images">';
        foreach ($atts as $att) {
            $att_url  = $config['http_home_url'] . 'uploads/forum/' . $att['filename'];
            $att_name = htmlspecialchars($att['original_name'], ENT_QUOTES, 'UTF-8');
            $att_size = round($att['filesize'] / 1024, 1) . ' KB';

            if ($att['is_image']) {
                $attachments_html .= '<a href="' . $att_url . '" class="df-attach-img-link"><img src="' . $att_url . '" class="df-attach-img" alt="' . $att_name . '"></a>';
            } else {
                $attachments_html .= '<a href="' . $att_url . '" target="_blank" class="df-attach-file"><i class="fa fa-download"></i> ' . $att_name . ' <span style="color:var(--df-text-muted)">(' . $att_size . ')</span></a>';
            }
        }
        $attachments_html .= '</div></div>';
    }

    // --- Beğeni / Dislike Butonları ---
    $like_btn = '';
    $my_vote  = isset($user_likes[$p_id]) ? $user_likes[$p_id] : '';

    if ($is_logged && $p_uid != intval($member_id['user_id'])) {
        $like_active  = ($my_vote === 'like') ? ' active-like' : '';
        $dis_active   = ($my_vote === 'dislike') ? ' active-dislike' : '';
        $likes_count  = intval($p['likes']);
        $dislike_count = intval($p['dislikes']);
        $like_btn     = '<button onclick="forumLike(' . $p_id . ',\'like\',this)" data-type="like" class="df-btn-action df-btn-like' . $like_active . '"><i class="fa fa-thumbs-up"></i> <span class="like-count">' . $likes_count . '</span></button>';
        $like_btn    .= '<button onclick="forumLike(' . $p_id . ',\'dislike\',this)" data-type="dislike" class="df-btn-action df-btn-like' . $dis_active . '"><i class="fa fa-thumbs-down"></i> <span class="dislike-count">' . $dislike_count . '</span></button>';
    } elseif (!$is_logged) {
        $like_btn  = '<span class="df-vote-display"><i class="fa fa-thumbs-up"></i> ' . intval($p['likes']) . '</span>';
        $like_btn .= '<span class="df-vote-display"><i class="fa fa-thumbs-down"></i> ' . intval($p['dislikes']) . '</span>';
    }

    $action_btns = $like_btn;
    $action_btns .= ' <a href="javascript:void(0)" onclick="AddComplaint(\'' . $p_id . '\',\'forum\')" class="df-btn-action" title="Şikayet"><i class="fa fa-flag"></i></a>';

    // --- Giriş Yapmış Kullanıcı Aksiyonları ---
    if ($is_logged) {
        // XSS KORUMASI: json_encode tüm özel karakterleri kaçıştırır.
        // JSON_HEX_TAG      -> < ve > encode eder (HTML enjeksiyonu önler)
        // JSON_HEX_AMP      -> & encode eder (HTML entity çakışması önler)
        // JSON_HEX_APOS     -> ' encode eder (onclick tırnak patlaması önler)
        // JSON_HEX_QUOT     -> " encode eder (çift tırnak escape)
        $js_text_for_quote = strip_tags(str_replace(array("\r", "\n"), " ", $p['text']));
        $js_text_json      = json_encode($js_text_for_quote, JSON_HEX_TAG | JSON_HEX_AMP | JSON_HEX_APOS | JSON_HEX_QUOT);
        $js_user_json      = json_encode($p_user, JSON_HEX_TAG | JSON_HEX_AMP | JSON_HEX_APOS | JSON_HEX_QUOT);

        // json_encode false dönerse (örn. derin iç içe dizi), boş string ata.
        if ($js_text_json === false) {
            $js_text_json = '""';
        }
        if ($js_user_json === false) {
            $js_user_json = '""';
        }

        $onclick_attr = htmlspecialchars("forumQuote(" . $js_user_json . ", " . $js_text_json . ")", ENT_QUOTES, 'UTF-8');
        $action_btns .= ' <button onclick="' . $onclick_attr . '" class="df-btn-action" title="Alıntı"><i class="fa fa-quote-left"></i></button>';

            // Düzenle ve Sil butonları (sahibi veya moderatör)
        if ($p_uid == intval($member_id['user_id']) || $Forum->canDo('moderate')) {
            $action_btns .= ' <a href="' . forum_fix_url(forum_url('forum_topic', ['newsid' => $topic_id]), 'edit_post=' . $p_id) . '#reply" class="df-btn-action" title="Düzenle"><i class="fa fa-pencil"></i></a>';
            $action_btns .= ' <a href="javascript:void(0)" onclick="forumDeletePost(' . $p_id . ')" class="df-btn-action" title="Sil"><i class="fa fa-trash"></i></a>';
        }
    }

    $post_class = $is_op ? ' df-post-op' : '';

    // --- Özel Mesaj Butonu ---
    $pm_btn_html = '';
    if ($is_logged && $p_uid != intval($member_id['user_id'])) {
        $sender_group = intval($member_id['user_id']);
        if (isset($user_group[$member_id['user_group']]['allow_pm']) && $user_group[$member_id['user_group']]['allow_pm']) {
            if (isset($user_group[$group_id]['allow_pm']) && $user_group[$group_id]['allow_pm']) {
                $pm_link     = forum_fix_url(forum_url('forum_pm'), 'sub=compose&to=' . urlencode($p_user));
                $pm_btn_html = '<a href="' . $pm_link . '" class="df-btn-pm"><i class="fa fa-envelope"></i> Özel Mesaj</a>';
            }
        }
    }

    // --- Mesaj HTML Bloğu ---
    $posts_html .= '
    <div id="post-' . $p_id . '" class="df-post' . $post_class . '">
        <div class="df-post-user">
            <div class="df-user-panel">
                <img src="' . $p_avatar . '" class="df-avatar">
                <a href="' . $p_profile . '" class="df-post-username">' . $p_user . '</a>
                <div class="df-user-badges">
                    <span class="df-group-badge" style="background:' . $group_color . '">' . $group_title . '</span>
                    ' . $rank_html . '
                </div>
                <div class="df-user-stats">
                    <span><i class="fa fa-comment-o"></i> ' . intval($p['forum_post_count']) . '</span>
                    <span><i class="fa fa-star-o"></i> ' . intval($p['forum_points']) . '</span>
                </div>
                ' . $pm_btn_html . '
            </div>
        </div>

        <div class="df-post-main">
            <div class="df-post-header">
                <span class="df-post-date"><i class="fa fa-clock-o"></i> ' . $p_date . '</span>
                ' . $p_updated . '
                <span class="df-post-num">#' . $post_counter . '</span>
            </div>
            <div class="df-post-content">
                ' . $p_text . '
                ' . $attachments_html . '
            </div>
            ' . $p_sig . '
            <div class="df-post-footer">
                ' . $action_btns . '
            </div>
        </div>
    </div>';
}

// ============================================================
// SAYFALAMA
// ============================================================
$pagination_html = '';
if ($total_pages > 1) {
    $pagination_html = '<div class="df-pagination">';

    if ($current_page > 1) {
        $pagination_html .= '<a href="' . forum_fix_url(forum_url('forum_topic', ['newsid' => $topic_id]), 'page=' . ($current_page - 1)) . '" class="df-page-btn"><i class="fa fa-chevron-left"></i></a>';
    }

    for ($i = 1; $i <= $total_pages; $i++) {
        $active_cls = ($i == $current_page) ? ' active' : '';
        $pagination_html .= '<a href="' . forum_fix_url(forum_url('forum_topic', ['newsid' => $topic_id]), 'page=' . $i) . '" class="df-page-btn' . $active_cls . '">' . $i . '</a>';
    }

    if ($current_page < $total_pages) {
        $pagination_html .= '<a href="' . forum_fix_url(forum_url('forum_topic', ['newsid' => $topic_id]), 'page=' . ($current_page + 1)) . '" class="df-page-btn"><i class="fa fa-chevron-right"></i></a>';
    }

    $pagination_html .= '</div>';
}

// ============================================================
// YÖNETİM TOOLBAR (Moderatör)
// ============================================================
$admin_toolbar = '';

if ($is_logged && $Forum->canDo('moderate')) {
    $lock_val  = $topic['is_locked'] ? 0 : 1;
    $lock_icon = $topic['is_locked'] ? 'fa-unlock' : 'fa-lock';
    $lock_text = $topic['is_locked'] ? fl('forum_unlock') : fl('forum_lock');
    $pin_val   = $topic['is_pinned'] ? 0 : 1;
    $pin_text  = $topic['is_pinned'] ? fl('forum_unpin') : fl('forum_pin');
    $hash_cr   = $Forum->csrfToken();

    $admin_toolbar = '<div class="df-admin-bar">
        <span class="df-admin-label"><i class="fa fa-shield"></i> ' . fl('forum_admin_label') . '</span>
        <button onclick="forumMod(\'lock_topic\', ' . $lock_val . ')" class="df-btn-mod"><i class="fa ' . $lock_icon . '"></i> ' . $lock_text . '</button>
        <button onclick="forumMod(\'pin_topic\', ' . $pin_val . ')" class="df-btn-mod"><i class="fa fa-thumb-tack"></i> ' . $pin_text . '</button>
        <button onclick="forumDeleteTopic()" class="df-btn-mod df-btn-mod-danger"><i class="fa fa-trash"></i> ' . fl('forum_delete_topic') . '</button>
    </div>';
}

// ============================================================
// UYARI ve HATA BLOKLARI
// ============================================================
$lock_notice = '';
if ($topic['is_locked']) {
    $lock_notice = '<div class="df-alert df-alert-error"><i class="fa fa-lock"></i> Bu konu kilitlendi.</div>';
}

$reply_error_block = '';
if (!empty($reply_error)) {
    $reply_error_block = '<div class="df-alert df-alert-error"><i class="fa fa-exclamation-triangle"></i> ' . $reply_error . '</div>';
}

// ============================================================
// BUMP ve TAKİP BUTONLARI
// ============================================================
$bump_btn = '';
if ($is_logged && !$topic['is_locked'] && ($topic['user_id'] == intval($member_id['user_id']) || $Forum->canDo('moderate'))) {
    $bump_btn = '<button onclick="forumBump(this)" data-topic="' . $topic_id . '" class="df-btn-action"><i class="fa fa-arrow-up"></i> Bump</button>';
}

$follow_btn = '';
if ($is_logged) {
    $f_cls  = $is_following ? ' following' : '';
    $f_text = $is_following ? 'Takipten Çık' : 'Takip Et';
    $f_icon = $is_following ? 'fa-bell-slash' : 'fa-bell';
    $follow_btn = '<button onclick="forumFollow(this)" class="df-btn-follow' . $f_cls . '"><i class="fa ' . $f_icon . '"></i><span class="follow-text">' . $f_text . '</span></button>';
}

// ============================================================
// BİLDİRİM BUTONU
// ============================================================
$notif_btn = '';
if ($is_logged) {
    $notif_count = intval($Forum->countUnreadNotifs());
    $notif_btn   = '<a href="' . forum_url('forum_notifications') . '" class="df-btn-notif" style="position:relative;display:inline-flex;"><i class="fa fa-bell"></i><span id="df-notif-badge" class="df-notif-badge" style="display:' . ($notif_count > 0 ? 'inline-flex' : 'none') . '">' . $notif_count . '</span></a>';
}

// ============================================================
// TEMPLATE YÜKLEME ve DEĞİŞKEN ATAMA
// ============================================================
$tpl->load_template('forum/topic.tpl');
$upload_form = '';

if ($is_logged && (!$topic['is_locked'] || $Forum->canDo('moderate'))) {
    // TinyMCE editör yükle
    $onload_before = isset($onload_scripts) ? count($onload_scripts) : 0;
    ob_start();
    include(DLEPlugins::Check(ENGINE_DIR . '/editor/comments.php'));
    $editor_html = $wysiwyg;
    ob_end_clean();
    $onload_after = isset($onload_scripts) ? count($onload_scripts) : 0;

    // TinyMCE script manuel yükleme (sayfa modülde yüklü değilse)
    if ($onload_after > $onload_before) {
        $tinymce_url    = $config['http_home_url'] . 'public/editor/tiny_mce/tinymce.min.js';
        $dlebutton_url  = $config['http_home_url'] . 'public/editor/tiny_mce/plugins/dlebutton/plugin.min.js';
        $editor_html   .= '<script>if(typeof tinyMCE==="undefined"){var s=document.createElement("script");s.src="' . $tinymce_url . '";s.onload=function(){var s2=document.createElement("script");s2.src="' . $dlebutton_url . '";s2.onload=function(){';
        for ($si = $onload_before; $si < $onload_after; $si++) {
            $editor_html .= $onload_scripts[$si];
        }
        $editor_html .= '};document.head.appendChild(s2);};document.head.appendChild(s);}else{';
        for ($si = $onload_before; $si < $onload_after; $si++) {
            $editor_html .= $onload_scripts[$si];
        }
        $editor_html .= '}</script>';
    }

    // Düzenleme gizli alanı
    $edit_hidden = '<input type="hidden" name="edit_post_id" value="' . $edit_post_id . '">';

    // Düzenleme modu: editör içeriğini TinyMCE'ye yükle
    if ($edit_post_id > 0 && $edit_post_text) {
        $safe_text_js = json_encode($edit_post_text, JSON_UNESCAPED_UNICODE);
        $editor_html .= '<script>document.addEventListener("DOMContentLoaded",function(){var i=setInterval(function(){if(typeof tinymce!="undefined"&&tinymce.activeEditor&&tinymce.activeEditor.initialized){tinymce.activeEditor.setContent(' . $safe_text_js . ');clearInterval(i);}},200);});</script>';
    }

    // Dosya yükleme formu (sadece yeni cevap, düzenleme değil)
    if ($edit_post_id == 0 && intval($Forum->getSetting('allow_attachments'))) {
        $max_mb      = round(intval($Forum->getSetting('max_attachment_size') ?: 5242880) / 1048576, 1);
        $upload_form = '<div class="df-upload-wrap"><label class="df-field-label"><i class="fa fa-paperclip"></i> Dosya Ekle (max ' . $max_mb . ' MB)</label><input type="file" id="forum-file-input" name="forum_file[]" multiple class="df-upload-input"><div id="forum-upload-preview" class="df-upload-preview"></div></div>';
    }

    $csrf_input = '<input type="hidden" name="csrf_token" value="' . $csrf . '">';
    $tpl->set('{editor}', $edit_hidden . $csrf_input . $editor_html);
    $tpl->set('[logged]', '');
    $tpl->set('[/logged]', '');
    $tpl->set_block("'\\[not-logged\\](.*?)\\[/not-logged\\]'is", "");
    $tpl->set('{editor_title}', ($edit_post_id > 0 ? fl('forum_edit_msg') : fl('forum_write_reply')));
} else {
    $tpl->set('{editor}', '');
    $tpl->set('[not-logged]', '');
    $tpl->set('[/not-logged]', '');
    $tpl->set_block("'\\[logged\\](.*?)\\[/logged\\]'is", "");
    $tpl->set('{editor_title}', ($topic['is_locked'] ? fl('forum_topic_locked_t') : fl('forum_err_must_login_short')));
}

// ============================================================
// TÜM TEMPLATE ETİKETLERİ
// ============================================================
// Prefix
$topic_prefix_html = '';
if(!empty($topic['prefix_id'])) {
    $t_pf = $Forum->getPrefixById(intval($topic['prefix_id']));
    if($t_pf) $topic_prefix_html = $Forum->formatPrefix($t_pf);
}
$tpl->set('{topic_prefix}', $topic_prefix_html);
$tpl->set('{topic_title}', htmlspecialchars($topic['title'], ENT_QUOTES, 'UTF-8'));

// Etiketler
$topic_tags     = $Forum->getTopicTags($topic_id);
$topic_tags_html = '';
if($topic_tags) {
    $topic_tags_html = '<div class="df-tags-bar" style="margin:8px 0;">';
    foreach($topic_tags as $tg) {
        $tg_slug = htmlspecialchars($tg['slug']);
        $tg_name = htmlspecialchars($tg['name']);
        $topic_tags_html .= '<a href="' . forum_url('forum_main') . '?action=tag&slug=' . $tg_slug . '" class="df-tag" style="display:inline-block;font-size:11px;padding:2px 8px;border-radius:12px;background:#e2e8f0;color:#475569;margin-right:4px;text-decoration:none;">#' . $tg_name . '</a>';
    }
    $topic_tags_html .= '</div>';
}
$tpl->set('{topic_tags}', $topic_tags_html);
$topic_desc_html = !empty($topic['description']) ? '<div class="df-topic-description" style="font-size:14px; color:#475569; margin-bottom:15px; padding-bottom:10px; border-bottom:1px dashed #cbd5e1;">' . htmlspecialchars($topic['description'], ENT_QUOTES, 'UTF-8') . '</div>' : '';
$tpl->set('{topic_description}', $topic_desc_html);
$tpl->set('{topic_id}', $topic_id);

// v1.1.3 Sticky OP - İlk mesajı sabitleme (DLE Cache Entegrasyonlu)
$sticky_html = '';
$is_sticky_enabled = intval($Forum->getSetting('sticky_op') ?: 0);

if ($current_page > 1 && $is_sticky_enabled) {
    $sticky_cache_key = 'forum_sticky_' . $topic_id;
    $cached_sticky = dle_cache($sticky_cache_key, $config['skin']);

    if ($cached_sticky !== false && $cached_sticky !== '') {
        $sticky_html = $cached_sticky;
    } else {
        $first_post = $db->super_query("SELECT p.*, u.name as username, u.foto as avatar, u.user_group, u.signature, u.forum_post_count, u.forum_points, 
            r.title as rank_title, r.color as rank_color, r.icon as rank_icon 
            FROM " . PREFIX . "_forum_posts p 
            LEFT JOIN " . USERPREFIX . "_users u ON u.user_id = p.user_id 
            LEFT JOIN " . PREFIX . "_forum_ranks r ON r.id = u.forum_rank_id 
            WHERE p.topic_id = '{$topic_id}' AND p.is_deleted = '0' 
            ORDER BY p.id ASC LIMIT 1");

        if ($first_post) {
            $p_id      = intval($first_post['id']);
            $p_user    = htmlspecialchars($first_post['username'] ?: fl('forum_guest'), ENT_QUOTES, 'UTF-8');
            $p_avatar  = $first_post['avatar'] ? rtrim($config['http_home_url'], '/') . '/' . $first_post['avatar'] : $noavatar;
            $p_text    = $parse->BB_Parse(stripslashes($first_post['text']), false);
            
            $sticky_html = '
            <div id="post-' . $p_id . '" class="df-post df-post-sticky">
                <div class="df-sticky-label"><i class="fa fa-thumb-tack"></i> ' . fl('forum_original_post') . '</div>
                <div class="df-post-user">
                    <img src="' . $p_avatar . '" class="df-avatar">
                    <span class="df-post-username">' . $p_user . '</span>
                    <span class="df-op-badge" style="background:var(--df-accent); color:#fff; font-size:10px; padding:1px 5px; border-radius:3px;">' . fl('forum_topic_starter') . '</span>
                </div>
                <div class="df-post-main">
                    <div class="df-post-content">' . $p_text . '</div>
                </div>
            </div>';

            create_cache($sticky_cache_key, $sticky_html, $config['skin']);
        }
    }
}

$tpl->set('{posts_list}', $sticky_html . $posts_html);
$tpl->set('{pagination}', $pagination_html);
$tpl->set('{admin_toolbar}', $admin_toolbar);
$tpl->set('{lock_notice}', $lock_notice);
$tpl->set('{reply_error_block}', $reply_error_block);
$tpl->set('{follow_btn}', $follow_btn);
$tpl->set('{bump_btn}', $bump_btn);
$tpl->set('{upload_form}', $upload_form);
$tpl->set('{notif_btn}', $notif_btn);
$tpl->set('{breadcrumb}', $Forum->getBreadcrumb(intval($topic['cat_id']), $topic_id));
$tpl->set('{SITEURL}', $config['http_home_url']);

$tpl->compile('content');
$tpl->clear();

define('KSEARCH', false);
define('RSSPARSE', false);

$metatags['title'] = htmlspecialchars($topic['title'], ENT_QUOTES, 'UTF-8') . ' - Forum';
?>
]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/modules/forum_block.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
/*
=====================================================
 DLE Native Forum (Nexus) - by Dlehub
-----------------------------------------------------
 https://github.com/dlehub
-----------------------------------------------------
 Copyright (c) 2024-2026 Dlehub & Elegance
=====================================================
 This code is protected by copyright
=====================================================
 File: engine/modules/forum_block.php
-----------------------------------------------------
 Use: Ana sayfa veya sidebar için Son Konular (Recent Topics) bloğu
=====================================================
*/
if (!defined('DATALIFEENGINE')) {
    die("Hacking attempt!");
}

// SEO Fonksiyonlarını Yükle (Hata almamak için kontrol ekledik)
if (!function_exists('forum_url')) {
    include_once (DLEPlugins::Check(ENGINE_DIR . '/forum/forum.helper.php'));
}

// --- PARAMETRİK LİMİT ---
// Kullanım: {include file="engine/modules/forum_block.php?limit=12"}
// $_GET['limit'] DLE include mekanizması ile aktarılır.
// intval ile zorla integer yap, boşsa 5, bounds: 1-50 arası.
$limit = 5;

if (isset($_GET['limit'])) {
    $limit = intval($_GET['limit']);
}

if ($limit < 1) {
    $limit = 5;
}
if ($limit > 50) {
    $limit = 50;
}

// Cache key: skin + dil + limit kombinasyonu.
// Her limit değeri için ayrı cache — yanlış veri dönüşü yok.
$cache_key    = "forum_block_" . $config['skin'] . "_" . $config['langs'] . "_" . $limit;
$forum_block  = dle_cache($cache_key, $config['skin']);

if (!$forum_block) {
    $noavatar_path = rtrim($config['http_home_url'], '/') . '/templates/' . $config['skin'] . '/dleimages/noavatar.png';

    $db->query("SELECT t.id, t.title, t.alt_name, t.date, t.replies, u.name, u.foto, c.name as cat_name, c.alt_name as cat_alt_name
                FROM " . PREFIX . "_forum_topics t
                LEFT JOIN " . USERPREFIX . "_users u ON t.user_id = u.user_id
                LEFT JOIN " . PREFIX . "_forum_cats c ON t.cat_id = c.id
                WHERE t.is_deleted = '0'
                ORDER BY t.date DESC LIMIT " . $limit);

    $forum_block = "";
    $row_count   = 0;

    // Template'i döngü öncesi bir kez yükle
    $tpl->load_template('forum/forum_block.tpl');

    while ($row = $db->get_row()) {
        if (!is_array($row)) {
            break;
        }

        $row_count++;

        // XSS KORUMASI: Tüm kullanıcı verileri katı escape ile template'e aktarılır.
        $safe_title = htmlspecialchars($row['title'], ENT_QUOTES, 'UTF-8');

        $safe_name    = isset($row['name']) && is_string($row['name']) ? htmlspecialchars($row['name'], ENT_QUOTES, 'UTF-8') : fl('forum_guest');
        $safe_catname = isset($row['cat_name']) && is_string($row['cat_name']) ? htmlspecialchars($row['cat_name'], ENT_QUOTES, 'UTF-8') : '';

        $safe_id      = intval($row['id']);
        $safe_replies = intval($row['replies']);

        // Tarih: strtotime başarısız olursa bugünün tarihi kullanılır
        $timestamp = strtotime($row['date']);
        if ($timestamp === false) {
            $timestamp = time();
        }
        $safe_date = langdate("j F Y", $timestamp);

        // Avatar: foto varsa onu kullan, yoksa default noavatar
        $safe_foto = isset($row['foto']) && is_string($row['foto']) && $row['foto'] !== '' ? $row['foto'] : '';
        if ($safe_foto !== '') {
            // Dosya yolu injection kontrolü: ../ veya baştaki / engellenir
            $safe_foto = ltrim($safe_foto, '/');
            if (strpos($safe_foto, '..') !== false) {
                $safe_foto = '';
            }
        }
        $avatar = ($safe_foto !== '') ? rtrim($config['http_home_url'], '/') . '/' . $safe_foto : $noavatar_path;

        $safe_link = forum_url('forum_topic', [
            'newsid' => $safe_id,
            'news_name' => $row['alt_name'],
            'category' => $row['cat_alt_name']
        ]);

        $tpl->set('{title}', $safe_title);
        $tpl->set('{link}', $safe_link);
        $tpl->set('{author}', $safe_name);
        $tpl->set('{category}', $safe_catname);
        $tpl->set('{replies}', $safe_replies);
        $tpl->set('{date}', $safe_date);
        $tpl->set('{avatar}', $avatar);

        $tpl->compile('forum_block_content');
    }

    // Sorgu sonrası bellek temizliği
    $db->free();

    // Sonuç: En az bir kayıt bulunduysa compiled HTML'i al, boşsa sessizce geç
    if ($row_count > 0 && isset($tpl->result['forum_block_content']) && is_string($tpl->result['forum_block_content'])) {
        $forum_block = $tpl->result['forum_block_content'];
    } else {
        $forum_block = '';
    }

    // Template result'ı sıfırla — diğer modüllerle çakışmaz
    $tpl->result['forum_block_content'] = '';

    // Önbelleğe yaz
    create_cache($cache_key, $forum_block, $config['skin']);
}

echo $forum_block;
?>
]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/modules/profile.php">
		<operation action="before">
			<searchcode><![CDATA[$tpl->set( '{usertitle}', stripslashes( $row['name'] ) );]]></searchcode>
			<replacecode><![CDATA[
	// --- Forum Bilgileri Entegrasyonu (v1.1.1) ---
	$tpl->set( '{forum-post-count}', isset($row['forum_post_count']) ? intval($row['forum_post_count']) : 0 );
	$tpl->set( '{forum-points}', isset($row['forum_points']) ? intval($row['forum_points']) : 0 );
	
	$forum_rank_html = '';
	if(isset($row['forum_rank_id']) && intval($row['forum_rank_id']) > 0) {
		$fr_row = $db->super_query("SELECT title, color, icon FROM " . PREFIX . "_forum_ranks WHERE id = '" . intval($row['forum_rank_id']) . "'");
		if($fr_row) {
			$rc = isset($fr_row['color']) ? $fr_row['color'] : '#475569';
			$ri = isset($fr_row['icon']) ? $fr_row['icon'] : 'fa-user';
			$rt = isset($fr_row['title']) ? $fr_row['title'] : '';
			$forum_rank_html = '<span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-widest border" style="color:'.$rc.'; border-color:'.$rc.'30; background:'.$rc.'10"><i class="fa '.$ri.'"></i> '.htmlspecialchars($rt).'</span>';
		}
	}
	$tpl->set( '{forum-rank}', $forum_rank_html );
	// --- Forum Bilgileri Entegrasyonu ---
]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/forum/actions/user_posts.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
if(!defined('DATALIFEENGINE')) die("Hacking attempt!");

$user_name = isset($_GET['user']) ? urldecode($_GET['user']) : '';
$user_name = preg_replace('/[^a-zA-Z0-9ğĞüÜşŞıİöÖçÇ\s\-_]/', '', $user_name);

if(empty($user_name)) {
    msgbox(fl('forum_error'), fl('forum_err_user_not_found'));
    return;
}

$user_row = $db->super_query("SELECT user_id, name, foto FROM " . USERPREFIX . "_users WHERE name = '" . $db->safesql($user_name) . "'");

if(!$user_row) {
    msgbox(fl('forum_error'), fl('forum_err_user_not_found'));
    return;
}

$user_id = intval($user_row['user_id']);
$page = isset($_GET['page']) ? max(1, intval($_GET['page'])) : 1;
$per_page = 20;
$offset = ($page - 1) * $per_page;

$base_url = rtrim($config['http_home_url'], '/') . '/';
$noavatar = $base_url . 'templates/' . $config['skin'] . '/dleimages/noavatar.png';
$user_avatar = !empty($user_row['foto']) ? $base_url . $user_row['foto'] : $noavatar;

// Toplam mesaj sayısı
$total_posts = $db->super_query("SELECT COUNT(*) as c FROM " . PREFIX . "_forum_posts WHERE user_id = '$user_id' AND is_deleted = '0'");
$total_count = intval($total_posts['c']);

// Konuları ve mesajları birlikte çek
$posts = $db->super_query(
    "SELECT p.id, p.text, p.date, p.topic_id, t.title as topic_title, t.cat_id, c.name as cat_name, c.alt_name as cat_alt_name
     FROM " . PREFIX . "_forum_posts p
     LEFT JOIN " . PREFIX . "_forum_topics t ON t.id = p.topic_id
     LEFT JOIN " . PREFIX . "_forum_cats c ON c.id = t.cat_id
     WHERE p.user_id = '$user_id' AND p.is_deleted = '0'
     ORDER BY p.date DESC
     LIMIT $offset, $per_page",
    true
);

$posts_html = '';

if(empty($posts)) {
    $posts_html = '<div class="df-empty">' . fl('forum_no_user_posts') . '</div>';
} else {
    foreach($posts as $p) {
        $p_id = intval($p['id']);
        $p_topic_id = intval($p['topic_id']);
        $p_date = date('d.m.Y H:i', strtotime($p['date']));
        $p_title = htmlspecialchars($p['topic_title'] ?: fl('forum_deleted_topic'));
        $p_cat_name = htmlspecialchars($p['cat_name'] ?: '-');
        $p_cat_url = !empty($p['cat_alt_name']) ? forum_url('forum_cat', ['category' => $p['cat_alt_name']]) : '#';
        $p_text = mb_strimwidth(strip_tags(html_entity_decode($p['text'], ENT_QUOTES, 'UTF-8')), 0, 200, '...');
        $p_text = htmlspecialchars($p_text);
        
        $posts_html .= '
        <div class="card mb-2" style="border: 1px solid #e6e7eb;">
            <div class="card-body py-2 px-3">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <a href="' . forum_url('forum_topic', ['newsid' => $p_topic_id]) . '#post-' . $p_id . '" class="font-weight-bold" style="font-size: 14px;">' . $p_title . '</a>
                        <small class="text-muted d-block"><a href="' . $p_cat_url . '" class="text-muted">' . $p_cat_name . '</a> • ' . $p_date . '</small>
                        <p class="mt-1 mb-0 text-muted" style="font-size: 12px;">' . $p_text . '</p>
                    </div>
                    <a href="' . forum_url('forum_topic', ['newsid' => $p_topic_id]) . '#post-' . $p_id . '" class="btn btn-xs btn-outline-primary" style="border-radius: 15px;">
                        <i class="fa fa-arrow-right"></i>
                    </a>
                </div>
            </div>
        </div>';
    }
}

// Sayfalama
$total_pages = ceil($total_count / $per_page);
$pagination = '';
if($total_pages > 1) {
    $pagination = '<div class="text-center mt-3">';
    for($i = 1; $i <= $total_pages; $i++) {
        $active = ($i == $page) ? 'btn-primary' : 'btn-outline-primary';
        $pagination .= '<a href="' . forum_fix_url(forum_url('forum_user', ['user' => $user_name]), 'page=' . $i) . '" class="btn btn-sm ' . $active . ' mr-1" style="border-radius: 15px;">' . $i . '</a>';
    }
    $pagination .= '</div>';
}

$tpl->load_template('forum/user_posts.tpl');
$tpl->set('{breadcrumb}', $Forum->getBreadcrumb(0, 0, fl('forum_user_posts', $user_name)));

if(!isset($tpl->result['user_posts_tpl']) || empty($tpl->result['user_posts_tpl'])) {
    // Eğer şablon dosyası yoksa varsayılan HTML oluştur
    $output = '
    <div class="container mt-4">
        <div class="card mb15">
            <div class="card-body text-center">
                <img src="' . $user_avatar . '" style="width: 80px; height: 80px; border-radius: 50%; object-fit: cover; margin-bottom: 10px;">
                <h5>' . htmlspecialchars($user_name) . '</h5>
                <span class="badge badge-primary">Toplam Mesaj: ' . $total_count . '</span>
            </div>
        </div>
        <div class="card">
            <div class="card-header">
                <i class="fa fa-comments"></i> Forum Mesajları (' . $total_count . ')
            </div>
            <div class="card-body">
                ' . $posts_html . '
                ' . $pagination . '
            </div>
        </div>
    </div>';
    
    $tpl->result['content'] = $output;
} else {
    $tpl->set('{user_name}', htmlspecialchars($user_name));
    $tpl->set('{user_avatar}', $user_avatar);
    $tpl->set('{total_posts}', $total_count);
    $tpl->set('{posts_list}', $posts_html);
    $tpl->set('{pagination}', $pagination);
    $tpl->compile('content');
}

$tpl->clear();
$metatags['title'] = htmlspecialchars($user_name) . ' - Forum Mesajları';
?>]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/forum/forum.helper.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
if (!defined('DATALIFEENGINE')) die("Hacking attempt!");

/**
 * forum_url() — DLE 18 / 19+ Çift Uyumlu SEO Link Üretici
 *
 * DLE 19+  → DLEUrl::BuildUrl() kullanır (FastRoute motoru)
 * DLE 18   → .htaccess mod_rewrite kurallarına uygun URL üretir
 * Fallback → Her iki durumda da parametreli URL döner (SEO kapalıysa)
 *
 * Kullanım:
 *   forum_url('forum_main')
 *   forum_url('forum_cat',   ['category' => $alt_name])
 *   forum_url('forum_topic', ['newsid' => $id, 'news_name' => $slug, 'category' => $cat_alt])
 *   forum_url('forum_user',  ['user' => $username])
 *   forum_url('forum_pm_read', ['newsid' => $pm_id])
 */
function forum_url($rule, $params = []) {
    global $config;

    $base = rtrim($config['http_home_url'], '/') . '/';

    // ── DLE 19+: DLEUrl sistemi ──────────────────────────────────────────────
    if (class_exists('DLEUrl') && !empty($config['allow_alt_url'])) {
        $url = DLEUrl::BuildUrl('custom.' . $rule, $params);
        // BuildUrl başarısız olursa (kural yok, {placeholder} kaldıysa) fallback'e düş
        if (!empty($url) && strpos($url, '{') === false) {
            return $url;
        }
    }

    // ── DLE 18 + .htaccess modu veya SEO aktif / DLEUrl fallback ─────────────
    // SEO URL'leri aktifse (DLE 18'de $config['allow_alt_url'] == 1)
    if (!empty($config['allow_alt_url'])) {
        switch ($rule) {
            case 'forum_main':
                return $base . 'forum/';

            case 'forum_cat':
                $cat = isset($params['category']) ? urlencode($params['category']) : '';
                return $base . 'forum/' . $cat . '/';

            case 'forum_topic':
                $newsid    = isset($params['newsid'])    ? intval($params['newsid'])    : 0;
                $news_name = isset($params['news_name']) ? urlencode($params['news_name']) : 'konu';
                $category  = isset($params['category'])  ? urlencode($params['category'])  : '';
                // Kategori slug'ı olmadan da çalışsın
                if ($category !== '') {
                    return $base . 'forum/' . $category . '/' . $newsid . '-' . $news_name . '.html';
                }
                return $base . 'forum/konu/' . $newsid . '-' . $news_name . '.html';

            case 'forum_user':
                $user = isset($params['user']) ? urlencode($params['user']) : '';
                return $base . 'forum/user/' . $user . '/';

            case 'forum_new_topic':
                return $base . 'forum/new-topic/';

            case 'forum_search':
                return $base . 'forum/search/';

            case 'forum_notifications':
                return $base . 'forum/notifications/';

            case 'forum_pm':
                return $base . 'forum/pm/';

            case 'forum_pm_read':
                $id = isset($params['newsid']) ? intval($params['newsid']) : 0;
                return $base . 'forum/pm/read/' . $id . '/';

            case 'forum_ajax':
                return $base . 'forum/ajax/';
        }
    }

    // ── Fallback: SEO kapalı — klasik parametreli URL ─────────────────────────
    $query_map = [
        'forum_main'          => '',
        'forum_cat'           => '&action=category&cat='    . urlencode($params['category'] ?? ''),
        'forum_topic'         => '&action=topic&id='        . intval($params['newsid'] ?? 0),
        'forum_user'          => '&action=user_posts&user=' . urlencode($params['user'] ?? ''),
        'forum_new_topic'     => '&action=new_topic',
        'forum_search'        => '&action=search',
        'forum_notifications' => '&action=notifications',
        'forum_pm'            => '&action=pm',
        'forum_pm_read'       => '&action=pm&sub=read&id='  . intval($params['newsid'] ?? 0),
        'forum_ajax'          => '&action=ajax',
    ];

    return $config['http_home_url'] . 'index.php?do=forum' . ($query_map[$rule] ?? '');
}

/**
 * forum_fix_url() — Mevcut URL'e parametre ekler (& / ? otomatik seçer)
 */
function forum_fix_url($url, $param) {
    if (empty($url)) return '';
    $sep = (strpos($url, '?') === false) ? '?' : '&';
    return $url . $sep . $param;
}
?>]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/forum/actions/unanswered.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
if(!defined('DATALIFEENGINE')) die("Hacking attempt!");

$page     = isset($_GET['page']) ? max(1, intval($_GET['page'])) : 1;
$per_page = 25;
$total    = $Forum->countUnansweredTopics();
$topics   = $Forum->getUnansweredTopics($page, $per_page);

$list_html = '';
$noavatar = rtrim($config['http_home_url'], '/') . '/templates/' . $config['skin'] . '/dleimages/noavatar.png';

if($topics) {
    foreach($topics as $t) {
        $t_id      = intval($t['id']);
        $t_url     = forum_url('forum_topic', ['newsid' => $t_id, 'news_name' => $t['alt_name'] ?? '', 'category' => $t['cat_slug'] ?? '']);
        $t_title   = htmlspecialchars($t['title'], ENT_QUOTES, 'UTF-8');
        $t_user    = htmlspecialchars($t['username'] ?? fl('forum_guest', 'Misafir'), ENT_QUOTES, 'UTF-8');
        $t_date    = date('d.m.Y H:i', strtotime($t['date']));
        $t_views   = number_format(intval($t['views'] ?? 0));
        $t_replies = 0; 
        
        $last_user = $t_user;
        $last_date = $t_date;
        $last_avatar = !empty($t['avatar']) ? rtrim($config['http_home_url'], '/') . '/' . htmlspecialchars($t['avatar'], ENT_QUOTES, 'UTF-8') : $noavatar;

        $list_html .= '
        <div class="df-topic-row">
            <div class="df-col-icon"><i class="fa fa-question-circle df-icon-muted" style="color:var(--df-danger);"></i></div>
            <div class="df-col-main">
                <div class="df-title-row">
                    <a href="' . $t_url . '" class="df-title-link">' . $t_title . '</a>
                </div>
                <div class="df-topic-meta">' . $t_user . ' &bull; ' . $t_date . '</div>
            </div>
            <div class="df-col-stats">
                <div class="df-stat-num">' . $t_replies . ' <small>' . fl('forum_reply', 'Cevap') . '</small></div>
                <div class="df-stat-num">' . $t_views . ' <small>' . fl('forum_views', 'Görüntülenme') . '</small></div>
            </div>
            <div class="df-col-last">
                <img src="' . $last_avatar . '" class="df-last-post-avatar" alt="' . $last_user . '">
                <div class="df-last-post-wrap">
                    <a href="' . $t_url . '" class="df-last-title">' . $last_user . '</a>
                    <div class="df-last-meta">' . $last_date . '</div>
                </div>
            </div>
        </div>';
    }
} else {
    $list_html = '<div class="df-empty">' . fl('forum_no_unanswered', 'Yanıtlanmamış konu yok.') . '</div>';
}

$pagination = $Forum->pagination($total, $per_page, $page, forum_url('forum_main') . '?action=unanswered');

$tpl->load_template('forum/category.tpl');
$tpl->set('{breadcrumb}', $Forum->getBreadcrumb(0, 0, fl('forum_unanswered', 'Yanıtlanmamış Konular')));
$tpl->set('{cat_name}', fl('forum_unanswered', 'Yanıtlanmamış Konular'));
$tpl->set('{cat_desc}', fl('forum_unanswered_desc', 'Cevap bekleyen ve desteğe ihtiyacı olan konular.'));
$tpl->set('{sub_forums}', '');
$tpl->set('{mark_read_btn}', '');
$tpl->set('{new_topic_btn}', '');
$tpl->set('{topics_list}', $list_html);
$tpl->set('{pagination}', $pagination);
$tpl->set('{SITEURL}', $config['http_home_url']);

$tpl->compile('content');
$tpl->clear();

$metatags['title'] = fl('forum_unanswered', 'Yanıtlanmamış Konular') . ' - ' . fl('forum_forum', 'Forum');
?>
]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/forum/actions/active.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
if(!defined('DATALIFEENGINE')) die("Hacking attempt!");

$page     = isset($_GET['page']) ? max(1, intval($_GET['page'])) : 1;
$days     = isset($_GET['days']) ? max(1, min(30, intval($_GET['days']))) : 7;
$per_page = 25;
$total    = $Forum->countActiveTopics($days);
$topics   = $Forum->getActiveTopics($page, $per_page, $days);

$day_filter = '<div style="margin-bottom:15px; padding:0 16px;">';
foreach([1,7,30] as $d) {
    $active_cls = $days == $d ? 'btn-primary' : 'btn-default';
    $day_text = $d == 1 ? fl('forum_filter_today', 'Bugün') : sprintf(fl('forum_filter_days', 'Son %d Gün'), $d);
    
    $day_filter .= '<a href="' . forum_url('forum_main') . '?action=active&days=' . $d . '" class="btn btn-xs btn-raised ' . $active_cls . '" style="margin-right:5px;">';
    $day_filter .= $day_text;
    $day_filter .= '</a>';
}
$day_filter .= '</div>';

$list_html = '';
$noavatar = rtrim($config['http_home_url'], '/') . '/templates/' . $config['skin'] . '/dleimages/noavatar.png';

if($topics) {
    foreach($topics as $t) {
        $t_id      = intval($t['id']);
        $t_url     = forum_url('forum_topic', ['newsid' => $t_id, 'news_name' => $t['alt_name'] ?? '', 'category' => $t['cat_slug'] ?? '']);
        $t_title   = htmlspecialchars($t['title'], ENT_QUOTES, 'UTF-8');
        $t_user    = htmlspecialchars($t['username'] ?? fl('forum_guest', 'Misafir'), ENT_QUOTES, 'UTF-8');
        
        $last_date_raw = $t['last_post_date'] ?? $t['date'];
        $t_date    = (strtotime($last_date_raw) > 0) ? date('d.m.Y H:i', strtotime($last_date_raw)) : '-';
        
        $t_views   = number_format(intval($t['views'] ?? 0));
        $t_replies = number_format(intval($t['replies'] ?? 0));
        
        $last_user = htmlspecialchars($t['last_username'] ?? $t_user, ENT_QUOTES, 'UTF-8');
        $last_avatar = !empty($t['last_user_avatar']) ? rtrim($config['http_home_url'], '/') . '/' . htmlspecialchars($t['last_user_avatar'], ENT_QUOTES, 'UTF-8') : $noavatar;

        $is_unread = $is_logged ? $Forum->isUnread($t_id, intval($t['last_post_id'] ?? 0)) : false;
        $icon_cls  = $is_unread ? 'df-icon-unread' : 'df-icon-muted';
        $icon_style = $is_unread ? 'color: var(--df-accent);' : '';

        $list_html .= '
        <div class="df-topic-row">
            <div class="df-col-icon"><i class="fa fa-comments ' . $icon_cls . '" style="' . $icon_style . '"></i></div>
            <div class="df-col-main">
                <div class="df-title-row">
                    <a href="' . $t_url . '" class="df-title-link">' . $t_title . '</a>
                </div>
                <div class="df-topic-meta">' . $t_user . ' &bull; ' . $t_date . '</div>
            </div>
            <div class="df-col-stats">
                <div class="df-stat-num">' . $t_replies . ' <small>' . fl('forum_reply', 'Cevap') . '</small></div>
                <div class="df-stat-num">' . $t_views . ' <small>' . fl('forum_views', 'Görüntülenme') . '</small></div>
            </div>
            <div class="df-col-last">
                <img src="' . $last_avatar . '" class="df-last-post-avatar" alt="' . $last_user . '">
                <div class="df-last-post-wrap">
                    <a href="' . $t_url . '#post-' . intval($t['last_post_id'] ?? 0) . '" class="df-last-title">' . $last_user . '</a>
                    <div class="df-last-meta">' . $t_date . '</div>
                </div>
            </div>
        </div>';
    }
} else {
    $list_html = '<div class="df-empty">' . fl('forum_no_active', 'Bu dönemde aktif konu yok.') . '</div>';
}

$pagination = $Forum->pagination($total, $per_page, $page, forum_url('forum_main') . '?action=active&days=' . $days);

$tpl->load_template('forum/category.tpl');
$tpl->set('{breadcrumb}', $Forum->getBreadcrumb(0, 0, fl('forum_active', 'Aktif Konular')));
$tpl->set('{cat_name}', fl('forum_active', 'Aktif Konular'));
$tpl->set('{cat_desc}', fl('forum_active_desc', 'Belirtilen zaman diliminde güncellenen forum konuları.'));
$tpl->set('{sub_forums}', '');
$tpl->set('{mark_read_btn}', '');
$tpl->set('{new_topic_btn}', '');

$tpl->set('{topics_list}', $day_filter . $list_html);
$tpl->set('{pagination}', $pagination);
$tpl->set('{SITEURL}', $config['http_home_url']);

$tpl->compile('content');
$tpl->clear();

$metatags['title'] = fl('forum_active', 'Aktif Konular') . ' - ' . fl('forum_forum', 'Forum');
?>
]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
	<file name="engine/forum/actions/tag.php">
		<operation action="create">
			<replacecode><![CDATA[<?php
if(!defined('DATALIFEENGINE')) die("Hacking attempt!");

$slug = isset($_GET['slug']) ? preg_replace('/[^a-zA-Z0-9\-]/', '', $_GET['slug']) : '';

if(empty($slug)) {
    $all_tags = $Forum->getPopularTags(50);
    $tags_html = '<div class="df-tags-cloud" style="padding:20px; background:#fff; border:1px solid var(--df-border); border-radius:var(--df-radius);">';
    if($all_tags) {
        foreach($all_tags as $tg) {
            $size = min(20, max(11, 11 + intval($tg['count'])));
            $tags_html .= '<a href="' . forum_url('forum_main') . '?action=tag&slug=' . htmlspecialchars($tg['slug']) . '" class="df-tag" style="font-size:' . $size . 'px;display:inline-block;margin:4px;padding:3px 10px;border-radius:12px;background:#e2e8f0;color:#475569;text-decoration:none;">#' . htmlspecialchars($tg['name']) . ' <sup>' . intval($tg['count']) . '</sup></a>';
        }
    } else {
        $tags_html .= '<p>' . fl('forum_no_tags', 'Henüz etiket yok.') . '</p>';
    }
    $tags_html .= '</div>';

    $tpl->load_template('forum/category.tpl');
    $tpl->set('{breadcrumb}', $Forum->getBreadcrumb(0, 0, fl('forum_tags', 'Etiketler')));
    $tpl->set('{cat_name}', fl('forum_tags', 'Etiketler'));
    $tpl->set('{cat_desc}', fl('forum_tags_desc', 'Forumda en çok kullanılan etiketler listeleniyor.'));
    $tpl->set('{sub_forums}', '');
    $tpl->set('{mark_read_btn}', '');
    $tpl->set('{new_topic_btn}', '');
    $tpl->set('{topics_list}', $tags_html);
    $tpl->set('{pagination}', '');
    $tpl->set('{SITEURL}', $config['http_home_url']);

    $tpl->compile('content');
    $tpl->clear();
    
    $metatags['title'] = fl('forum_tags', 'Etiketler') . ' - ' . fl('forum_forum', 'Forum');
    return;
}

$page     = isset($_GET['page']) ? max(1, intval($_GET['page'])) : 1;
$per_page = 25;
$topics   = $Forum->getTopicsByTag($slug, $page, $per_page);

$list_html = '';
$noavatar = rtrim($config['http_home_url'], '/') . '/templates/' . $config['skin'] . '/dleimages/noavatar.png';

if($topics) {
    foreach($topics as $t) {
        $t_id      = intval($t['id']);
        $t_url     = forum_url('forum_topic', ['newsid' => $t_id, 'news_name' => $t['alt_name'] ?? '', 'category' => $t['cat_slug'] ?? '']);
        $t_title   = htmlspecialchars($t['title'], ENT_QUOTES, 'UTF-8');
        $t_user    = htmlspecialchars($t['username'] ?? fl('forum_guest', 'Misafir'), ENT_QUOTES, 'UTF-8');
        $t_date    = date('d.m.Y H:i', strtotime($t['date']));
        $t_views   = number_format(intval($t['views'] ?? 0));
        $t_replies = number_format(intval($t['replies'] ?? 0));
        
        $last_user = htmlspecialchars($t['last_username'] ?? $t_user, ENT_QUOTES, 'UTF-8');
        $last_date_raw = $t['last_post_date'] ?? $t['date'];
        $last_date = (strtotime($last_date_raw) > 0) ? date('d.m.Y H:i', strtotime($last_date_raw)) : '-';
        $last_avatar = !empty($t['last_user_avatar']) ? rtrim($config['http_home_url'], '/') . '/' . htmlspecialchars($t['last_user_avatar'], ENT_QUOTES, 'UTF-8') : $noavatar;

        $list_html .= '
        <div class="df-topic-row">
            <div class="df-col-icon"><i class="fa fa-tag df-icon-muted"></i></div>
            <div class="df-col-main">
                <div class="df-title-row">
                    <a href="' . $t_url . '" class="df-title-link">' . $t_title . '</a>
                </div>
                <div class="df-topic-meta">' . $t_user . ' &bull; ' . $t_date . '</div>
            </div>
            <div class="df-col-stats">
                <div class="df-stat-num">' . $t_replies . ' <small>' . fl('forum_reply', 'Cevap') . '</small></div>
                <div class="df-stat-num">' . $t_views . ' <small>' . fl('forum_views', 'Görüntülenme') . '</small></div>
            </div>
            <div class="df-col-last">
                <img src="' . $last_avatar . '" class="df-last-post-avatar" alt="' . $last_user . '">
                <div class="df-last-post-wrap">
                    <a href="' . $t_url . '#post-' . intval($t['last_post_id'] ?? 0) . '" class="df-last-title">' . $last_user . '</a>
                    <div class="df-last-meta">' . $last_date . '</div>
                </div>
            </div>
        </div>';
    }
} else {
    $list_html = '<div class="df-empty">' . fl('forum_no_topics_tag', 'Bu etiketle konu bulunamadı.') . '</div>';
}

$tpl->load_template('forum/category.tpl');
$tpl->set('{breadcrumb}', $Forum->getBreadcrumb(0, 0, '#' . htmlspecialchars($slug)));
$tpl->set('{cat_name}', '#' . htmlspecialchars($slug));
$tpl->set('{cat_desc}', fl('forum_tagged_topics', 'Bu etikete sahip konular'));
$tpl->set('{sub_forums}', '');
$tpl->set('{mark_read_btn}', '');
$tpl->set('{new_topic_btn}', '');
$tpl->set('{topics_list}', $list_html);
$tpl->set('{pagination}', ''); 
$tpl->set('{SITEURL}', $config['http_home_url']);

$tpl->compile('content');
$tpl->clear();

$metatags['title'] = '#' . htmlspecialchars($slug) . ' - ' . fl('forum_forum', 'Forum');
?>
]]></replacecode>
			<enabled>1</enabled>
		</operation>
	</file>
</dleplugin>